# 決定性は単一スレッドのビット一致を保証する

CPU・単一スレッド設定で、同一入力の求解がビット一致することを保証する。並列実行と Parquet 出力はビット一致を保証せず、単一スレッド解との値一致で検証する。並列性能を取る代わりに、組立・集約の順序を決定化し、単一スレッドを既定に据える。

Legacy source:
- `dev_docs/specs/構造計算一貫プログラム_実装設計書.md` R28・§5.2
