# 床領域・壁領域の再設計 申し送り

作成日: 2026-08-23
対象クレート: `squid-n-core` / `squid-n-load` / `squid-n-io` / `squid-n-edit` / `squid-n-app` / `squid-n-job` / `squid-n-mcp`

---

## 本書の位置づけ

**設計決定を記録した文書である。実装は Step 0（`check.rs` のレベル一致）・Step 1（`region_gen` の面走査）・
Step 2a（`FloorRegion` 型の導入）・Step 2b（領域名と取り付き領域の入力経路）・
Step 3（取り込み・準備計算での領域入れ直し）・**Step 4（床領域の再設計・荷重分配の作り替え。
D3 を §3.1 のとおり修正のうえ完了）**が完了している。

PR #218（[壁領域・床領域編集コマンド Step3](壁領域・床領域編集コマンド_Step3_申し送り.md)）で入れたデータモデルは、
前提が誤っていることが判明した。本書はその誤りの内容と、代わりに採る設計、
および移行の段取りを記録する。

**2026-08-24 追記（Step 4 着手時の設計修正）**: 実装を Step 4 まで進めた段階で、D3・D9・D19 が
再び誤っていたことが判明した。§3.1 に修正内容を記録する。あわせて、**「パネル」という呼称は
本書以降のコード・コメント・ドキュメントで禁止する**（建築構造で「パネル」は柱梁接合部
＝仕口パネルを指す語であり、床の床領域と衝突するため）。以下の §1〜§5.3 は Step 3 までの
決定過程をそのまま残すが、実装は §3.1 の修正を反映済みである。「パネル」という語が出てくる
箇所は Step 3 までの決定当時の呼称であり、現在のコードでは使わない
（`RegionBoundary` / `FloorRegion` / `Slab` と呼ぶ）。

[`specs/床領域と壁領域.md`](../specs/床領域と壁領域.md) は本書の決定によって設計方針が置き換わった
文書であり、§3.1 の修正を反映して全面改訂する必要がある（未着手。残課題）。

**2026-08-25 追記（壁側 Step 7+8 着手前の設計決定）**: §8.2 の未決事項（斜め構面の検出条件・
柱梁に囲まれない雑壁の受け皿・開口モードとの関係）と Step 7/8 の段取りを、利用者との dig
（対話）で確定した。§3.2 の E1〜E8 に記録する。**実装はまだ着手していない**（次の一手は
E8: 壁を含む実フィクスチャの新設と代表スカラのピン止め）。

**2026-08-26 追記（壁側 `region_gen` の実装。未結線）**: E1〜E3（構面のグルーピングと面走査、
ニューエルの公式による面積算定）を実装した。`WallRegion`/`WallPlate` 型の作り替え・要素生成
（D4）・モデルとの結線はまだ行っていない（下記 §5.7、§9 参照）。

**2026-08-26 追記（壁版の取り込み・要素生成・参照張り替え。§5.14）**: dig（7問）で
壁展開モデルのアーキテクチャを確定し、実装した。ST-Bridge取り込みを `WallPlate` 生成へ
切り替え、`squid_n_load::wall_expand::expand_wall_elements` が準備計算・解析実行・
ST-Bridge出力・GUI診断の直前に壁の解析要素を都度生成する（D5）。既存の壁消費者
（自重算定・保有水平耐力等）は壁展開モデルへ合成した `WallAttr` 経由で無改修のまま
動く。壁入りフィクスチャの代表スカラは型移行前後で完全一致を確認した。**残るのは
GUI の断面力表示・壁属性編集タブ・3Dビューアの壁対応（Step 9）と、取り付き壁版・
T字取り付き境界の一般化。**

**2026-08-27 追記（壁展開モデルを見ていなかった4箇所の是正。§5.15）**: §5.14の
実装をレビュースキルを使わない敵対的レビュー（利用者の指示）で検証したところ、
剛域自動算定（`rigid_zone_consider_walls`。既定 true）が壁展開前のモデルに対して
走っており、壁版経由の壁に対して恒久的に無効化されていたことが発覚した（全解析の
剛性を静かに誤らせる回帰）。dig で是正方針を確定し実装した。さらにこの是正後の
advisorレビューで同種の不具合が3件（耐震壁のせん断断面検定・数量拾い・保有水平
耐力の部材ランク自動判定）見つかり、いずれも実行して再現・是正・回帰テスト追加を
行った。詳細は §5.15 参照。

**2026-08-27 追記（3Dビューア・検定比図・モデル化図の壁対応。§5.17）**:
Step 9 のうち GUI 表示側の一部を実装した。`viewer::wall_expanded_view_model`
（壁版を持たないモデルでは複製せず借用する性能ガード付き）を新設し、3D形状表示・
検定比図・モデル化図が壁の解析要素を描けるようにした。成功条件は「壁が見える」まで。
応力図は壁内力を落とさず描くが材軸は下辺 2 節点のまま、ホバーは多角形ピックではない。
部材表・壁属性編集タブ・クリック選択/詳細ウィンドウ・ヒンジ図マーカーは未対応。
詳細は §5.17 参照。

**2026-08-27 追記（取り付き壁版の入力経路。§5.18）**: 残課題「取り付き壁版
（`Attached`）の入力経路が未整備」と壁側 D20 相当（§9）を dig（5問）で確定し実装した。
ST-Bridge 取り込みで、どの壁領域にも収まらず水平な辺が大梁に全長載る壁版を
取り付く壁版へ自動変換するようにし（床側 D20 の壁版版）、`squid-n-edit` に
`AddAttachedWallPlate` 等の編集コマンドを新設した。実装時に
`node_has_structural_ref`（D21 の節点削除判定）が壁版・壁領域を一切見ていなかった
既存の抜けも発見し是正した。詳細は §5.18 参照。

**2026-08-27 追記（T字取り付き境界の一般化を見送り。§5.19）**: 「実測55件中2件」の
根拠が再現可能なフィクスチャとしてリポジトリに存在せず（実データが壁版55枚では
なく壁領域の境界数だったと判明）、非4節点の壁版の実際の輪郭を検証できなかった。
壁エレメントの4節点24自由度の定式化を崩さないため、利用者との対話で「一般化しない
（Q6=C を維持）」と確定した。3箇所に重複していた「境界が4節点か」の判定は
`WallPlate::has_quad_boundary` へ集約した。詳細は §5.19 参照。

**2026-08-27 追記（敵対的レビューで発覚した2件の是正。§5.20）**: §5.18・§5.19の
実装をレビュースキルを使わない敵対的レビュー（利用者の指示）で検証したところ、
`Model::visit_node_ids`/`node_referenced_outside_elements` が `wall_regions` を
一切走査していなかったことが判明した。前者は `rebuild_wall_regions` 自身が新設した
節点削除処理と組み合わさって壁領域の境界をダングリング参照へ壊す重大な回帰
（再現テストで実際にpanicを確認）、後者は `squid-n-edit::DeleteNode` が壁領域だけが
参照する節点を誤って削除できてしまう軽微な回帰だった。両方とも修正前に失敗する
テストを書いて確認してから是正した。詳細は §5.20 参照。

**2026-08-27 追記（§5.20の根本原因を是正。§5.21）**: 「更新すべき箇所が3箇所に
分散し機械的に強制されない」という根本原因について、利用者の指示で是正した。
`node_referenced_outside_elements`（squid-n-edit の削除ガード）と
`region_rebuild::node_has_structural_ref`（D21 の削除判定）が同じ問いを独立に
重複実装していた部分を `Model::node_referenced_by_regions_or_plates` へ集約し、
全フィールド種別を1つずつ固定する網羅テストを新設した。`visit_node_ids`
（書き換え用）との整合は今回も機械的に強制されるわけではない（Rust に
リフレクション相当の仕組みがないため）。詳細は §5.21 参照。

---

## 1. 既存設計の何が誤っていたか

### 1.1 「床版は常に存在する」という前提が誤り

PR #218 は床領域を `Slab` そのものとし、`WallRegion` だけを独立した型にした。
根拠は「床版のない床領域は荷重も発生せず、小梁をまとめる意味がない」であった。

**これは誤りである。** 吹抜けに架かる繋ぎ小梁、階段室まわり、デッキを張らない範囲の小梁は、
床版がなくても小梁をまとめる単位として実在する。版なし壁領域を認めながら版なし床領域を認めないのは、
対称性の破れではなく単に前提の誤りである。

### 1.2 「壁版は解析要素である」という前提が誤り

`WallRegion.wall` は `Option<ElemId>` で、`validate` は `ElementKind::Wall` であることを確認している。

**これも誤りである。** 壁面には雑壁・仕上げ材のように解析要素にならない荷重があり、
壁版を解析要素に限ると受け皿がない。実際、現状のコードには次の欠落がある。

| 表現 | 実体 | 荷重の扱い |
|---|---|---|
| `Element`（`Wall`/`Shell`） | 解析要素（耐震壁） | 自重は ρ·t·(A−開口) ＋開口重量のみ。**仕上げ荷重を入れる欄がない** |
| `WallAttr` | 要素に紐づく開口・三方スリット | 自重の開口控除・剛性低減 |
| `MiscWall` | フレーム外雑壁（解析対象外） | `weight_per_area`（仕上げ込み）を 0.5m 分割で節点へ集計 |
| `WallRegion` | PR #218 で追加 | なし |

仕上げ荷重は線材（`MemberWeightCfg.w_f`）とスラブ（`Slab.loads`）にはあるが、壁要素にはない。

### 1.3 領域の粒度が定義されていなかった

最大の見落としはこれである。**領域は、床であれば大梁で囲まれた領域、壁であれば柱と梁で囲まれた領域ごとに 1 つ**でなければならない。
この不変条件がないと、荷重を主架構へ分配する処理が定義できない。

- 領域がパネルより細かい（小梁で切られた小片）と、境界辺の一部が小梁になり、分配先が主架構でなくなる
- 領域がパネルより粗い（複数パネルにまたがる）と、領域の内部に大梁があり、分配先を特定できない

PR #218 の設計にはこの制約がなく、そのため荷重分配の設計を持っていなかった。

---

## 2. 現状コードの問題点（実測）

`crates/squid-n-app/tests/fixtures/model.stb`（4 層＋PH の S 造。節点 166・解析要素 115・二次部材 56・スラブ 82）で実測した結果を記録する。

### 2.1 小梁設計が別階のスラブを掴んでいる（不具合）

`design_secondary_joist_checks`（`crates/squid-n-app/src/app/actions/design/check.rs:337`）は、
小梁中点が属するスラブを `point_in_slab_boundary` の **XY 平面のみ**で判定し、`find` で最初に見つかったものを採る。
`point_in_slab_boundary`（`crates/squid-n-load/src/floor/geometry.rs:72`）は XY へ投影した判定であり、Z を見ない。

**結果、56 本の小梁のうち 49 本が別レベルのスラブに割り当たっている。**
その小梁は、別階のスラブの板厚から出る固定荷重と、別階のスラブ形状から出る負担幅（`slab_perp_extent`）で設計されている。
なお本フィクスチャは全スラブが `usage = None`（積載荷重 0）のため、積載荷重の取り違えは今回は起きていない。
室用途が設定されたモデルでは、それも取り違える。

レベル一致を条件に加えると、別レベルへの帰属は 0 本になる。**ただしそれでも帰属は一意にならない。**
同一レベル内の該当枚数は次のとおりで、1 枚に決まる小梁は 1 本もない。

| 判定条件（いずれもレベル一致つき） | 該当 0 枚 | 1 枚 | 2 枚 | 3 枚 |
|---|---|---|---|---|
| 厳密内包（辺上を除く） | **56** | 0 | 0 | 0 |
| 内部または辺上（現行 `point_in_slab_boundary`） | 0 | 0 | 46 | 10 |

**小梁は必ずスラブの辺上に載る。** §2.2 のとおりスラブが小梁で分割されているためで、
厳密内包で判定すると 56 本すべてが該当なしになる（＝小梁設計が全滅する）。
複数該当したスラブの断面は 56 本すべてで同一のため、面荷重強度 w は同じで、
差が出るのは負担幅の算定に用いるスラブ形状だけである。

この不具合は移行と切り離し、**先に単独の `fix` PR で直す**（§5 Step 0。実施済み。
記録は [`v_and_v/小梁設計のスラブ帰属_レベル一致_2026-08.md`](../v_and_v/小梁設計のスラブ帰属_レベル一致_2026-08.md)）。
帰属の一意化は Step 0 では解けない。パネル単位の分配（D1・D9）へ移して初めて解ける。

### 2.2 荷重分配が入力データの細分に依存している

`auto_loads.rs:122` は、スラブ境界の辺 `LoadTarget::Edge(k)` について `find_beam(n0, n1)` で大梁要素を探し、
見つからなければ `LoadTarget::Span([n0, n1])` へ落として、最終的に辺の両端節点へ振り分ける。

フィクスチャのスラブ 82 枚の共有辺 55 本の内訳は次のとおりである。

| 共有辺の種別 | 本数 |
|---|---|
| 大梁がある辺 | 14 |
| **小梁がある辺** | **41** |
| 部材のない辺 | 0 |

つまり ST-Bridge のスラブは小梁で細分された小片であり、小片の辺荷重は**小梁の曲げを一切経由せず**
小梁の両端節点へ落ちている。そこから `squid-n-load/src/secondary.rs` の CMQ 経路で大梁の中間集中荷重になるため、
大梁への総和は保たれる。保たれるが、小梁は荷重伝達材として働いておらず、辺荷重を両端へ振る役をしているだけである。
小梁の曲げは分配経路に現れず、`check.rs` が幾何から負担幅を再導出して別計算している（それが §2.1 の不具合）。

**これが成立しているのは、ST-Bridge のスラブが既に小梁で細分された状態で入ってくるからにすぎない。**
パネル内に小梁があるのにスラブが分割されていない入力、および版のないパネルでは、この経路は何も出さない。

### 2.3 パネル単位へ統合できることの確認

大梁が囲むパネル数を、独立した 2 つの方法で求めた。

1. **Euler の公式**: レベルごとの水平大梁を平面グラフとみなし、内部面数 `F = E − V + C` を数える
2. **幾何隣接による統合**: スラブの辺が 100mm 以上重なる隣接を、その重なりを大梁が覆っていない場合に限り結合する（union-find）

| レベル Z [mm] | 水平大梁 V/E | Euler の内部面 | スラブ | 幾何統合後 | 断面混在 | 用途混在 |
|---|---|---|---|---|---|---|
| 200 | 12/17 | 6 | 14 | 6 | 0 | 0 |
| 4700 | 12/17 | 6 | 22 | 6 | 0 | 0 |
| 8700 | 12/17 | 6 | 22 | 6 | 0 | 0 |
| 12700 | 14/20 | 7 | 21 | 7 | 0 | 0 |
| 16500 | 4/4 | 1 | 3 | 1 | 0 | 0 |
| **計** | | **26** | **82** | **26** | **0** | **0** |

**2 つの方法が全レベルで一致し、82 枚 → 26 パネルへ統合できる。統合後に板厚（断面）または室用途が混在するパネルは 0 件である。**
パネル単位の 1 枚の版へ畳んでも情報は失われない。

**注意**: 辺の節点対が完全一致する隣接だけを結合する素朴な union-find では 41 クラスタとなり、真のパネル数と合わない。
大梁が節点で分割されている箇所（T 字取り付き）で辺の節点対が一致せず、同一パネル内が分断されるためである。

### 2.4 `region_gen`（Step 1 実装済み）による確認

面走査の実装（`squid-n-core/src/region_gen.rs`）でパネルを検出したところ、
§2.3 の 2 通りの見積もりと**完全に一致**した（レベル別 6 / 6 / 6 / 7 / 1、計 26）。
パネル面積の合計は各レベルのスラブ面積の合計と一致する（相対差 1e-6 未満）。

この真のパネルに対してスラブ片を割り当て直し、D3・D9 の前提を再測定した。

| 項目 | 結果 |
|---|---|
| パネル数 | 26（すべて版を持つ） |
| どのパネルにも割り当たらなかったスラブ | 0 枚 |
| パネルあたりのスラブ片数 | 1 枚: 3、2 枚: 4、3 枚: 5、4 枚: 14 |
| **断面が混在するパネル** | **0** |
| **室用途が混在するパネル** | **0** |

**パネル単位の 1 枚の版へ畳んでも情報は失われない**という D3・D9 の前提は、真のパネルでも成立する。

この測定は `crates/squid-n-app/tests/full_model.rs` の `slabs_fold_into_panels_without_mixing`
として固定した（判定は `Panel::contains` を用いる。自作の内外判定は辺上の点で誤るため、
実装が使う述語をそのまま用いること）。あわせて、節点を共有せずに交差する大梁が 0 組であること
（面走査が前提とする平面グラフが成り立っていること）も同テストで確認している。

---

## 3. 決定事項

利用者との設計検討（2026-08-23）で決めた内容を、決定単位で記録する。
D1〜D12 が領域の基本設計、D13〜D23 が主架構に囲まれない領域（片持ち・パラペット等）の扱いである。

### D1. 領域は「大梁で囲まれた領域 / 柱と梁で囲まれた領域」ごとに 1 つ（最重要）

床領域は水平面内で大梁が囲む閉領域（パネル）ごとに 1 つ、壁領域は鉛直構面内で柱と梁が囲む閉領域ごとに 1 つとする。
**これは他のすべての決定の前提であり、`validate` で守る不変条件とする。**
現時点の `validate` は**完全一致の重複だけ**を見る。幾何的な一意性の検査を広げるのは Step 3 であり、今波では `validate` を広げない。

この制約により、荷重分配の規則が幾何から一意に決まる。

- 境界辺は必ず主架構（大梁、または柱・梁）である。**囲まれた領域については** `find_beam` が外れる
  `Span` フォールバックが不要になる（取り付き領域は下記のとおり別途対応が要る）
- 領域内の部材は必ず二次部材である。「境界＝最終の受け手、内部＝中間の受け手」が幾何で確定する
- 版は 1 領域、二次部材も 1 領域にしか属さないため、二重計上が構造的に防げる
  （`specs/床領域と壁領域.md` が残課題に挙げていた「床をまたいだ小梁の重複」の検証は不要になる）

### D2. 版なし領域を認める（床・壁とも）

床領域・壁領域とも版を任意とする。`Slab` を床領域そのものとする設計は破棄する。

### D3. 領域が版の仕様を直接持つ

**⚠ この決定は Step 4 着手時に誤りと判明し、§3.1 のとおり覆っている（床側のみ。壁側の
`WallRegion`/`WallPlate` は本決定のまま Step 7〜9 へ持ち越す）。** 以下は決定当時の記述として残す。

領域は「幾何（境界）＋ 版の仕様（任意）＋ 二次部材」を持つ。
版の仕様には面荷重（自重・仕上げ）・開口・厚さ・材料が入る。

`Model.slabs` は `Model.floor_regions` へ置き換える。`MiscWall` と `WallAttr` は壁領域へ吸収する。
これにより、雑壁は「解析算入しない壁領域」、耐震壁は「解析算入する壁領域」として同じ型に収まる。

版を ID 参照（`Option<SlabId>` / `Option<ElemId>`）にする案は退けた。
スラブは解析要素ではないため 2 リストに分ける必然性がなく、分ければ
「版を消したときの `Option` 戻し・ID 繰り上げ」という `WallRegion.wall` と同じ保守が発生するためである。

### D4. 解析要素は領域から生成する派生物とする

入力の正は領域とし、剛性に算入する領域からは解析要素（`ElementKind::Wall`/`Shell`）を生成する。
領域と要素・断面に幾何と厚さが二重に存在する状態を作らない。

**代償**: 要素 ID が入力ではなく生成物になるため、断面力の表示・保有水平耐力の対象指定・ST-Bridge 出力といった
「要素 ID を参照先にしている経路」を、すべて領域 ID 起点へ張り替える必要がある。
現状の壁要素を直接編集する UI（`tables/wall_attrs.rs`・要素テーブル）は領域編集に置き換わる。

### D5. 生成は準備計算で行い、生成物はモデルに残さない

生成の置き場所は `App::ensure_preparation`（`crates/squid-n-app/src/app/preparation.rs:408`）とする。
既にモデルを触る同期処理（剛域の反映・DL/LL/EX/EY の同期）を冪等に行い、ハッシュでスキップ判定し、
モデル編集で `preparation_stale` が立つ枠組みを持っているためである。
解析入口ごとに書くと静的・固有値・増分・時刻歴で重複する。

生成した要素は `model.elements` へは入れず、展開済みの解析用モデルを派生データとして作る。
`io.rs:254` の「準備計算・解析結果はいずれもモデルから再計算できる派生データ」という既存原則に従う。
保存対象は領域のみで、開き直すと再生成される。生成は決定的（領域の順序で並べる）とし、同じ ID になるようにする。

**条件**: 展開関数は `squid-n-app` ではなく下位クレート（`squid-n-load` か新設）に置き、
GUI の準備計算とヘッドレス実行（`squid-n-job`・`squid-n-mcp`）の両方から同じ関数を呼ぶこと。

### D6. 二次部材の実体は領域が持つ

`Model.secondary_members` の横断リストは廃止し、`FloorRegion.joists: Vec<SecondaryMember>` /
`WallRegion.posts: Vec<SecondaryMember>` として領域が実体を持つ。参照は「領域 ID ＋ 領域内の添字」とする。

所属が構造的に必然になり、`validate` に規則を足さずに済む。
**あわせて、PR #218 で入れた `SecondaryMemberId` の「配列添字と一致」不変条件・`visit_secondary_member_ids`・
`retain_secondary_members` の ID 張り替えは、まるごと不要になる**（§6 参照）。
あの仕組みは横断リストと領域参照が二重に存在することから生じた保守負担であった。

**代償**: 二次部材を走査する箇所がすべて二段ループになる。非テストの走査箇所は §7 のとおり 10 箇所ほどで、
`Model` に「全二次部材を走査する反復子」を 1 本用意すれば多くはそれで置き換えられる。

### D7. 所属は「中点の厳密内包 ＋ Z 一致」で決める

二次部材の所属先は、中点がその**パネル**の内側にあり、**同じレベルにある**ことで決める。
辺上（`point_on_polygon_boundary`）は内包に含めない。含めると、辺を共有する隣接パネルの双方が該当するためである。

**この規則が使えるのはパネルに対してだけである。** 現行の小片スラブに当てはめると、
小梁は必ずスラブの辺上に載るため 56 本すべてが該当なしになる（§2.1）。
パネルの境界は大梁だけであり、小梁はその内部に入るため、厳密内包が成立する。

D1 によりパネルは平面を覆うため、版の有無に関わらず所属先は決まる。
それでも決まらない小梁（建物外周の外など）は診断に出し、黙って別の領域へ寄せない。

### D8. 領域は主架構から生成する

水平面ごとに大梁の閉路（平面グラフの面）を検出して床領域を、
鉛直構面ごとに柱・梁の閉路を検出して壁領域を作る。取り込んだ版は該当パネルへ統合する。

版から組み立てる案（共有辺に大梁がないスラブ片を結合する）は退けた。
版のあるところにしか領域ができず、**版のないパネルが領域として現れない**ためである。

`axis_gen::generate_axes`（柱位置から通り芯を自動生成し、`AxisSource::Auto`/`Manual` で再生成対象を分ける）を前例として、
`region_gen` を同じ位置づけで置く。

**申し送り**: AGENTS.md は「自作実装よりライブラリを優先」とするが、
**平面グラフの面抽出（最小閉路の列挙）を行う保守された Rust クレートは見当たらず、ここは自作になる見込みである。**
アルゴリズムは各節点で出辺を角度順に並べ、進入辺の次の辺を選んで一周する定型手法（面走査）で、実装量は 150 行程度。
外周面を捨てる処理が要る（**符号付き面積が負の面**として判別すること。面積の大小で判別すると、
中庭のある建物で内部面を誤って捨てる。§8.1 参照）。

### D9. 領域内の分配は「小梁で再分割して既存の三角形・台形分配」

**⚠ 「帰結」の一文（`JoistLine` 廃止）は §3.1 の修正で覆っている。`JoistLine` は
`FloorRegion.joists`（交差小梁の格子解析用の手入力）として存続する。** 小片への再分割自体は
D9 のとおり実装した（`squid-n-load::floor::distribute_region`）が、小片の実体は `FloorRegion`
ではなく新設した `Slab`（大梁または小梁で囲まれた版）である。

パネル境界と小梁で平面を再分割して小片を作り、各小片を既存の三角形・台形分配で境界（大梁または小梁）へ配る。
小梁が受けた分は単純梁の反力として境界大梁の中間集中荷重にする。

三角形・台形分配は日本の設計慣行で `docs/` に計算根拠が既にあり、その根拠を変えずに済む。
これは §2.2 でいま入力データが肩代わりしている細分を、プログラム側へ移す変更である。
小片の生成は D8 の面走査に小梁の辺を足すだけで、同じ実装を再利用できる。

負担面積法（`distribute_polygon_supported` のグリッドサンプリング）へ一般化する案は、
結果が変わり計算根拠の記述と検証記録を書き換えることになるため退けた。
床格子サブモデル（grillage）で全床を解く案は、`JoistLine.pinned_onto` を含む格子解析をすべての床に課すことになり、
計算コストと説明の複雑さに見合わないため退けた。格子は交差小梁がある領域に限った上乗せとして現状の位置づけのまま残す。

**帰結**: `JoistLine`（`Slab.joists`）は役目を終える。小梁の実体は領域が持ち（D6）、細分は面走査が行うためである。

### D10. 領域は保存対象とし、再生成は「作り直し」ではなく「入れ直し」

領域を保存対象（利用者入力の持ち主）とし、準備計算で主架構からパネルを再検出して
既存領域と**重心・レベルで対応付け**、版仕様と二次部材を引き継ぐ。

重心とレベルをキーにすると、梁が節点で分割されて境界の節点列が変わっても対応が付く（重心も面積も変わらないため）。
パネルが実際に割れた・融合したときだけ対応が外れる。そのときは利用者入力を引き継げないのが正しい挙動であり、
属性未設定の新規領域として現れ、診断に「引き継げなかった領域が N 件」と出す。

再生成時は全二次部材をいったん回収し、パネルを検出し直し、D7 の規則で入れ直す。二次部材そのものは消えない。

### D11. 領域は表示名を持つ

床領域・壁領域とも `name: String` を持たせる。現状の `Slab` は名前を持たず `SlabId` だけで、
ナビゲータでも番号でしか見えない。意図した版なし領域（「階段室」「吹抜け」）と、
所属を引き継げなかった領域を、フラグを増やさずに名前で区別できる。

### D12. ST-Bridge の持ち方には引きずられない

取り込みは「ST-Bridge の構造を写す」処理ではなく「正しいモデルへ変換する」処理と位置づける。
**結果として ST-Bridge との往復互換性が下がってもよい。**
出力で表現しきれないもの（版なし領域、パネル統合した版）は落ちてよい。

### D13. 領域には「囲まれた領域」と「取り付き領域」の 2 種別がある

床領域・壁領域とも、次の 2 種別を持つ。D1 の「囲まれた領域ごとに 1 つ」は前者にのみ適用する。

| 種別 | 境界の決まり方 | 生成 | 数の制限 |
|---|---|---|---|
| 囲まれた領域 | 主架構の閉路（大梁、または柱と梁） | 準備計算で自動生成（D8） | 1 閉路につき 1 つ（D1） |
| 取り付き領域 | 取付き先 ＋ 利用者が与える張り出し量 | 利用者入力・取り込み時の変換 | なし |

床の片持ちスラブ・バルコニー・出隅と、壁のパラペット・腰壁・垂れ壁・自立壁は、すべて取り付き領域である。
これらは **主架構に囲まれていない**点と、**輪郭が主架構から導けない利用者データである**点が共通しており、
D8 の閉路検出では生成できない。片持ちスラブの跳ね出し長さも、パラペットの立ち上がり高さも、
主架構のどこにも書かれていない。

版の仕様・二次部材・所属・編集コマンド・UI は 2 種別で共有する。違うのは境界の決まり方と荷重の出口だけである。

### D14. 取付き先は「線 / 点 / 床領域」の 3 つを取る

取り付き領域の取付き先は種類つきの値とし、次の 3 つを取る。

| 取付き先 | 内容 | 該当する現行の表現 |
|---|---|---|
| 線 | 節点対 `[NodeId; 2]` ＋ 区間 `[t_i, t_j]`（D22） | 片持ちスラブ（辺 0）、梁に載るパラペット・腰壁 |
| 点 | 節点 1 つ（柱） | 出隅スラブ（`distribute_corner` が `boundary[0]` へ全荷重集中） |
| 床領域 | 床領域の ID | 自立壁（床の上に立つ間仕切り等） |

**節点参照にするのは、節点移動に追随し、大梁を分割しても取り付きが外れないためである。**
現行の片持ちスラブは節点対の完全一致で受け手の大梁を探すため（`auto_loads.rs` の `find_beam`）、
大梁が分割されると一致せず `Span` フォールバックへ落ちる。`MiscWall` は絶対座標だけを持ち、
部材を一切参照しないため、節点を動かしても追随しない。どちらも新しい設計へ持ち込まない。

荷重の受け手は準備計算で幾何的に解決する。`squid-n-load/src/secondary.rs` の
`beam_span_position` / `resolve_nodal_to_primary` が既に「節点共有なしで梁のスパン中間に載る荷重」を
中間集中荷重へ変換しており、同じ経路に載せられる。

### D15. 取り付き領域の輪郭は「取付き線 ＋ 両端の張り出し量」

輪郭は `[d_i, d_j]`（床は跳ね出し長さ、壁は立ち上がり高さ）で持ち、矩形も台形も表せる。
現行の `distribute_cantilever` は先端 2 節点の取付き線からの距離を**平均**して使っており（台形は潰れている）、
`[d_i, d_j]` はそれを上回る情報量である。

輪郭を節点で持たないのは、**要素が接続しない節点をモデルへ増やさない**ためである。
そうした節点は `DofMap::build` が解析自由度から外す一方、モデルの節点一覧には残り続け、
ビューアと節点編集に「触れない節点」として並ぶ。現行の片持ちスラブがその状態にある。

L 形バルコニーのような不整形は、取付き線ごとに領域を分けて表す（取り付き領域に数の制限はない）。

### D16. 伝達規則は取付き先の種類で決まる

| 取付き先 | 荷重の出口 |
|---|---|
| 線 | 「取付き線へ分布」または「取付き線の両端の柱へ集中」（利用者が選ぶ） |
| 点 | その節点（柱）へ集中（一択） |
| 床領域 | その床領域へ渡す（一択。D17） |

出隅スラブの「全荷重を柱へ集中」と、雑壁の柱伝達（`MiscWallTransfer::Column`）は、
規準上の根拠こそ違え荷重の出口としては同じ操作なので、ひとつの規則に統一する。
`MiscWallTransfer::Beam` は「取付き線へ分布」、`SelfStanding` は「床領域へ渡す」に対応する。

### D17. 床領域の上に立つ壁の荷重は等価な面荷重へならす

自立壁（間仕切り等）の総重量は、所属する床領域の面荷重へ等価換算して加え、床の分配にそのまま乗せる。
間仕切り壁の重量を床の固定荷重へ等価等分布として含めるのは実務の慣行であり、`docs/` に計算根拠として書ける。

位置に依存する厳密さが要る壁（パラペット・腰壁・垂れ壁）は D14 の「線」で大梁に取り付くため、
この経路に来るのはパネルの内側に立つ壁だけで、位置の影響は小さい。

**限界として、面荷重へならした時点で壁の位置情報は失われる。** 重い壁がパネル中央にある場合、
近い大梁は過小、遠い大梁は過大になる（総和は保たれる）。この既定は `docs/` の床荷重分配の節に明記し、
厳密に扱いたい場合は壁の直下に小梁を入れて主架構へ取り付ける運用とする。

### D18. `Slab.edge_supported` は廃止し、支持辺は幾何から導く

辺上に主架構の部材（大梁・片持ち梁）または領域が持つ二次部材（先端リブ小梁）が実在すれば支持辺とする。

この欄が担っていた 2 用途は、いずれも新しい設計では構造的に消える。
一般スラブの「開口際で支持されない辺」は D1 で消える（パネルの境界は定義上すべて大梁で、
吹抜けはパネルそのものが版なし領域になる）。片持ちの「片持ち梁・先端リブ小梁の支持辺追加」は
D6 で消える（片持ち梁は実部材、先端リブは領域が持つ二次部材で、実在を見れば決まる）。

なお **`edge_supported` には現状書き手が存在しない**。非テストの出現箇所はすべて `None` の初期化で、
UI・取り込み・編集コマンド・MCP のいずれも値を設定せず、利用者向け `docs/` にも記載がない。

`distribute_polygon_supported`（最近接支持辺の負担面積で分割）は捨てずに残す。
片持ち領域に片持ち梁・先端リブ小梁がある場合の分配経路として必要で、変わるのは支持辺の決め方だけである。

### D19. `SlabKind` は廃止する

`Interior` はパネル領域、`Cantilever` は取付き先が線の取り付き領域、`Corner` は取付き先が点の
取り付き領域に対応するため、種別を表す欄が二重になる。数量積算の「片持ち床」分類
（`quantity/mod.rs:1142`）も「取り付き領域である」ことから導く。

種別を残すと「出隅なのに線へ分布」のような無意味な組み合わせを表現できてしまう点も避けられる。

### D20. 取り込みでパネルに収まらない版は片持ちと自動判定する

ST-Bridge 取り込みは現状 `kind` を常に既定（`Interior`）で入れるため、バルコニーも一般スラブとして
取り込まれ、大梁のない辺へ配られた荷重は `Span` フォールバックで両端節点へ落ちている。

D8 でパネルを検出したうえで、**どのパネルにも収まらず、1 辺が大梁と重なる版は片持ち**として
取り付き領域へ変換する（`[d_i, d_j]` は取付き線からの距離）。判定できなかった版
（どの大梁とも辺を共有しない宙に浮いた版）は診断に出し、黙って落とさない。

落とす・対象外にする案は採らない。**バルコニーの荷重がモデルから消え**、長期荷重にも
地震用重量にも入らなくなるためである。

### D21. 畳んだ結果、参照が 0 になった節点は削除する

D15 で輪郭を `[d_i, d_j]` に畳むと、版の先端節点は誰からも参照されなくなる。
取り込み時にこれを削除する。ただし他の要素・二次部材・支点・領域が参照している節点は残すため、
削除は**参照が 0 になった節点のみ**に限る。

### D22. 部分的な取り付きは区間で表す

取付き線は節点対に加えて区間 `[t_i, t_j]`（取付き線上の無次元位置 0.0〜1.0、既定は全長 `[0.0, 1.0]`）を持つ。
腰壁が開口部で切れる、パラペットが階段室の手前で終わる、バルコニーがスパンの途中までしか出ていない、
といった形は実務で普通に現れる。

無次元にするのは、節点が動いて取付き線が伸縮しても割り付けが相対位置として保たれるためである。
区間の両端が梁のスパン中間に来ても、D14 の幾何解決で中間集中荷重・部分分布荷重として渡せる。

大梁上に節点を追加して表す案は、D15 と同じ理由（解析に不要な節点を増やす）で採らない。

### D23. 地面に直接立つ塀は対象外とする

床領域の上にない自立壁は、建物の架構へ荷重を渡さないため対象外とし、取り込み時に警告して落とす。

---

## 3.1 Step 4 着手時の設計修正（2026-08-24）

Step 4（D9: 領域内の分配を小梁で再分割）を実装する段階で、D3（領域が版の仕様を直接持つ）が
再び「床版は 1 領域につき 1 枚」という誤った前提に戻っていたことが判明した。

### 何が誤っていたか

D3 は「床領域＝大梁が囲む 1 区画」と「版（面荷重・厚さ・室用途を持つ実体）」を同一の型に
畳んだ。これは Step 2a〜3 の実装（`FloorRegion::plate: Option<SlabPlate>`）ではおおむね
成立していたが、**D9 で「小梁で再分割して既存の三角形・台形分配」を実装しようとすると
破綻する**。床領域内が小梁でさらに細かい打設単位（厚さ・室用途が違う版）に分かれている場合、
「床領域＝版」では 1 つの `FloorRegion` に複数の版仕様を持たせる場所がない。

加えて、床領域＝区画という決定は、大梁の 1 スパン区画と、その中の小梁・版という
利用者の実際のメンタルモデル（「床領域は大梁の1スパン区画。この中に小梁やスラブがある。
スラブは大梁または小梁で囲まれた領域に定義される」）とも一致しない。

### 修正内容

**`FloorRegion`（床領域）と `Slab`（床板）を再び別の型に分離する。** ただし D3 が退けた
「ID 参照にすると `Option` 戻し・ID 繰り上げの保守が発生する」という懸念は、
`FloorRegion` 側が `Vec<SlabId>` で複数の床板を保持する設計（一方向の参照、`Option` を使わない）
にすることで避ける。

| 型 | 持つもの | 対応する現行実装 |
|---|---|---|
| `FloorRegion` | 名前（D11）・境界（大梁の閉路。D1・D8）・所属する小梁 ID 一覧・所属する `Slab` ID 一覧（`slab_ids: Vec<SlabId>`）・手入力小梁ライン（`joists: Vec<JoistLine>`。交差小梁の格子解析専用） | `crates/squid-n-core/src/model/region.rs` |
| `Slab` | 形（`SlabShape::Enclosed { boundary }` = 大梁または小梁で囲まれた版 / `SlabShape::Attached { anchor, extent }` = 取り付く版）＋ 版の仕様（`SlabPlate`: 断面・仕上荷重・室用途・分配方法・一方向） | `crates/squid-n-core/src/model/slab.rs` |

**1 つの `FloorRegion` は複数の `Slab` を持ちうる**（床領域内が小梁でさらに細かい打設単位に
分かれている場合）。取り付く `Slab`（片持ち・バルコニー・出隅）はどの `FloorRegion` からも
参照されない独立した床板として存在する（D13 のとおり）。

`JoistLine`（D9 の「帰結」で廃止予定としていたもの）は **廃止しない**。手入力の交差小梁
格子解析・矩形床板の二段階伝達は `FloorRegion.joists` として存続する。二次部材の実体
（D6）は依然 `Model.secondary_members` の横断リストのままで、D6 は引き続き保留である
（§8.4 に統合。理由は変わらず、`FloorRegion` が複数の `Slab` を持ちうる現状では
「中点の厳密内包」が `Slab` 単位でしか一意にならないため）。

### 荷重分配（D9）の実装

`squid-n-load::floor::distribute_region(model, region, w_of)` を分配の入口とする。

- `region.joist_lines()` が空でなく、代表床板（`region.slab_ids` の先頭）が矩形かつ
  分配法が三角形・台形または一方向なら、既存の `distribute_rect_with_joists`（手入力小梁の
  二段階伝達）を使う
- それ以外は `region.slab_ids` の各 `Slab` を独立に `distribute_slab_w` で分配する
  （`Slab` は大梁または小梁で囲まれた版であり、その境界辺がそのまま分配先になる）
- どの `FloorRegion` からも参照されない `Slab`（取り付く版・浮き床板）は
  `distribute_slab_resolved` で個別に分配する（`squid-n-job::auto_loads::slab_beam_loads_with`
  が `FloorRegion` 経由と `Slab` 直接の 2 経路を統合する）

`LoadTarget::Edge(k)` は `Slab` の文脈で解決する（`resolve_edges_to_span`）。1 つの
`FloorRegion` が複数の `Slab` を束ねるため、辺インデックス `k` は「どの `Slab` の境界か」に
よって別の実節点対を指すことに注意。

### 影響を受けた決定・廃止予定の取り消し

- **D3 は上記のとおり修正。** 「版を ID 参照にする案は退けた」の判断を覆す
  （床領域が複数の版を持ちうるため、1 対 1 の畳み込みでは表現できない）
- **D9 の「帰結」（`JoistLine` 廃止）を取り消す。** `FloorRegion.joists` として存続する
- **§6「廃止・削除の対象」の `Model.slabs` / `Slab` 行を取り消す。** `Slab` は名前を変えて
  存続する（床領域そのものではなく、床領域内の版という意味に変わった）
- D19（`SlabKind` 廃止）・D13〜D18・D20〜D22 は影響を受けない（`Slab` の形・取付き先・
  分配方法として引き続き成立する）

### 用語: 「パネル」の使用禁止

建築構造で「パネル」は柱梁接合部（仕口パネル、`crates/squid-n-element` の `PanelZone` 等）を
指す語であり、床の床領域にこの語を使うと衝突する。§1〜§3（D23 まで）は決定当時の記述として
「パネル」を残すが、コード・コメント・以降のドキュメントでは使わない。対応する現在の呼称は
次のとおり。

| 旧称（本書 §1〜§3 での呼称） | 現在の呼称 |
|---|---|
| パネル（`Panel`/`PanelScan`） | 大梁の区画（`RegionBoundary`/`RegionBoundaryScan`。`region_gen` の内部型） |
| `scan_floor_panels` / `generate_floor_panels` | `scan_region_boundaries` / `generate_region_boundaries` |
| パネル単位の版 | 床領域（`FloorRegion`）または床板（`Slab`）。文脈により使い分ける |

---

## 3.2 壁領域（Step 7 着手前）の設計決定（2026-08-25、dig による合意）

§8.2 の未決事項（斜め構面の検出条件、柱梁に囲まれない雑壁の受け皿、開口モードとの関係）と、
Step 7/8 の段取りについて、利用者との対話（dig スキル）で合意した内容を記録する。
実装はこれから行う（本節は着手前の設計記録）。

### E1. 鉛直面のグルーピングは通り芯（AxisGroup）に依存しない新規の幾何判定とする

床の面走査は「同じレベル Z にある水平大梁だけ」を先に絞り込んでから面走査をかけている
（D8・`region_gen.rs`）。壁でこれに相当する絞り込み（「同一鉛直面に乗る柱・梁だけを集める」）は、
床の「Z が一致するか」ほど自明ではない。面走査アルゴリズム自体が「対象の辺をすべて 1 つの
2D 平面へ投影して角度順にソートする」処理を前提にするため、面を先に固定するステップは省略できない
（1 本の柱には通常 4 方向の梁が取り付き、それぞれ異なる鉛直面に属する。面を固定せずに
「柱・梁が閉じたループになっていれば十分」とすると、同一平面上にない部材どうしでも
グラフ上の閉路として拾ってしまう）。

**グルーピング規則**: 全柱の上下節点ペアから鉛直な芯線候補（XY 平面上の直線）を列挙し、
各芯線上にある柱・梁を [`crate::geom::MEMBER_AXIS_TOL_MM`]（10mm）の許容差で拾う。
直交グリッドか斜めかを問わず同じロジックで扱う。通り芯（`AxisGroup`）とは独立の判定とする
（`AxisGroup` は「構造計算には一切用いない」表示専用のデータであり、利用者が通り芯を
手動編集・削除しても壁領域の検出結果が連動して変わってはならないため）。

**新規実装が必要。** `region_gen.rs` は床（水平面）のみ実装済みで、壁（鉛直面）のグルーピングは
本節が最初の設計決定である。

### E2. 壁領域は「1 閉路＝1 平面」が前提。非平面な壁面は自然に複数領域へ分解される

E1 の帰結として、**壁領域の境界検出は常に厳密な平面（1 つの法線で表せる面）を対象とする**。
これは面走査アルゴリズムの数学的な前提であり、変更できない（床の「対象は水平な大梁のみ」と
同じ制約）。

現実の壁面が鉛直方向に完全な平面でない場合（芯ずれ、途中で折れ曲がる壁列、傾いた壁面など）は、
1 つの `WallRegion` にはならず、**平面ごとに複数の `WallRegion` へ自然に分解される**。
これは床の段差床が「同じ階でも別の平面グラフになる」のと同じ扱いであり、L 形バルコニーが
取付き線ごとに複数の取り付き領域に分かれる D15 の考え方とも一貫する。特別扱いや例外処理を
新設するのではなく、E1 のグルーピング規則がそのまま複数の面走査対象を作ることで実現する。

**この制約を実装時に見失わないこと**（利用者からの明示的な申し送り）。壁領域の設計・実装の
どこかで「1 領域は必ず 1 つの平面上にある」という前提を緩めるような近道（例: 複数面の節点を
まとめて 1 つの `WallRegion` にする）を取らないこと。

### E3. 壁領域の面積・荷重計算は、理想平面への投影を経由せず 3 次元座標から直接求める

E2 のグルーピング許容差（`MEMBER_AXIS_TOL_MM` = 10mm）は、床の `LEVEL_TOL_MM`（1mm）より
1 桁緩い。床は許容差が小さいため「理想平面へ投影した 2D 座標から面積を出す」現行実装
（`RegionBoundary::polygon`／`area`。Z を捨てて XY のシューレース公式）でも実務上誤差は
無視できるが、壁で同じ手法を使うと、芯ずれ・非平面性による誤差（壁スパンによっては
無視できない大きさ）が自重・仕上げ荷重（面積 × w）に混入する。

**壁側の面積・荷重計算は、理想平面へ投影せず、実際の 3 次元節点座標からニューエルの公式
（3D 多角形面積）で直接求める。** 面走査（閉路検出のための角度ソート）は引き続き E1 の近似法線を
使ってよい（トポロジーの決定にのみ用いるため）。用途によって「近似平面でよい処理」と
「実座標が必要な処理」を分けること。

### E4. `MiscWall`（自由座標の雑壁）は節点参照へ完全移行し、自由座標入力は廃止する

現行の `MiscWall` は `start`/`end` の絶対座標のみを持ち、節点・部材への参照が一切ない
（ST-Bridge 取り込み経路には `MiscWall` を生成するコードが存在せず、実フィクスチャにも
`MiscWall` は 0 件。単体テストの合成データのみ）。D14（取付き先は 線／点／床領域 の 3 つで、
節点参照にする）と現行の自由座標入力は直接矛盾する。

AGENTS.md が「後方互換性は維持しない」と明記しており、実データが存在しないため移行ロジック
（既存の自由座標を最近傍アンカーへスナップする処理）は不要と判断し、**新規の壁領域入力
（Step 9 UI・MCP・ST-Bridge 取り込み）は D14 の 3 アンカー（線／点／床領域）を必須とし、
アンカーを持たない自由座標の壁は作れない仕様にする。**

**帰結**: §8.2 の「柱梁に囲まれない雑壁の受け皿」は本決定で自動的に解消する。取付き先が
必ず存在するため、「囲まれない」かつ「取り付き先もない」状態自体が作れなくなる
（地面に直接立つ塀は D23 で対象外と既に決まっている）。

**実装範囲への影響**: `MiscWall`（`start`/`end` の自由座標）を直接構築している非フィクスチャの
単体テストが 3 ファイルにある。E4 着手時はこれらもアンカー付きの新表現へ書き換える対象に含めること。

- `crates/squid-n-load/src/story_gen/tests.rs:1446,1531`
  （`test_misc_wall_beam_transfer_conserves_total_weight`／`test_misc_wall_column_transfer_splits_to_column_ends`。
  `MiscWallTransfer::Beam`／`Column` の 0.5m 分割集計を数値で検証している。D16 のアンカー種別への
  写像後も同じ検証数値が再現できるかを併せて確認すること）
- `crates/squid-n-design-jp/src/secondary/eccentricity/tests.rs:241,250,259`
  （偏心率算定における雑壁剛性寄与のテスト）

### E5. `WallRegion` は複数の `WallPlate` を持ちうる設計とする（D3 の教訓を先取りする）

§3.1 は、床領域を最初「1 床領域＝1 版」（D3）と決めたのちに「1 床領域は複数の版を持ちうる」
（`Vec<SlabId>`）へ手戻りした経緯を記録している。壁でも同じ状況が起こりうる
（1 つの柱・梁パネル内に、間柱で実際には分割された壁、異なる厚さ・仕様の雑壁が隣接する等）。
ST-Bridge の `StbWall` は親子関係を表すタグを持たず、床のスラブ片と同様に、1 つの
柱・梁パネル内に複数の壁要素が幾何的に収まる入力が普通にありうる。

**同じ手戻りを避けるため、`WallRegion` は最初から `Vec<WallPlateId>`（複数の壁版）を
持てる設計とする。**

### E6. 開口モード（`MultiOpeningMode`）は引き続き `Model` 全体で 1 つのグローバル設定のまま

開口データ（`WallOpening`）の持ち主が `WallAttr` から `WallPlate`（`WallRegion` の一部）へ
移っても、複数開口の取り扱い（`Model.multi_opening_mode`）は建物全体で一律のままでよいと判断した。
壁ごとに評価方式を変えたいという利用者ニーズは想定していない。**帰結**: §8.2 の
「開口モードとの関係」は影響なしと確認でき、`WallPlate` に `MultiOpeningMode` フィールドは追加しない。

### E7. Step 7 と Step 8（D4）は分離せず一括で実施する

当初の Step 表は Step 7（型の作り直し・`MiscWall`/`WallAttr` 吸収。壁要素は
引き続き `Model.elements` に直接入力されたまま）と Step 8（D4: 解析要素を準備計算からの
生成物にする）を分けていた。しかし Step 7 だけで区切ると、`WallPlate` が
「引き続き手入力される `Element::Wall` への一時的なつなぎ（`elem: Option<ElemId>`）」を
一時的に持つことになり、Step 8 でこのつなぎを剥がして生成方式へ切り替える二度手間が生じる。

**利用者の判断により、Step 7・8 は分離せず一括で実装する。** `WallRegion`/`WallPlate` を
入力の正とし、剛性に算入する壁の解析要素は準備計算（D5・`App::ensure_preparation`）で
生成する形を最初から実装する。断面力表示・保有水平耐力の対象指定・ST-Bridge 出力の
参照先の張り替え（§8.4）も同じ波に含む。

### E8. 実装着手前に、壁を含む実フィクスチャを新設し、現行動作の代表スカラをピン止めする

床側 Step 4 は実フィクスチャ（`crates/squid-n-app/tests/fixtures/model.stb`、82 枚のスラブ）の
`snapshot_key_scalars` を安全網として、型再分離が生んだ回帰を含む既存バグ 6 件を検知できた
（§5.4）。**壁側は実フィクスチャに `Element::Wall` が 0 件で、`full_model.rs` に壁の
スナップショットが 1 つもない**（`WallAttr`・`wall_nonlinear`・`joint_wiring` 等の壁関連機能は
すべて単体テストのみで検証されている）。

E7 のとおり Step 7・8 を一括で行うと、断面力表示・保有水平耐力・ST-Bridge 出力の参照先切り替えを
回帰検知網なしで行うことになる。これは `dev_docs/handoff/README.md` が記録する P7・P8 の
「非デフォルト機能が実装済みと申告されながら実際には壊れていた」再発パターンと同じ危険を持つ。

**着手前に、壁を含む最小実フィクスチャを新設し、型変更前の現行動作をピン止めするテストを
追加する。** これを Step 7・8 の Step 0 相当（床の §5 Step 0 に対応）として最初に行う。

**2026-08-25、実施済み。** `crates/squid-n-app/tests/wall_model.rs` を新設した。
`Model.floor_regions`（26 件、`full_model.rs` の既存フィクスチャ）を巻き込まない独立の
最小モデル（柱 4 本・梁 4 本・耐震壁 1 枚〔開口あり〕・フレーム外雑壁 1 本、1 スパン 1 層）を
`App` の全解析入口（`run_preparation`／`run_static_all`／`run_eigen`）に通し、
`insta`（`full_model.rs::snapshot_key_scalars` と同じ仕組み）で代表スカラをスナップショットした
（`snapshot_wall_bay_scalars`。地震用重量・固有周期・耐震壁要素の材端力〔DL・EX 両方〕・
EX 荷重時の柱頭水平変位）。`cargo clippy -p squid-n-app --all-targets --locked -- -D warnings`
を通過済み。

**確認した注意点**: 耐震壁要素の材端力（面内せん断剛性・開口低減の回帰検知に使う想定だった値）は、
DL（対称な架構への鉛直荷重）単体ではほぼゼロになる（この対称形状では壁柱換算の軸力・曲げが
小さいため。固有周期が壁の有無で 0.0797s→0.0522s と動くことから、壁は剛性には確実に効いている。
`test_wall_element_changes_eigen_period` で固定）。壁の面内せん断・開口低減を実際に動かすのは
地震荷重（EX）であり、スナップショットは EX 側の材端力・柱頭水平変位も記録する。
**次にこのフィクスチャへ手を入れる際、「動かない代表値」を回帰検知の根拠に使わないこと**
（値が動かない列は型変更の影響を検知できず、検知網があるという誤った安心感だけを生む）。

**このフィクスチャに含まれないもの（未着手）**: D13〜D16 の取り付き領域（パラペット・腰壁等の
アンカー付き壁）は、現行コードにその型がまだ存在しないため含められない（ベースラインを
ピン止めする対象がまだない）。E1〜E7 の型実装後、取り付き領域を含む代表値を別途追加すること。
また `保有水平耐力`（Qu・τu・Ds 群判定）は現状すべて単体テストのみで検証されており、
このフィクスチャでは通していない（プッシュオーバー・Ds 群判定を最小構成で通す準備には
別途作業が要るため、E8 のスコープからは意図的に外した。Step 8 の参照張り替えで
保有水平耐力の対象指定を壁領域 ID 起点へ切り替える際は、通す作業を先に行うこと）。

## 3.3 用語の改名（2026-08-26）

レビューで見つけた名前衝突を修正した。**同じ Rust 識別子 `MiscWall` が、別クレート・
別モジュールに別概念として 2 つ存在していた**（両方とも本書 §1〜§3.2 では旧称
`MiscWall` のまま登場する）。

| 旧称 | 新称 | 意味 | 変更理由 |
|---|---|---|---|
| `squid_n_element::wall::misc_wall::MiscWall`（`pub(crate)`） | `InFrameMiscWallGeometry` | フレーム内雑壁（腰壁・垂壁・袖壁）1 枚分の幾何情報。既存の壁エレメントが耐震壁の成立条件を満たさなかったとき、周辺の柱・梁の断面性能へ合成する処理の入力 | 下記と同名で衝突していたため改名（`squid-n-element` 内 6 箇所） |
| `squid_n_core::model::MiscWall`（公開型） | `OutOfFrameMiscWall` | フレーム外雑壁。壁を要素としてモデル化せず、始点・終点の絶対座標に高さ・面重量を持つだけの重量情報。0.5m 分割で近傍節点へ集計 | 上記と同名で衝突していたため改名（10 ファイル・28 箇所。`squid-n-edit` の `AddMiscWall`/`SetMiscWall`/`DeleteMiscWall` コマンド名と `MemberCategory::MiscWall`（数量積算の分類）は、他の何とも衝突しないため対象外とした） |

**本書 §1〜§3.2 は決定当時の呼称としてそのまま残す**（`RegionBoundary`/`FloorRegion`/`Slab`
への改名を記録した §3.1 の「パネル」注記と同じ扱い）。以下、本書内で `MiscWall` とだけ
書かれている箇所は、文脈からどちらの型かが読み取れる（節点・部材参照を持たず絶対座標の
みという記述は `OutOfFrameMiscWall`、袖壁・腰壁・断面性能への合成という記述は
`InFrameMiscWallGeometry` を指す）。新しく書く節では新称を使うこと。

`dev_docs/specs/用語集.md` の該当項目・関連する他の申し送り（`剛域長の算定を技術基準へ揃える_申し送り.md`
§3.2 等）は新称へ更新済み。

---

## 4. 決定後のデータモデル（骨格）

```
Model
 ├ floor_regions: Vec<FloorRegion>   // 旧 Model.slabs を置き換え
 └ wall_regions:  Vec<WallRegion>    // MiscWall・WallAttr を吸収

FloorRegion
 ├ name:     String                  // D11
 ├ boundary: Vec<NodeId>             // 大梁の閉路（D1・D8）
 ├ plate:    Option<SlabPlate>       // 版。None は版なし床領域（D2）
 └ joists:   Vec<SecondaryMember>    // 領域が実体を持つ（D6）

WallRegion
 ├ name:     String
 ├ boundary: Vec<NodeId>             // 柱・梁の閉路（D1・D8）
 ├ plate:    Option<WallPlate>       // 版（耐震壁・雑壁・仕上げを同じ型で表す。D3）
 └ posts:    Vec<SecondaryMember>
```

```
FloorRegion / WallRegion の幾何（D13〜D16・D22）
 └ shape:
     ├ Enclosed { boundary: Vec<NodeId> }              // 閉路から自動生成
     └ Attached {
           anchor: Line { nodes: [NodeId; 2], span: [f64; 2] }  // 区間は無次元 0..1
                 | Point(NodeId)
                 | Floor(FloorRegionId),
           extent: [f64; 2],                            // 両端の張り出し量 d_i, d_j
           transfer: …,                                 // 線のときのみ選択肢を持つ
       }
```

`SlabPlate` は現行 `Slab` から幾何と所属を除いた残り（断面・仕上荷重・室用途・分配方法）。
`SlabKind` と `edge_supported` は持たない（D18・D19）。
`WallPlate` は現行 `WallAttr`（開口・三方スリット）＋ `OutOfFrameMiscWall`（面重量・荷重伝達タイプ・厚さ）＋
「剛性に算入するか」（＝解析要素を生成するか。D4）。

---

## 5. 移行の段取り

**床を端から端まで先に通し、そのあと壁へ移る**（層で切る案・機能で細かく縦に切る案は退けた）。
床側は依存が浅く、パネル検出・所属判定・分配の一式を §2 の実測値を基準に検証できる。
壁側は D4 の「要素は生成物」への転換を含み、断面力表示・保有水平耐力・ST-Bridge 出力の参照先を張り替える大工事になるため、
床で仕組みを固めてから入る。

| Step | 内容 | 備考 |
|---|---|---|
| 0 | **`check.rs` の小梁→スラブ判定へレベル一致を加える（単独の `fix` PR）**。**実施済み** | §2.1。帰属の一意化は Step 4・5 まで残る |
| 1 | `region_gen`（平面グラフの面走査）を下位クレートに実装。**実装済み**（`squid-n-core/src/region_gen.rs`。床のみ。壁は構面の切り出し規則が未決）。交差梁の検出（`PanelScan::crossings`）を含む | D8 |
| 2a | `FloorRegion` 型の導入と `Model.slabs` の置き換え。囲まれた領域・取り付き領域の 2 種別と取付き先。**実装済み**（下記 §5.1） | D2・D3・D13〜D16・D18・D19・D22 |
| 2b | 領域名の UI・取り付き領域の編集コマンド・階複製の追随。**実装済み**（下記 §5.2） | D11 |
| 3 の後 | 二次部材の実体を領域へ移す（`Model.secondary_members` の廃止）。**Step 3 の後でなければ成立しない**（§5.2 参照） | D6 |
| 3 | 取り込み・既存モデルからの領域生成と、版・小梁の入れ直し。パネルに収まらない版の片持ち判定を含む。**実装済み**（下記 §5.3） | D7・D10・D12・D20・D21。D23 は壁の取り込み変換（Step 7 以降） |
| 4 | 荷重分配を領域単位へ作り替え（小梁で再分割 → 三角形・台形分配）。**実装済み**（`squid-n-load::floor::distribute_region`。§3.1 の設計修正〔`Slab`/`FloorRegion` の再分離〕を含む） | D9（§3.1 のとおり修正）。フィクスチャの代表スカラが変わった。V&V 記録済み（`dev_docs/v_and_v/床領域の再設計_荷重分配とSlabFloorRegion分離_2026-08.md`） |
| 5 | 小梁設計を分配計算の結果から出す（`check.rs` の幾何再導出を削除） | **一部対応**（下記 §5.5）。境界に載る小梁の負担幅は床板境界の幾何から求めるよう直したが、荷重の基準（`w × spacing`）は変えていない（`distribute_region` の固定端前提の出力へ乗り換えると `docs/calc_basis` の計算根拠が変わるため見送った） |
| 6 | 床領域・床板の UI（ナビゲータ・表）・編集コマンド。**表・編集コマンドは Step 4 と同時に実装済み**（`tables/slabs.rs`・`squid-n-edit` の `AddSlab`/`AddAttachedSlab`/`SetSlabSection`/`SetFloorRegionJoists` 等）。MCP は床板・床領域を編集する専用ツールが未整備（Step 4 時点で影響なし） | PR #218 の `SetSlabSecondaryJoistIds` 等は作り直し済み |
| 7+8 | `WallRegion` の作り替え（`OutOfFrameMiscWall`・`WallAttr` の吸収）と、壁の解析要素を準備計算で生成する経路への転換を一括実施（**E7 により分離しない**） | D3・D4・D5・E1〜E5。参照先の張り替えを含む。着手前に E8（壁入りフィクスチャ・スナップショット）が要る |
| 9 | 壁領域の UI・編集コマンド・MCP、ST-Bridge 入出力の追随 | D12 |

**Step 0 で直るのは「別階のスラブを掴む」ところまでである。** 同一レベル内では
2〜3 枚のスラブが該当したまま残り、並び順の先頭を採る状態が続く（§2.1）。
辺上を除いた厳密内包へ替える案は採れない。小梁は必ずスラブの辺上に載るため、全滅する。
帰属を一意にするには、スラブではなくパネル（大梁で囲まれた領域）を単位にするほかない。

### 5.1 Step 2a で実際に入れたもの（2026-08-23）

`Model.slabs: Vec<Slab>` を `Model.floor_regions: Vec<FloorRegion>` へ置き換え、`SlabId` を
`FloorRegionId` へ改名した。`FloorRegion` は `shape`（`Enclosed` / `Attached`）と
`plate: Option<SlabPlate>` を持ち、版なし床領域を表せる。幾何は
`boundary_coords`（両方の形で使える座標列）・`boundary_nodes`（囲まれた領域のみ）・
`edge_nodes`・`level`・`reference_node` から取る。

**この移行にともなって、次の 3 点は当初の決定から変えている。**

1. **`distribute_polygon_supported` は残さず削除した。** D18 で `edge_supported` を廃止した結果、
   この関数に呼び手がなくなった。呼び手のない実装を残すと腐るため、Step 4（領域が持つ二次部材から
   支持辺を導く分配）で呼び手とともに書き直す。`polygon_edge_areas` の「候補辺の部分集合」は残した。
2. **取付き線の部分区間は `validate` が弾く。** データとしては `span: [t_i, t_j]`（D22）を持てるが、
   その区間だけへ荷重を載せる経路は Step 4 まで無い。黙って全長へ載せると荷重の位置が実際とずれるため、
   `span != [0, 1]` はエラーとする。**⚠ この制限は §5.6 で解消した。**
3. **ST-Bridge 出力は「囲まれた領域かつ版あり」だけを書き出す。** `StbSlab` は境界節点の並びで版を
   表すため、取り付き領域（自由端に節点がない）と版なし床領域は表現できない（D12 の方針による）。

あわせて、UI の「スラブ種別」ピッカー（一般/片持ち/出隅）と編集コマンド `SetSlabKind` を削除した。
種別は領域の形そのものが表すため（D19）、表では読み取り専用の「囲まれ／取り付き」表示に置き換えた。
取り付き領域を UI から作る導線は Step 2b で用意する。

### 5.2 Step 2b でやったこと・D6 を Step 3 の後へ回した理由（2026-08-23）

**D6（二次部材の実体を領域が持つ）は、Step 3 のパネル統合より後でなければ成立しない。**
D7 の所属規則は「中点がその領域に厳密内包される」だが、現在の小片スラブ領域では
小梁は必ず辺の上に載るため、内包されるものが 1 本もない。実測は次のとおり。

| 判定対象 | 小梁 56 本の該当数の分布 |
|---|---|
| 現在の小片スラブ領域（辺上を含む） | 2 枚: 46 本、3 枚: 10 本（一意にならない） |
| `region_gen` のパネル（厳密内包） | **1 つ: 56 本（すべて一意）** |

横断リストを先に廃止すると、取り込んだ小梁 56 本の置き場所がなくなる。
そのため Step 2b では D6 を扱わず、D11（領域名）と取り付き領域の入力経路を入れた。

- `SetFloorRegionName` / `AddAttachedFloorRegion` を `squid-n-edit` に追加
  （取付き先が実在しない節点・部分区間は Noop）
- 床タブに「名前」列と「取り付き領域を追加」フォームを追加（取付き先＝大梁の 2 節点
  または柱の 1 節点、張り出し量、荷重の出口）
- 階複製が**領域名を引き継ぐ**ようにした
- `LoadTransfer` を `RegionAnchor::Line` の中へ移した（B1）。点に取り付く領域は
  その節点への集中しかありえないため、値を持たせると無意味な組み合わせを表現できてしまう
- 階複製の `slab_story` が取り付き領域で `None` を返し、**複製の対象からも見送り件数からも
  外れていた**のを修正した（取付き先の節点で階を判定する）。取り付き領域を複製しないこと自体は
  変えていないが、見送った件数として利用者に見えるようになった

Step 0 を分ける理由は三つある。移行は長期になる見込みで、その間ずっと誤った小梁設計が出続けること。
修正は判定条件の差し替えだけで小さく、単独で検証できること。
そして移行の PR で一緒に直すと、分配の作り替えによる結果変化と不具合修正による結果変化が同じ差分に混ざり、
V&V でどちらの影響か切り分けられなくなることである。

**Step 0 自体が 49 本の小梁を別のスラブへ移すため、小梁の検定比と `snapshot_key_scalars` が変わった。**
実測は `design.joist_checks` 28 → 56 件、小梁の最大検定比 0.2049 → 0.3006。
他のスナップショット値（応力・変位・固有周期・保有水平耐力・時刻歴・部材検定比）は不変である。
記録は [`v_and_v/小梁設計のスラブ帰属_レベル一致_2026-08.md`](../v_and_v/小梁設計のスラブ帰属_レベル一致_2026-08.md)。

### 5.3 Step 3 でやったこと（2026-08-23）

`rebuild_floor_regions`（`squid-n-core`）を新設し、ST-Bridge 取り込みの直後と準備計算の荷重同期の前に同じ関数を呼ぶ。明示ボタンは置かない。

- 大梁閉路（`region_gen`）ごとに囲まれ領域を作り、小片の版を畳む。フィクスチャは 82 枚 → **26 パネル**
- 版仕様と名前は重心・レベルで引き継ぐ。相対面積差が \\( 10^{-3} \\) 未満なら inherited。割れたパネルは重心が入る側だけ継承。混在は版なし＋警告
- どのパネルにも入らず、水平大梁にちょうど 1 辺が全長覆われ自由端が 1〜2 点の版は取り付き線へ変換（`span=[0,1]`、`LoadTransfer::Anchor`、左正の張り出し）。出隅・L 形・宙に浮いた版は Enclosed のまま残し、警告（落とさない）
- 小梁は横断リストのまま、所属だけ D7（中点の厳密内包＋レベル一致）で入れ直す。0 または 2 以上は載せない。フィクスチャでは 56 本すべて一意
- 参照 0 の節点だけ削除（階リスト・通り芯は参照に数えない。削除 ID は `Option` で 0 に化けさせない）
- 中庭は版なしの囲まれ領域として残す。版の重心が外側多角形と内側閉路の両方に入るときは **面積が小さいパネル**へ付ける（レビュー指摘の是正）
- 診断はモデルを書き換えない。`validate` は `span != [0,1]` を弾く（当時。§5.6 で解消）
- D23（地面に立つ塀）と D10 の副次キーはこの波ではやらない

荷重分配は Step 4 まで現行のままのため、畳んだ結果として辺荷重の行き先が変わり、代表スカラが動く。
記録は [`v_and_v/床領域のパネル統合_2026-08.md`](../v_and_v/床領域のパネル統合_2026-08.md)。

### 5.4 Step 4 でやったこと（2026-08-24）

**§3.1 の設計修正（`FloorRegion`/`Slab` の再分離）を先に行い、そのうえで D9 の荷重分配を実装した。**
ワークスペース全体（`squid-n-core`・`squid-n-load`・`squid-n-element`・`squid-n-solver`・
`squid-n-design-jp`・`squid-n-job`・`squid-n-io`・`squid-n-edit`・`squid-n-app`）を一気通貫で
更新した（クレートごとに段階分割すると、型の変更が波及する途中で個々のクレートが
コンパイル不能な状態を跨ぐため）。

- `squid-n-core`: `SlabId` を新設。`FloorRegion` を `{id, name, boundary, secondary_joist_ids,
  slab_ids: Vec<SlabId>, joists}` へ、`Slab` を `{id, shape, plate}`（`plate: SlabPlate` は
  必須。版なしは `plate.section: None` で表す）へ作り直した。`region_gen::Panel`/`PanelScan`
  を `RegionBoundary`/`RegionBoundaryScan` へ改名（パネル呼称の禁止）。`rebuild_floor_regions`
  を書き直し、境界→旧 `FloorRegion` の対応付け（名前・joists の継承）と、`Slab` の所属
  `FloorRegion` への割当（重心・レベル、面積最小タイブレーク）を分離した
- `squid-n-load`: `distribute_region(model, region, w_of)` を新設（§3.1 参照）。
  `LoadTarget::Edge` を `Slab` の文脈で `Span` へ解決する `resolve_edges_to_span` を新設
- `squid-n-job`: `auto_loads::slab_beam_loads_with` を `FloorRegion` 経由（`distribute_region`）と
  未参照 `Slab` の直接分配（`distribute_slab_resolved`）の 2 経路へ分離。二重計上しないよう
  `HashSet<SlabId>` で処理済みを追跡
- `squid-n-app`: `tables/slabs.rs` を「床領域一覧（名前編集のみ）」と「床板一覧（版仕様の編集）」
  の 2 表へ分割。`check.rs` の床設計を「床領域ごとの手入力小梁（代表床板で積載を決める）」と
  「床板ごとの版設計」の 2 ループへ分割し、`JoistCheck`/`SlabCheck` の識別子を `SlabId` へ
  変更（片持ち床板は `FloorRegion` を持たないため、`FloorRegionId` では表現できない）

**実装中に見つけて直した既存バグ 6 件（型の再分離とは独立の不具合）**:

1. `resolve_edges_to_span` が `LoadTarget::Edge → Span` へ変換する際、辺インデックスを
   一時的に入れていた `elem` フィールドを番兵 `ElemId(u32::MAX)` へ戻し忘れており、
   偶然実在する `ElemId` と衝突して無関係な部材へ全荷重が載る場合があった
2. `squid-n-edit::DeleteSlab` が汎用マクロ（削除のたびに ID を単純に繰り上げるだけ）を
   使っており、`FloorRegion.slab_ids` からの参照除去（カスケード）をしていなかった。
   削除後に別の床板の ID と衝突し、「複数の床領域から参照されている」不整合を生んでいた。
   `DeleteSecondaryMember` と同じ手書きのカスケード削除（`FloorRegion.slab_ids` から先に
   除去してから ID を繰り上げ、`InsertSlab` で位置を復元）に書き直した
3. **`squid_n_element::beam::stiffness_factors::slab_cooperating_width`（スラブ協力幅・
   S造合成梁剛性）が、床領域が小梁で複数の `Slab` へ細分されると一切マッチしなくなっていた
   （本 Step 4 の型再分離自体が生んだ回帰）。** 判定が「梁の両端節点をともに含む床板の境界」
   だったため、床領域の外周を走る大梁の両端が別々の `Slab` へ分かれると、どちらの床板の
   境界にも両端が揃わなくなる。判定を `FloorRegion::boundary`（床領域の外周そのもの。小梁の
   分割によらず不変）優先へ直し、板厚は床領域内の `Slab`（`region.slab_ids`）の最大値を使う
   よう修正した。あわせて `build_prep_member_stiffness` の事前判定が建物一律
   `Model::slab_thickness`（既定 0）だけを見て個々の床板の板厚を見ていなかった別バグも
   直した。再発防止テスト:
   `test_beam_new_slab_cooperation_width_survives_joist_subdivided_region`
   （`crates/squid-n-element/src/frame/beam/tests.rs`）
4. `squid_n_load::floor::distribute_region` の手入力小梁ライン経路（`uses_joist_distribution`）が、
   代表床板（`region.slab_ids` の先頭）1 枚だけを見ており、床領域が床板を 2 枚以上持つと
   代表以外の面積・荷重を無視していた（総和保存が崩れる）。フィクスチャは import 後に
   手入力小梁ラインが残らないため顕在化しないが、複数床板と手入力小梁ラインが両方ある
   床領域（Step 4 が想定する典型例）で静かに荷重を失う経路だった。`region.slab_ids.len() != 1`
   のときはこの経路を拒否して通常経路（各床板を独立に分配）へ落とすよう修正した。
   再発防止テスト: `test_uses_joist_distribution_false_for_multiple_slabs`
   （`crates/squid-n-load/src/floor/tests.rs`）
5. `squid_n_design_jp::beam_has_attached_slab`（RC 梁の中央 T 形断面略算・数量積算の
   スラブ厚控除のスラブ取付き判定）が、3 と同じ理由で床領域が細分されると判定できなく
   なっていた（型の再分離作業でこの関数を `model.floor_regions.iter()` から
   `model.slabs.iter()` へ機械的に移し替えた際、床板ではなく床領域の境界で判定すべき
   だった点を見落としていた。コミット後のレビューで発見）。3 と同じ「床領域の外周を
   優先し、床領域内のどれか 1 枚でも版があればよい」判定へ直した。再発防止テスト:
   `test_beam_has_attached_slab_survives_joist_subdivided_region`
   （`crates/squid-n-design-jp/tests/attached_slab.rs`）
6. `region_rebuild::node_has_structural_ref`（D21 の節点削除判定）が `Story::node_ids`・
   `AxisGroup` への所属を「参照あり」として扱っており、実運用のモデルでは常に削除が
   無効化されていた（ST-Bridge 取り込みは `rebuild_floor_regions` を呼ぶ前に全節点の
   階所属を確定させ `Story::node_ids` へ登録するため、削除候補の節点はほぼ必ずどこかの
   階の節点一覧に載っている）。本文書・`docs/calc_basis/01_荷重/03_床荷重の分配.md`
   が明記する「階の節点一覧と通り芯は参照に数えない」という仕様どおりに、両者を
   参照判定から外した。あわせて `Node::support_spring` を参照判定に加えた（ばね支点
   だけを持つ節点が誤って削除候補になりうる漏れだったため）。コミット後のレビューで
   発見。再発防止テスト: `test_story_node_list_membership_does_not_block_deletion`
   （`crates/squid-n-core/src/region_rebuild.rs`）

**フィクスチャの代表スカラが変化した。** 小梁が `Slab` 単位の分配へ実際に参加するようになり
（Step 3 まではパネル統合により小梁が分配上は無視されていた）、床板検定件数（25→81。
床領域内の複数床板が個別に検定されるため）が変わった。バグ 3 件目（剛性の回帰）を直した後は
固有周期・層間変位・保有水平耐力・時刻歴応答は Step 3 とほぼ一致している。詳細な V&V
レポートは
[`v_and_v/床領域の再設計_荷重分配とSlabFloorRegion分離_2026-08.md`](../v_and_v/床領域の再設計_荷重分配とSlabFloorRegion分離_2026-08.md)。

### 5.5 Step 5 でやったこと・一部にとどめた理由（2026-08-25）

Step 表の「`distribute_region` の出力から導く」を文字どおり実行しようとしたところ、
`distribute_region` の出力（`LoadShape`＋CMQ）は固定端梁前提であり、小梁検定
（`design_joist_simple`/`design_joist_from_forces`）が前提とする単純梁の負担幅モデル
（`w × spacing`）とは基準が異なることが分かった（レビュー指摘）。乗り換えると
`docs/calc_basis/01_荷重/03_床荷重の分配.md` §1.4.2 の計算根拠（負担幅 spacing による
等分布荷重）を書き換えることになり、`FloorRegion.joists` 経路（グリレージ・単純小梁とも
`w × spacing` のまま）とも基準が食い違う。

そのため、**荷重の基準は変えず、`spacing` の求め方だけを直した**。
`squid_n_load::secondary::joist_edge_tributary_width` を新設し、小梁の辺に沿う境界辺を
持つ床板（同じレベルに限る）の幅の合計を半分にして負担幅とするようにした。これにより、
複数の床板の辺に載る小梁の負担幅を「載っている床板の幅の単純平均」としていた既存実装
（T 字取り付き等、3 枚以上に載る場合は近似だった）が、片側が複数枚に割れているときも
正しく扱えるようになった。床板の境界に載らない小梁（内部を通る等）は既存の平行小梁
グループ化へフォールバックする。

**実装中に危険側の不具合を 3 件見つけて直した**（詳細は V&Vレポート）。1 件目は、隣の
床板が T 字取り付きの途中の節点で境界を分割されている場合、節点対の完全一致では反対側を
取りこぼし負担幅が半分程度になる欠陥（コミット前レビューで発見）。2 件目は、幾何判定へ
直した直後に見つかった、レベル一致の絞り込み漏れ（上下階で平面が繰り返される建物では
別階の床板を拾ってしまう。コミット前レビューで発見）。3 件目は、PR 提出後の実装への
敵対的レビューで見つかった、退化した（ほぼ長さ 0 の）辺が遠く離れた無関係な床板を誤って
拾いうる欠陥（実フィクスチャでは再現しないが回帰テストで固定）。すべてを直した最終状態で、
フィクスチャの
`design.joist_max_ratio` は 3.097e-1→**4.645e-1** に動いた（それ以外の代表スカラは
不変）。上がる方向が正しい理由（T 字取り付きの負担幅の過小評価を是正したため）は
V&V レポートに記録した。詳細は
[`v_and_v/小梁検定の負担幅を床板境界から求める_2026-08.md`](../v_and_v/小梁検定の負担幅を床板境界から求める_2026-08.md)。

**Step 5 は一部対応にとどまる。** 荷重の基準を分配計算の固定端前提へ揃える
（本来の Step 5 の狙い）には、`docs/calc_basis` の計算根拠の書き換えと、
`FloorRegion.joists` 経路も含めた基準の統一が要る。今回は着手しなかった（残課題）。

### 5.6 取付き線の部分区間対応でやったこと（2026-08-25）

§9 残課題「Step 4 で取付き線の部分区間（`span != [0, 1]`）に対応する」に対応した。
D22 が決めていた「区間の両端が梁のスパン中間に来ても、幾何解決で中間集中荷重・
部分分布荷重として渡せる」を、§8.3 で片持ちスラブの辺荷重向けに実装済みだった
座標ベースの幾何割付（`beam_span_position`／`emit_along_segment`）へそのまま乗せる形で実装した。

- `squid_n_load::floor::LoadTarget::Span([NodeId; 2])` を
  `Span { nodes: [NodeId; 2], t: [f64; 2] }`（既定 `[0.0, 1.0]`）へ拡張した。既存の呼び出し箇所
  （小梁反力・矩形床境界の残り分など）はすべて `t = [0.0, 1.0]` を渡すだけで動作は変わらない
- `resolve_edges_to_span`（`squid-n-load`）が、取り付く床板（`SlabShape::Attached`）の辺 0
  （取付き線）を `Span` へ解決する際、取付き先の `span`（`RegionAnchor::Line::span`）を
  そのまま `t` へ引き継ぐ。囲まれた床板の境界辺は常に `t = [0.0, 1.0]`
- `squid-n-job::auto_loads::slab_load_case_content` の `Span` 解決を、`t == [0.0, 1.0]`
  のときだけ従来の高速経路（`bl.elem` 直接ヒット、`a0 = 0`・`len = 全長`）を使い、
  `t != [0.0, 1.0]`（部分区間）は `nodes` の座標を `t` で線形補間した区間端点
  （実節点ではない）を使って幾何解決へ回す形に分けた。高速経路は要素の局所 x 軸の
  向きを検証せずに使っており（対称形状の `Uniform` は影響しないが一般には潜在リスク）、
  部分区間をそのまま高速経路へ乗せず、既に向きに依らず解決できる幾何経路へ回すことで
  この潜在リスクに触れずに済ませた。件数が少ない（部分区間を使うのは取り付き領域のみ）ため、
  既存の大多数（小梁反力等）が使う高速経路の性能には影響しない
- `Model::validate`（`squid-n-core`）と `squid-n-edit` の `AddAttachedSlab`／`SetAttachedAnchor`
  の「`span != [0, 1]` は弾く」制限を、「`span` が有限かつ `0.0 <= t_i < t_j <= 1.0`」という
  妥当性チェックへ置き換えた
- 床タブの取り付く床板フォーム・一覧表（`squid-n-app::tables::slabs`）に区間 `[t_i, t_j]`
  の入力を追加した。これを追加しない限り、GUI からは `span != [0, 1]` を作る手段がなく
  （ST-Bridge 取り込みも `span = [0, 1]` 固定、MCP にも専用ツールが未整備）、
  データモデルとしては通っても実際には作れない機能になってしまうため

**実装中に見つけて直した既存の潜在的な誤りが 1 件ある**（コミット前レビューで発見）。
`LoadTransfer::Columns`（両端の柱へ集中）は `distribute_attached` が `span` を無視して
常に半分ずつ（0.5 / 0.5）を両端の柱へ振っていた。`span = [0, 1]` の間は区間の中点が
ちょうど中央（`t_mid = 0.5`）に一致するため誤りが表面化しなかったが、部分区間を
弾く制限を外すとこの組み合わせが到達可能になり、位置がずれたまま黙って通ってしまう
状態だった。単純梁の集中荷重反力公式（\\( R_i = W(1-t_{\text{mid}}) \\)、
\\( R_j = W \cdot t_{\text{mid}} \\)、\\( t_{\text{mid}} = (t_i+t_j)/2 \\)）で按分するよう直した
（`squid_n_load::floor::distribute_attached`）。`docs/calc_basis/01_荷重/03_床荷重の分配.md`
に「両端の柱へ集中」の按分規則を追記した。

再発防止・回帰テストを追加した（`crates/squid-n-job/src/auto_loads.rs`）。取付き線の下の
大梁が中間節点で分割されている場合に部分区間が分割点をまたいでも両方の梁へ正しく分割して
載ること（`test_attached_anchor_partial_span_crosses_beam_split`）、区間の始端・終端が
取付き線の実節点にちょうど一致し `beam_span_position` の節点近傍除外を通って幾何割付側へ
落ちても正しく載ること（`test_attached_anchor_partial_span_touches_start_node`／
`test_attached_anchor_partial_span_touches_end_node`）、`LoadTransfer::Columns` が区間の
中点で按分すること（`test_attached_anchor_columns_transfer_partial_span_splits_by_lever_arm`）
を含む。既存フィクスチャは `span != [0, 1]` を使っていないため、代表スカラの変化はない。

**敵対的レビューで見つけて直した点**（コミット後、コードレビュースキルを使わない手動レビュー）:

1. `distribute_to_node`（`cantilever.rs`）・`distribute_slab`／`distribute_attached`
   （`floor/mod.rs`）の rustdoc が、`LoadTransfer::Columns` の按分を「常に半分ずつ」と
   古い記述のまま残っていた。実装（`t_mid` による按分）は直したが、それを説明する
   doc コメントを直し忘れていた。区間中点で按分する旨へ更新した
2. `LoadTransfer::Columns` の `t_mid` 按分は、張り出し量が区間の両端で等しい
   （`extent[0] == extent[1]`）ときに限り厳密である。張り出しが異なる台形の
   footprint（D15）では真の面積重心が区間中点からずれるが、その差は見ていない。
   これは今回の変更が持ち込んだものではなく、`span=[0,1]` 固定だった旧実装の
   半分ずつ按分も同じ近似を持っていた。`distribute_attached` の doc コメントに明記した
3. GUI（`squid-n-app::tables::slabs`）の区間入力の妥当性チェックが、有限性と
   始端 < 終端しか見ておらず、`Model::validate` と同じ範囲（`0.0 <= t_i, t_j <= 1.0`）を
   確認していなかった。`egui::DragValue::range(0.0..=1.0)` の入力側クランプに実質的に
   依存していたため、クランプが効かない経路があると「追加」ボタンが有効に見えたまま
   クリックしても `Noop` で何も起きない、という利用者に見えない失敗になりうる状態
   だった。GUI 側にも同じ範囲チェックを足した
4. 区間の始端が実節点に一致するケース（`t = [0.0, 0.5]`）はテストしていたが、対称の
   終端一致ケース（`t = [0.5, 1.0]`）は未検証だった。テストを追加した
   （`test_attached_anchor_partial_span_touches_end_node`）

### 5.7 壁側 `region_gen`（構面のグルーピングと面走査）の実装（2026-08-26、未結線）

Step 7+8 の最初の一手として、E1〜E3（構面のグルーピング・面走査・面積算定）を
`squid-n-core` に実装した。**`WallRegion`/`WallPlate` 型の作り替え・要素生成（D4）・
モデルとの結線（診断接続を含む）はまだ行っていない。** 本節の実装は、既存モデル・
既存の解析結果を一切変えない（`crates/squid-n-app/tests/wall_model.rs::snapshot_wall_bay_scalars`・
`crates/squid-n-app/tests/full_model.rs::snapshot_key_scalars` とも無変化）。

**モジュール構成**: `crates/squid-n-core/src/region_gen.rs`（1 ファイル）を
`region_gen/{mod,floor,wall}.rs` へ分割した。半辺（有向辺）をたどる面走査エンジン
（`scan_faces`）と、座標系に依存しない 2D 幾何ヘルパー（`signed_area`・
`point_segment_dist`・`polygon_contains_strict`）を `mod.rs` に共有として切り出し、
既存の床専用ロジック（`horizontal_beams_by_level`・`crossing_pairs`・`RegionBoundary` 等）を
`floor.rs` へそのまま移した。外部から見える公開 API（`region_gen::{generate_region_boundaries,
scan_region_boundaries, RegionBoundary, RegionBoundaryScan, crossing_beams,
polygon_contains_strict, BOUNDARY_TOL_MM}`）は変えていない。**回帰の安全網は「分割前の
`region_gen.rs` の全テストが、分割後も変更なしに通ること」とした**（実際に無変更で通過）。

**壁側 `region_gen::wall` の実装内容**（E1〜E3）:

- **構面の検出（E1）**: 全柱（`ElementKind::Beam` のうち `is_vertical_pair` を満たすもの）の
  柱脚位置（XY）を `MEMBER_AXIS_TOL_MM`（10mm）以内で重複排除し、全ペアから直線候補を作る。
  既知の直線と「同一の構面」とみなす判定は、**角度の許容差ではなく、柱位置から直線までの
  実距離 [mm] で行う**（角度の許容差は直線が長いほど遠方でのずれが拡大するため使わない。
  梁の交差診断を同じ理由で `MEMBER_AXIS_TOL_MM` へ統一した経緯（§8.3）と同じ判断）。
  直線の方向は見出し角 `[0, π)` に正規化する（`atan2` の mod 演算による 0°/180° 境界の
  不連続を避けるため、ベクトルの符号反転で正規化する）。
- **面走査（E1・E2）**: 構面ごとに、両端の XY 射影が `MEMBER_AXIS_TOL_MM` 以内にある
  `ElementKind::Beam`（柱・梁の区別、鉛直・水平・斜めの区別は問わない）を辺として集め、
  局所座標 `(s, z)`（s=構面方向の位置、z=グローバル Z）へ射影して床と共通の面走査
  エンジンへ渡す。1 本の構面は建物の全高さを 1 つの平面グラフとして扱うため、
  複数階にまたがる壁面は階ごとの内部面として現れる。外周面の判別（符号付き面積が
  正か負か）は床（グローバル XY・+Z 上が固定の基準）と同じ規則をそのまま使えるかが
  設計時点で未確定だったため、直交（見出し角 0°・90°）と斜め（30°）の 3 通りで
  面数・面積を検証するテストを追加し、いずれも正しく検出できることを確認した。
- **面積算定（E3）**: `WallRegionBoundary::area` はニューエルの公式による 3 次元面積で、
  理想平面への投影を経由しない。トポロジー（面走査）は直線への射影という近似で
  行ってよいが、面積・荷重は実座標から直接求める、という使い分けを型で強制する
  （投影面積を返す公開メソッドは用意していない）。境界節点が理想平面から数 mm
  ずれていても面走査の結果（面の数・境界）は変わらないが、ニューエル面積は
  投影面積から測定可能な差として動くことをテストで確認した。

**計算量について（実測値。当初の見積もりは誤りだった）**: 柱脚位置の全ペアから
直線候補を作るため候補数は柱の本数 N に対し O(N²) だが、**候補ペアごとに「既知の
全直線と実距離で比較する」素朴な実装は O(N²) ではなく O(N²·L)（L=見つかっている
直線の本数）である。** 格子状の柱配置では、軸方向の直線に加えて格子点を結ぶ斜め方向の
直線が候補として大量に残るため、L 自体が N の増加とともに増える。実測
（`crates/squid-n-core/tests/perf_probe.rs`、release ビルド）は次のとおりで、
柱 400 本（20×20 格子）で 840ms、柱 900 本（30×30 格子）で 19.4 秒に達した。
「実建物の柱本数（数百〜数千本程度）では実用上問題にならない」という当初の記述
（実測なしの見積もり）は誤りだった。

| 柱本数 | 素朴な実装 | グリッド索引後 |
|---|---|---|
| 100（10×10） | 8.0ms | 6.0ms |
| 225（15×15） | 101.4ms | 39.5ms |
| 400（20×20） | 840.9ms | 189.5ms |
| 900（30×30） | 19,361.6ms | 1,813.4ms |

対策として、候補直線の重複判定にグリッド索引（`LineIndex`）を導入した。キーは
「直線の見出し角を 2 倍した角度（`2θ`）の単位円上の位置」と「柱脚位置の重心からの
符号なし距離」を `MEMBER_AXIS_TOL_MM` 相当の刻みで量子化したものとし、近傍のバケットだけを
候補として拾ってから実距離で確認する（索引はあくまで性能上の絞り込みで、**同一直線の
最終判定は必ず実距離（`is_same_line`）で行う**ため、索引の取りこぼし・衝突は
正しさを損なわない）。`2θ` を使うのは、直線の向きが持つ ±180° の符号の曖昧さを
素朴な角度（mod π）でバケット化すると 0°/180° の境界で不連続になる
（179.99° と 0.01° はほぼ同じ直線だが、実数直線上のバケットでは両端に離れて割り当たる）
ためで、角度を 2 倍すると `cos 2(θ+π) = cos 2θ` によりこの不連続が生じない。

上の表のとおり、グリッド索引で 30×30 格子は 19.4 秒 → 1.8 秒まで縮んだが、**それでも
超線形の増加が残っている**（400→900 本の柱本数比 2.25 倍に対し、所要時間は約 9.6 倍）。
残りのボトルネックは、直線ごとに全要素を線形走査する `members_on_plane`（O(L·M)、
M=要素数）と見られるが、詳細な切り分けは未実施（残課題）。梁の交差診断
（`region_gen::floor::crossing_beams`）も当初は総当たりで実装し、実測して初めて
走査線法へ最適化した経緯があり（§8.3）、本モジュールも同じ方針
（先に正しさを固め、実測してから最適化する）を踏襲しているが、**今回は 1 段階の
最適化で止めている。実建物（数百〜数千本の柱）でどの程度の時間がかかるかは、
グリッド索引後の値でも未検証（数百本規模の実測データがない）。Step 7+8 の結線時に
`members_on_plane` のボトルネックを切り分け、必要なら追加の最適化を行うこと。**
性能の悪化と下記 4 の退化閉路は、根が同じである（他の部材と接続しない直線候補が
大量に生き残ること）。そのため `wall_planes` の段階で「乗る部材の射影後 `s` の値が
2 種類未満（＝閉じた面を作りえない）の直線候補を捨てる」事前フィルタを入れれば、
L を大きく減らしつつ退化した面をそもそも発生させない、より根本的な対策になる。
面走査の後で面積しきい値によって除外するより望ましい対策であり、
`members_on_plane` のボトルネック切り分けと合わせて検討すること。

**未結線であることの明記**: `region_gen::wall` は幾何の検出のみを提供し、`WallRegion`/
`WallPlate` への統合、モデルとの結線（要素生成・交差診断の接続）はまだ行っていない。
これは `dev_docs/handoff/README.md` が記録する P7・P8 の「非デフォルト機能が実装済みと
申告されながら実際には壊れていた」再発パターンと同じ形（未結線のコードが放置される）に
なりうるため、ここで明記する。**次の一手は E4〜E7 に沿った型実装
（`WallRegion`/`WallPlate`/`RegionAnchor` for wall・要素生成）であり、`region_gen::wall` を
入力としてそれらを組み立てる。**

**E8 の壁入りフィクスチャは、境界検出（本節の実装）を一切通らない。** §3.2 E8 で
新設した `crates/squid-n-app/tests/wall_model.rs::wall_bay_model` は、柱脚 4 節点を
固定支点とし、梁は「頂部で閉路をなす 4 本」のみで、**柱脚どうしを結ぶ梁を持たない**
（モジュール doc の記述どおり）。そのため各鉛直構面は「柱 2 本＋頂部の梁 1 本」という
開いた U 字にしかならず、`scan_wall_region_boundaries` は境界を 1 つも検出しない
（実際に確認済み）。E8 は解析結果（自重・剛性・応力）のピン止めには有効だが、
**壁領域の境界検出そのものには実データのカバレッジを一切提供しない。**
この穴は、実建物データ（`crates/squid-n-app/tests/fixtures/model.stb`）に対して
`scan_wall_region_boundaries` を実行するテスト
（`crates/squid-n-app/tests/full_model.rs::region_gen_finds_wall_bounded_regions`）を
新設して埋めた。同フィクスチャは `ElementKind::Wall` を 0 件しか持たないが、
`region_gen::wall` は壁要素の有無を見ず柱・梁の幾何だけで構面を検出するため、
検出そのものは成立する（境界 55 件、合計面積 1,427,640,000mm²、`unclosed == 0` を固定）。
`wall_model.rs` 側の U 字構造を直す（柱脚どうしの梁を追加する）かどうかは、
Step 7+8 の型実装・結線時に判断すること（本節では変更していない。既存の
ピン止め済みスナップショットへの影響を検証と切り離すため）。

**2026-08-26 追記（E4〜E7 着手前に修正済み）**: `WallRegion`/`WallPlate` の型実装
（E4〜E7）は、`WallRegion` を境界検出（`region_gen::wall`）の出力から組み立てる
ため、本フィクスチャでの境界検出が前提になる。そこで型実装に先立ち、柱脚どうしの
梁（基礎大梁想定）4 本を追加し、4 つの鉛直構面すべてを閉じた矩形にした
（`test_region_gen_wall_finds_all_four_faces` で固定。4 面・面積 9,000,000mm²×2・
12,000,000mm²×2）。

副作用として、壁エレメントの四周判定（`wall_is_framed`。上下大梁の両方が実在するかで
決まる）が偽から真に変わり、`wall_frame_category_issue`（耐震壁と周辺架構〔上下大梁・
左右側柱〕の構造種別を一致させる検定）が新たに働くようになった。これにより、
本フィクスチャがもともと抱えていた不正な混合構造（RC 耐震壁に S 造の側柱・大梁）が
表面化した。柱 0・1（側柱）と大梁 4・8（壁の上下大梁）の材料を RC へ差し替えて是正した
（断面性能は鋼断面の計算値をそのまま流用し材料だけ差し替えている。現実的な RC 断面
形状で改めて算定すると鋼より大幅に剛性が高くなり、壁単体の剛性寄与を示す
`test_wall_element_changes_eigen_period` の判定が意味をなさなくなるほど埋もれたため）。
この材料差し替え（ヤング係数・密度の変化）と基礎大梁の自重により、
`snapshot_wall_bay_scalars` の代表スカラは実測して更新した（総重量・固有周期・
壁材端力・柱頭変位のいずれも変化。値の変化そのものが目的ではなく、型変更前の
現行動作を新たにピン止めし直したという位置づけ）。
`region_gen_finds_wall_bounded_regions` は、55 件・合計面積という単一の観測値の
ピン止めに留まらない。独立の不変条件として、次の 3 つも確認している。境界どうしが
節点集合として重複していないこと（構面の重複統合ミスの検出）、各境界の面積が必ず
正であること（外周面判別の破綻の検出）、そして本フィクスチャが直交グリッドの S 造
建物であるため正の面積を持つ境界の構面がすべて軸方向（X または Y）であること
（`wall_planes` が実際にはつながっていない柱の組を誤って直線候補として拾い、
構造的に無意味な面を検出していないことの確認）である。床側の Euler の公式検算
ほど厳密な独立検算ではないが、最低限の食い違い検出は持たせた。実測では 55 件
すべてが軸方向で、斜め構面に正の面積を持つ境界は 0 件だった。**この面積正値
チェックが、下記 4 の実バグを実際に検出した。**

**敵対的レビューで見つけて直した点、および未対応のまま申し送る点**（2026-08-26）:

1. **計算量の見積もり誤り**: 上記のグリッド索引導入と実測値の記録（既に本節に反映済み）。
2. `column_footprints` の重複排除（[`crate::region_gen::wall`] 内部）は `model.elements`
   の並び順に依存する（先勝ち）。生き残った代表点が候補直線の原点
   （`WallRegionBoundary::plane_origin`）になるため、構面自体・面走査の結果は
   変わらないが、原点の値は並び順の影響を受ける。D5 が「生成は決定的」と求める
   `Model.floor_regions`/将来の `WallRegion` の生成物とは異なり、`plane_origin` は
   `WallRegionBoundary` の内部表現にすぎず利用者に見えないため実害はないが、
   将来 `plane_origin` を何かの ID・キーに使う場合は要注意（コードコメントに記録済み）。
3. **未対応**: `MIN_PROJECTED_EDGE_LEN_MM`（構面上にはあるが射影後の長さがほぼ 0 の
   部材を除外するフィルタ）が実際に効く経路（既存の辺と近い方位角に紛れ込んで
   並べ替えを乱すケース）は、まだテストで再現できていない。角度の近接を意図的に
   作るテストは部材端点の座標を高精度で作り込む必要があり複雑になるため見送った
   （コードコメントに記録済み）。
4. **実データで見つけた実バグ（面積正値チェックで検出）**: `scan_wall_region_boundaries`
   の外周面判別が「射影後の符号付き面積 `> 0.0`」という厳密な不等式だったため、
   浮動小数点の丸め誤差を「正の面積」として誤って拾っていた。建物の柱は複数階に
   わたって節点が分割されるため、同一の柱に属する節点は XY が完全に一致する
   （Z のみ異なる）。この構面上で他のどの部材とも接続しない孤立した柱（行き止まり）
   を面走査がたどると、射影後の座標 `(s, z)` は全節点で `s` が完全に一致し、理論上の
   符号付き面積は厳密に 0 になるはずだが、シューレース公式の丸め誤差が
   `1e-8`〜`1e-7`mm² 程度残り、`> 0.0` の判定を通過して「正の面積を持つ境界」として
   検出されてしまっていた（実測: 実建物データで 12 件、境界数が 67→55 に減った）。
   `MIN_FACE_AREA_MM2`（`MEMBER_AXIS_TOL_MM²` = 100mm²。観測された丸め誤差より
   9 桁以上大きく、実在の壁面積より 4 桁以上小さい）をしきい値とすることで修正した。
   実建物データの座標（柱脚 (1000,1000) と孤立した柱 (13800,7400) の組。方向ベクトル
   `[0.8944271909999159, 0.4472135954999579]`）をそのまま再現した単体テスト
   `region_gen::wall::tests::test_isolated_dangling_column_does_not_create_spurious_boundary`
   を追加した（修正を戻すと実際に失敗することを確認済み。見出し角が軸方向〔0°・90°〕だと
   `s` の計算が単純な代入になり丸め誤差が出ずこのテストでは再現できないため、
   軸方向のテストケースは意図的に避けている）。
   **床側 `region_gen::floor` の外周面判別（`f.signed_area > 0.0`、変更していない）も
   同種の丸め誤差のリスクを理論上は抱えているが、床の実フィクスチャでは複数階の
   節点が同一 XY を持つことがなく現状は再現していない。** 床側でこの種の不具合が
   将来見つかった場合は、同じ `MIN_FACE_AREA_MM2` 相当のしきい値を導入すること。

### 5.8 `RegionAnchor` の床領域アンカー追加と `WallPlate` 型の実装（2026-08-26、未結線）

E4〜E7（`WallRegion`/`WallPlate` の型実装・要素生成・参照張り替え）着手にあたり、
dig で実装順序・PR の切り方（1 PR で一括、E7 の「一括実施」の精神をそのまま適用）・
保有水平耐力の前提作業（`wall_model.rs` へ Ds 群判定のスナップショットを追加してから
参照を張り替える）・`WallPlate` の型構造（`Slab` と同じ独立エンティティ）・
自重の算定方式（`Slab` と同じく必ず断面参照。`OutOfFrameMiscWall` の直接入力は廃止）を
確定した。本節はその最初の一手（型の骨格）を記録する。**`WallRegion` の作り替え・
要素生成・参照張り替えはまだ行っていない。**

**着手前に修正した前提**: `wall_model.rs`（E8 の壁入りフィクスチャ）は柱脚どうしの
梁を持たず `region_gen::wall` の境界検出を一切通らないことが分かっていたため
（§5.7）、柱脚間の梁 4 本を追加し 4 面すべてを閉じた矩形にした。副作用として
耐震壁の四周判定（`wall_is_framed`）が働くようになり、フィクスチャがもともと
抱えていた不正な混合構造（RC 耐震壁に S 造の側柱・大梁）が表面化したため是正した
（詳細・実測値の変化は §3.2 E8 の項を参照）。

**`RegionAnchor::FloorRegion` の追加**: D14 の「取付き先は線／点／床領域の 3 つ」の
うち床領域アンカーが未実装だった。自立壁（床の上に立つ間仕切り等）が使う。
`{ region: FloorRegionId, nodes: [NodeId; 2] }`（dig Q6=B）。`nodes` は壁自体の
平面上の始点・終点（節点参照。線アンカーと同じ理由で節点移動に追随させる）で
自重の算定（面積）に使い、荷重は `nodes` ではなく `region`（所属する床領域）へ渡して
等価な面荷重へならす（D17。位置情報は失われるが長さは失われない）。既存の
`RegionAnchor` の全 match は `_` を使わない網羅的マッチだったため、コンパイラが
影響箇所を全数洗い出した（本番コード 10 箇所、6 クレートにまたがる）。このアンカーは
壁専用（床板 `Slab` の取付き先には使わない）であるため、`Slab` 側の全箇所は
「到達しないが網羅のために必要な受け皿」として `None`/`false`/no-op を返すよう
明示した（`_ =>` のワイルドカードで握りつぶさない。既存の `Slab::edge_nodes` に
1 箇所あった `_ => None` も同じ理由で明示化した）。

**`WallPlate`/`WallPlateShape` の実装**: `Slab`/`SlabShape` と同型（囲まれた領域 /
取り付く領域）の独立エンティティとして実装した（dig Q3=A）。相違点は次の 2 つ。

1. **取付き線アンカーの張り出し方向が異なる。** 床は取付き線の左向き法線方向
   （水平面内）へ張り出すが、壁は鉛直上向き（D15「壁は立ち上がり高さ」）へ
   立ち上げる。`WallPlate::boundary_coords` はこの違いを反映した専用の
   `extrude_up` を使う（`Slab::boundary_coords` の水平張り出しロジックとは別実装）。
2. **自重は必ず断面参照から求める。** `OutOfFrameMiscWall` が持っていた直接入力
   （`weight_per_area`・`thickness`）は廃止した（dig Q5=A。ST-Bridge 取り込みに
   生成コードがなく実データが 0 件だったため、移行対象の実利用がないと判断した）。
   `Model::wall_plate_self_weight` は `Model::slab_self_weight_intensity` と同じ
   規約（断面未割当は `None`。既定厚で補わない）。

参入レベル（4 節点要素を生成する構造壁／n倍法で偏心率にのみ寄与する雑壁剛性／
自重のみ）を区別する列挙型は `WallPlate` に持たせなかった（dig Q4=B）。`section` の
有無と、所属する `WallRegion` の種別（囲まれた領域か取り付き領域か）の組み合わせで、
生成ロジック（Step 8 本体）側が決める設計とした。

`Model` への統合（`wall_plates: Vec<WallPlate>` フィールド、`validate` の境界重複・
陳腐化参照・取付き線区間の検証、節点参照の巡回、`SectionId` 参照の巡回）は `Slab`
と同じ規約で一通り行った。**`WallRegion.wall_plate_ids`（`Slab` の `slab_ids` に相当）
は `WallRegion` の作り替えとあわせて次に実装するため、「複数の壁領域から共有されない」
検証・`visit_wall_plate_ids`/`retain_wall_plates`（`Slab` の同等物）はまだない。**

**副産物（敵対的な自己レビューで発見・修正）**: `region_gen::wall::WallRegionBoundary::area`
が自前実装していたニューエルの公式（`newell_area`）は、`crate::geom::polygon_area_3d`
（壁・シェル要素の自重算定とスラブ・壁の数量拾いが共通で使う既存関数。2026-08-12
導入、region_gen::wall 実装〔前回 PR #224〕より前から存在）と完全に同じ計算だった。
region_gen::wall 実装時に見落として重複させていたため、自前実装を削除し既存関数へ
統合した（数値は完全一致。既存テスト・実フィクスチャのピン止め値で確認済み）。

**未結線であることの明記**: `WallPlate` はまだ `WallRegion`・要素生成・ST-Bridge
入出力・UI のいずれとも結線していない。次の一手は `WallRegion` の作り替え
（`id`・`name`・`boundary`・`wall_plate_ids: Vec<WallPlateId>` へ。現行の
`wall: Option<ElemId>` を廃止）と、`region_gen::wall` の境界から `WallRegion` を
組み立てる `wall_region_rebuild`（`region_rebuild.rs` の壁側版）である。

### 5.9 `WallRegion` を `FloorRegion` と同型へ作り替え（2026-08-26、未結線）

§5.8 の次の一手のうち `WallRegion` の作り替えを実施した。`region_gen::wall` の
境界から `WallRegion` を組み立てる `wall_region_rebuild`・要素生成・ST-Bridge 移行・
参照張り替えはまだ行っていない。

**型の変更**: `WallRegion { wall: Option<ElemId>, post_ids }` を
`WallRegion { id: WallRegionId, name: String, boundary: Vec<NodeId>,
wall_plate_ids: Vec<WallPlateId>, post_ids }` へ作り替えた（`FloorRegion` と同型）。
`model/wall.rs`（壁属性を集めたファイル）から独立した `model/wall_region.rs` へ移した
（`FloorRegion` が `region.rs` に独立しているのと同じ理由。`wall.rs` は
`WallAttr`/`OutOfFrameMiscWall` 等、Step 7+8 で廃止予定の型が主体のため）。

`WallRegion` は**もはや `ElemId` を一切持たない**（D4・D5: 壁の解析要素は準備計算
からの生成物であり、モデルには残さない）。これに伴い、以下の連動処理を削除した
（残すと存在しない不変条件を守ろうとして無意味なコードになる）。

- `Model::visit_elem_ids` の `WallRegion.wall` を辿る分岐
- `DeleteMember`/`InsertMember`（squid-n-edit）の「削除した壁要素を指す壁領域を
  『版なし』へ戻し、undo で復元する」カスケード処理（`wall_region_refs` フィールド）
- `squid-n-edit::secondary` の `AddWallRegion`/`DeleteWallRegion`/`InsertWallRegion`/
  `SetWallRegion`（壁領域を丸ごと追加・削除・全置換する CRUD コマンド）

**壁領域は床領域と同じく「主架構から再構成される派生的な入力の持ち主」（D10）であり、
利用者が任意の境界を与えて追加・削除する対象ではない**（`region_gen::wall`/
`wall_region_rebuild` が組み立てる）。`FloorRegion` に `AddFloorRegion`/
`DeleteFloorRegion` が存在しないのと同じ理由で、上記 CRUD コマンドは削除し、
利用者が変更できる範囲を表示名（`SetWallRegionName`。`SetFloorRegionName` と同型）と
間柱の割当（`SetWallRegionPostIds`。`SetSlabSecondaryJoistIds` と同型）だけに絞った。

**`Model` への統合**: `wall_regions` の `id == index` 検証、境界の陳腐化参照・重複
検証（D1: 同じ境界を持つ壁領域は 2 つ不可）、`wall_plate_ids` の陳腐化参照・重複・
「複数の壁領域から共有されない」検証（床板と同じ規約）、`visit_wall_plate_ids`
（`visit_slab_ids` の壁側版）を追加した。`retain_wall_plates`（`retain_slabs` の
壁側版）は、壁版を間引く経路（UI・ST-Bridge 移行）がまだないため見送った。

### 5.10 `wall_region_rebuild` を実装し、準備計算・取り込みへ結線する（2026-08-26）

§5.9 の次の一手として `crates/squid-n-core/src/wall_region_rebuild.rs` を新設し、
`rebuild_floor_regions`（床側）と同じ 3 箇所（`squid-n-app::app::actions::loads::
sync_auto_load_cases_action`、`squid-n-job::prepare::prepare_model_for_analysis`、
ST-Bridge 取り込み `assemble.rs`）へ `rebuild_wall_regions` を結線した。D5 が求める
「GUI の準備計算とヘッドレス実行の両方から同じ関数を呼ぶ」を壁側でも満たす。

**これは `region_gen::wall`/`RegionAnchor::FloorRegion`/`WallPlate`/`WallRegion` の
中で初めて実データパイプラインへつながったコード**である。ただし壁版
（`WallPlate`）の取り込み・要素生成はまだ行っていないため、`model.wall_regions` は
境界の幾何（`id`・`name`・`boundary`）だけが実際に埋まり、`wall_plate_ids` は常に
空のまま（版なし壁領域と同じ状態）になる。

**アルゴリズム**（`rebuild_floor_regions` との相違点）: 床は「同じレベル Z」で
新旧の床領域を対応付けるが、壁は構面（直線）ごとに閉領域が現れるため、まず
`WallRegionBoundary::is_same_plane`（候補点が実距離で同じ直線上にあるか。
`region_gen::wall` の直線統合と同じ判断基準）で構面を絞り込んでから、構面の
局所座標 `(s, z)` へ射影した重心で内包判定（`WallRegionBoundary::contains`）を行う。
面積は常に実座標（3 次元）から求める（§3.2 E3 を踏襲。射影はトポロジー判定にのみ使う）。
`WallRegionBoundary` にこの判定用の `contains`/`is_same_plane` を新設した
（`RegionBoundary::contains`/`is_same_level` の壁側版）。

**床側 D20（パネルに収まらない版を取り付き版へ自動変換する）に相当する規則は
壁側でまだ決めていない。** 囲まれた壁版でどの壁領域にも収まらないものは、変換せず
「帰属なし」として件数のみ報告する（残課題。取り付き領域〔パラペット・腰壁・
垂れ壁・自立壁〕の自動判定基準を dig で確定してから実装すること）。

**検証**: 実フィクスチャ（`full_model.rs::import_populates_wall_regions_from_region_gen`）
で、ST-Bridge 取り込みが実際に `model.wall_regions` を 55 件（`region_gen_finds_
wall_bounded_regions` と同じ件数）埋めること、`id == index` が保たれること、
本フィクスチャは壁版を持たないため `wall_plate_ids` が全件空であることを確認した。
`wall_region_rebuild.rs` 自身の単体テスト（4 件: 検出・名前と間柱の引き継ぎ・
壁版の帰属・帰属なしの壁版が残ること）も追加した。**この結線でも
既存の解析結果（`snapshot_key_scalars`・`snapshot_wall_bay_scalars`）は無変化**
（壁領域の追加は純粋に付加的なメタデータで、まだ何の計算にも使われないため）。

### 5.11 §5.10 の結線後レビューで見つかった 2 件の抜けを埋める（2026-08-26）

§5.10 の結線をコミットした直後、実データパイプラインへ初めてつながったことを
理由に改めてレビューし、以下を修正した。

- **`WallPlate` が `WallAttr` の開口関連フィールドを 2 つ引き継いでいなかった。**
  `opening_area`（個別開口 `openings` が空のときのフォールバック用の合計面積）と
  `opening_weight`（開口部・サッシ等の重量。控除後に加算）が抜けており、
  `WallAttr` を「合計面積のみ」で運用している壁を `WallPlate` へ移行すると
  開口控除が一切効かなくなる欠陥だった。dig Q5（自重は断面参照のみで求める）は
  `weight_per_area`/`thickness`（直接入力の面重量・厚さ）の廃止だけを扱っており、
  開口関連フィールドの扱いは合意事項に含まれていなかった。`WallAttr` と同じ
  2 フィールドを追加し、`total_opening_area`・`wall_plate_self_weight` の式を
  `WallAttr`/`enumerate_self_weight` と同じ規約に揃えた。
- **性能の実測が「グリッドの合成データのみ」で、実データでの比率が未確認だった。**
  `wall_region_rebuild` は §5.10 で `sync_auto_load_cases_action`（対話的操作）にも
  結線したため、超線形の絶対時間（残課題一覧参照）だけでなく「準備計算全体に対する
  比率」も見ておく必要があった。実 ST-Bridge フィクスチャで実測したところ
  `scan_wall_region_boundaries` は準備計算全体の 7.4%（0.36ms/4.9ms）で、この
  フィクスチャの規模では無視できる。ただし 900 本規模での絶対時間（1.8 秒）は
  未解消のまま残っており、規模が大きいモデルでは対話的操作のたびに数秒級の
  遅延が起きうる（残課題一覧に追記）。
- ついでに `WallRegion`/`WallPlate` のモジュール doc の「未結線」表記が §5.10 の
  結線前のまま更新されておらず、実態と食い違っていたため書き直した。用語集の
  「壁領域」「壁領域の境界」「壁版」の 3 エントリも同様に更新した。
- `WallRegion` の ID は `region_gen::wall` の走査順（`model.elements` の並び順に
  依存。D10）に頼っているため、「保存 → 読込 → 再度準備計算」を経て走査順が
  ぶれないことを固定する回帰テスト（`full_model.rs::
  wall_regions_survive_save_reopen_reprepare`）を追加した。

**未着手のまま残る指摘**: `WallPlate::area` は境界の生の面積（ニューエルの公式）
であり、既存の `enumerate_self_weight`（`squid-n-load`）が耐震壁の自重に適用して
いる「周辺柱梁の内法寸法」による面積低減（`wall_clear_area_factor`）を持たない。
自重算定を `WallAttr` から `WallPlate` へ移行する際にこの低減をどこへ持たせるかを
先に決めておかないと、型移行のはずが計算根拠まで変わってしまう（残課題一覧参照）。

### 5.12 壁入り Ds 群・保有水平耐力の回帰テストを用意する（dig 2026-08-26 Q1・Q2、2026-08-26）

参照張り替え（`holding.rs`/`pushover.rs` の ElemId→領域参照）に着手する前に、
壁入りモデルで一度も回帰されていなかった Ds 群・保有水平耐力・増分解析を
固定する（dig Q2 の前提条件）。実装前に dig で 2 点合意した。

- **Q1（WallPlate 取り込みの着手順序）**: WallPlate の ST-Bridge 取り込みは
  要素生成（D5）と同時に 1 つの変更として実施する。`build_walls`
  （`assemble.rs`）が今も `RawWall` から `ElementKind::Wall` を直接生成して
  おり（モジュール内の「`WallAttr` も生成する」というコメントは誤りで、
  実際は要素のみ生成していたことも判明した）、`WallPlate` を経由する経路が
  まだ存在しない。取り込みだけを先行させると「取り込まれるが解析には効かない
  `WallPlate`」を自ら作ることになるため。
- **Q2（回帰テストの中身）**: `wall_bay_model()` の耐震壁断面に
  `SectionShape::RcWall { thickness, ps }` を実際に設定して仕上げる。
  `shape: None` のままだと壁は Ds 判定から常にスキップされ選択ランクへ
  フォールバックし、回帰テストとして意味を持たないと判明したため。

**実装したところ、当初想定していなかった 2 つの実害が見つかった。**

1. **側柱・壁上下大梁の「鋼断面の数値流用」トリックが増分解析で破綻した。**
   §3.2 E8 で作った本フィクスチャは、耐震壁の側柱・壁上下大梁を
   `wall_frame_category_issue`（周辺架構の構造種別を壁と揃える）に通すため、
   鋼断面（`SteelH`）の断面性能をそのまま流用し材料だけ RC へ差し替えていた。
   これは実在しない断面（形状は鋼、材料は RC）で、線形解析（弾性剛性・自重）
   は材料の E・ρ だけを見るため気づかれずに通っていたが、増分解析のファイバー
   断面は `shape` だけで鋼材と判定して材料の `fy` を要求し収束エラーになり、
   保有水平耐力の壁の等価引張鉄筋比 pte も側柱の主筋（`RcRect` の `rebar`）が
   なければ算定できずエラーになった。ユーザー指摘（「RC 壁には RC 断面しか
   使わないのになぜ S 断面でテストするのか」）を受けて、本物の
   `SectionShape::RcRect`＋主筋（300x300・300x400、3-D22・D10@100、
   `MaterialId(2)` として SD345 を新設）に差し替えた。
2. **`test_wall_element_changes_eigen_period` に方向を無視した比較のバグが
   あった。** 本物の RC 矩形に差し替えると、壁の有無で `period[0]`（周期が
   最も長い最初のモード）が指す**方向自体が入れ替わる**ことが分かった
   （壁ありは壁の影響を受けない Y 方向が最も柔らかく `period[0]` を占め、
   壁なしは壁を失った X 方向が最も柔らかくなって `period[0]` を占める）。
   このため単純な `period[0]` 比較は無関係な方向どうしを比べており、
   「壁が剛性に効いていない」という誤った結論になっていた（鋼断面流用
   トリックがこの誤りを偶然覆い隠していた）。`ModalResult::participation`/
   `effective_mass` の X 成分（壁は Y=0 面で X 方向にスパンするため、
   面内せん断で効くのは X 方向のみ）が卓越するモードの周期を比較するよう
   修正した（`dominant_x_period` ヘルパー）。修正後の実測: 壁ありの X 方向卓越
   周期 0.0527s、壁なし 0.0725s（約 27% の差）。鋼断面流用トリック当時の
   `period[0]` 単純比較（約 10%強で通っていた）より、遥かに大きく、かつ
   物理的に正しい差になった。

上記 2 点の修正により `snapshot_wall_bay_scalars` の代表スカラは再び変わった
（総二次的重量・固有周期・壁材端力・頂部変位。実測して更新した）。加えて、
新規テスト `snapshot_wall_ds_group_and_holding_capacity` を追加し、壁入りの
Qu・Ds・Qun・部材ランクをピン止めした。参照張り替えで壁の応答
（`resp_by_elem`）や壁長・壁厚の参照が壊れると、壁は判定不能としてスキップ
され層は選択ランクへ**エラーにならず静かに**フォールバックするため、値の
変化でしか検出できない。

**この回帰テストの実装自体に、敵対的レビュー（利用者指示、2026-08-26）で
3 点目の実害が見つかった。** `App::design_frame`（Ds 表の行を選ぶ架構種別
設定）が既定の `FrameType::SteelFrame` のままで、RC 耐震壁を持つ本フィクス
チャでも Ds 計算が鋼構造の表（`ds_steel`）を使ってしまい、この回帰テストが
目的としていた RC 耐力壁の Ds 表（`ds_rc`）を一度も通っていなかった
（実測 Ds=0.25 は `ds_steel` の A・βu=0 の最小値で、RC 造の `ds_rc` が
取りうる最小値 0.30 を下回っていたのが手がかりだった。当初の範囲チェック
`(0.2..=0.6).contains(&s.ds)` がこの矛盾を覆い隠していた）。`wall_bay_app`
で `design_frame = FrameType::RcWall` を明示し、範囲チェックも RC 造の
規定範囲 `0.30..=0.55` へ絞った。修正後の実測: Qu=9.529e5N、Ds=0.35、
Qun=2.290e4N、rank=FA（この値でピン止め直した）。

---

### 5.13 壁領域 rebuild の敵対的レビュー指摘を修正（2026-08-26）

PR #225 のレビュー指摘 1・2、および結線に関する指摘 3 のコード側を修正した。

- `match_candidate` は候補の先頭点だけでなく**全頂点**の XY が同じ構面上にあることを
  確認する。内包判定点は、全頂点を構面の局所座標 `(s, z)` へ射影した**shoelace の
  多角形重心**とする。符号付き面積がほぼ 0 のときは床側と同じく頂点平均へフォール
  バックする。面積自体は従来どおり実座標の 3 次元面積を用いる。
- 旧 `WallRegion.post_ids` は継承しない。領域を書き込んだ後、間柱を ID 順に走査し、
  中点の構面一致と局所 `(s, z)` の厳密内包がちょうど 1 領域となる場合だけ D7 で
  `post_ids` へ入れ直す。失敗件数は `unassigned_posts` として報告する。
- ST-Bridge 取り込みでは `unassigned_posts` と `unmatched_old_regions` を warnings へ
  出す。領域 rebuild はすでに結線済みだが、壁版の取り込み・要素生成は未着手である。
- 旧 `.scz` の `WallRegion` は msgpack の位置配列を含むため、型変更により読み込みに
  失敗しうる。「次回準備計算で作り直される」という JSON 前提の説明は不正確だった。
  旧形式の読み込みを保証する移行処理は実装していない。

---

### 5.14 壁版の取り込み・要素生成・参照張り替え（2026-08-26、dig 7問）

§9 残課題「壁版（`WallPlate`）の取り込み・要素生成（D5）・参照張り替え」に着手し、
実装した。着手前に dig（利用者との対話）で 7 点を確定した。

**Q1（壁展開モデルの位置づけ）**: 生成した壁要素は `Model.elements` へは入れず、
`Model` とは別の一時的な派生値（**壁展開モデル**）としてのみ存在させる（D5 の原文
どおり。既存の剛域・荷重ケース同期のように `model` へ直接書き戻すパターンは採らない）。
**Q2（キャッシュの要否）**: 壁展開は都度計算とし、`App` に専用のキャッシュフィールドは
持たせない（境界検出という重い幾何走査は `rebuild_wall_regions` が既に済ませて
`model.wall_regions` へ持たせているため、展開自体は軽量）。**Q3（ST-Bridge出力の
展開責任）**: `export_stbridge` は内部で自己完結して展開する（呼び出し元に展開を
要求すると、忘れたときに壁が出力から静かに消える）。**Q4（GUI診断）**: `run_diagnostics`
も壁展開モデルへ切り替える（`model_issues` は診断とソルバ前チェックの共有関数のため、
GUI診断だけ未展開のままだと壁関連の不備が診断タブに出ない）。**Q5（自重算定の計算
根拠）**: `enumerate_self_weight` は無改修とし、`WallPlate` の開口・三方スリットは
壁展開モデルへ合成した `WallAttr`（下記参照）経由で読む。型移行で計算根拠を変えない
ことを最優先する。**Q6（T字取り付き境界の扱い）**: 境界が 4 節点でない壁版（実測
55件中2件、3.6%）は要素を生成しない（`wall_element_geometry` が `nodes.iter().take(4)`
と先頭4節点しか使わない実装であるため、5節点以上をそのまま生成すると幾何が壊れる。
一般化は残課題）。**Q7（回帰テスト）**: 壁ラウンドトリップ・断面力の `ElemId` 逆引き・
GUI診断反映の3種を新規に固定する。

途中、利用者からの直接の疑問（「4節点を超える壁は現在作成可能か」）を受けて Q6 の
前提を再検証し、当初「保守側フォールバック」と説明していたのを訂正した（幾何が
壊れる不具合であり安全側の近似ではない）。またadvisorのレビューで、二重展開の危険
（下記参照）と、回帰検知網が実際には新しい経路を通っていない懸念（`wall_bay_model`
が旧来どおり生の `ElementData`＋`WallAttr` のままでは、壁展開モデルへの移行を全く
検証しない）を指摘され、両方に対応した。

**実装内容**:

- **`squid_n_load::wall_expand`（新設）**: `expand_wall_elements(&Model) -> (Model,
  WallExpansionIndex, WallExpansionReport)`。`model.wall_regions` を辿り、`Enclosed`
  形・`section` 割当あり・境界4節点の `WallPlate` ごとに `ElementKind::Wall` を
  生成する（既存の `ElemId` 最大値より後ろへ決定的に採番）。生成対象外
  （`WallPlateShape::Attached`・`section` なし・非4節点）は `WallExpansionReport`
  で件数のみ報告する（Q6=C）。
- **`wall_attrs` の合成（移行期の内部表現）**: 生成した壁要素ごとに、由来する
  `WallPlate` の開口・三方スリットを写した `WallAttr` を壁展開モデルの `wall_attrs`
  へ積む。自重算定（`squid-n-load::story_gen::self_weight_calc`）を筆頭に、開口低減
  剛性（`squid-n-element::factory::wall_opening`）・偏心率の雑壁剛性・数量拾いなど、
  `model.wall_attrs.iter().find(|a| a.elem == data.id)` という同じ形の参照を持つ
  既存の壁消費者（実測9箇所）を、壁展開モデルを受け取るだけで**無改修のまま**動かす
  ための橋渡し（dig Q5 の狙いをコード化したもの）。`WallAttr` の恒久存続を意味しない
  （D3・E5 の吸収方針は変わらない）。
- **二重展開の防止**: 壁展開モデルへ再度 `expand_wall_elements` を適用すると、
  同じ壁版からもう一組の要素・合成 `WallAttr` が追加され、壁の自重・剛性が
  二重計上される（`ElemId` は既存の最大値より後ろへ振るため ID 衝突では検出
  できない）。現状の呼び出し経路（自重算定・ST-Bridge出力の内部展開、ソルバ・
  GUI診断の呼び出し元展開）は互いに素であることを確認したが、defense-in-depthとして
  `expand_wall_elements` 自身が `debug_assert!` で「入力モデルに壁要素が最初から
  含まれていないこと」を検査する（release ビルドではコストなし）。
- **ST-Bridge 取り込み（`assemble.rs::build_walls`）**: `ElementKind::Wall` を直接
  生成する代わりに `WallPlate`（`Enclosed` 形）を `model.wall_plates` へ積むだけに
  変えた。直後に呼ばれる `rebuild_wall_regions`（既に結線済み）が、検出済みの
  壁領域へこの壁版を帰属させる。開口・三方スリットは ST-Bridge に対応情報源が
  ないため引き続き既定値（開口なし）。
- **ST-Bridge 出力（`export.rs::export_stbridge`）**: 関数冒頭で壁展開モデルを
  組み立て、以降のスコープで `model` をそれへ差し替える（Q3=B）。壁の断面・部材
  出力（`members_body`/`wall_sections`）は無改修のまま、生成要素を含むモデルを
  見るだけで壁が出力に現れる。
- **参照張り替え（解析）**: `squid_n_job::compute` の各公開関数
  （`compute_linear_static`/`compute_eigen`/`compute_seismic`/`compute_pushover`/
  `compute_time_history`/`compute_lumped_mass`）の冒頭で壁展開する
  共通ヘルパー `expand_walls` を新設し呼び出した。**ただし `squid-n-app` 側には
  `Analysis::prepare` を直接呼ぶ独自経路が別途あり**（`app::actions::linear_static::
  {compute_combination, compute_static_all}`。一括解析・荷重組合せが使う、
  `squid_n_job::compute` を経由しない重複実装）、実装中に
  `snapshot_wall_bay_scalars` の member_forces 探索が失敗して発覚した。この2箇所にも
  同じ展開を個別に追加した。`summary.rs`（レポートCSVの主軸角計算）にも同様の
  局所的な `Analysis::prepare` 呼び出しがあり、同じく展開を追加した。
- **参照張り替え（GUI診断）**: `App::run_diagnostics` が `model_issues` へ渡す
  モデルを壁展開モデルへ切り替えた（Q4）。回帰テスト
  `test_wall_frame_mismatch_appears_in_gui_diagnostics`（`wall_model.rs`）を新設し、
  側柱を意図的に鋼へ差し替えた壁入りモデルで `run_diagnostics` が構造種別食い違いを
  検知することを固定した（この検知は壁展開を経由しない限り `model.elements` の
  壁要素が 0 件のため一切働かない）。
- **参照張り替え（保有水平耐力）**: 追加のコード変更は不要だった。`compute_pushover`
  経由で壁展開モデルが渡ればそれ以降の処理（Ds群判定・Qu算定・部材ランク）は
  `model.elements` を種別非依存に走査する既存実装がそのまま機能する。
  `snapshot_wall_ds_group_and_holding_capacity` が無改修で通ることで確認した。

**未対応のまま残る参照張り替え（残課題）**: GUI の断面力表示（`tables/members.rs`・
3Dビューア）は `app.model.elements` を直接走査する実装のため、生成された壁要素の
結果（`app.results.*.member_forces` に含まれる）を一切表示できない（無視されるだけで
クラッシュはしない）。ナビゲータ・壁属性編集タブ（`tables/wall_attrs.rs`）も
`model.elements` の `ElementKind::Wall` を対象にするため、生成専用となった今は
対象が常に空になる。これらは UI 層（Step 9 の領域）の作業で、今回のスコープには
含めていない。

**2026-08-27 追記**: このうち 3D ビューアの形状・検定比図・モデル化図は
§5.17 で是正済み（成功条件は「壁が見える」）。部材表（`tables/members.rs`）は
未対応のまま。応力図は壁の内力行を無視しなくなったが、描画材軸は下辺 2 節点の
線材前提のまま（§5.17 残課題）。壁属性編集タブも未対応。

**実測で見つけた既存の想定違い（advisorのレビューで発覚）**:

1. **`squid-n-app` の一括解析・荷重組合せが `squid_n_job::compute` を経由しない
   独自実装を持っていた。** 単体の荷重ケース解析（`run_linear_static`）は
   `squid_n_job::compute::compute_linear_static` を呼ぶが、一括解析
   （`run_static_all` → `compute_static_all`）と荷重組合せ（`compute_combination`）は
   `Analysis::prepare` を直接呼ぶ重複実装だった。壁展開の抜け漏れとして顕在化したが、
   これ自体は `squid-n-job`/`squid-n-app` の重複実装という別の残課題でもある
   （`クレート横断の重複統合_申し送り.md` へ追記候補）。
2. **`Analysis<'m>` は `&'m Model`（借用）を保持するため、`Analysis::prepare` 自身の
   内部で壁展開を完結させることはできない。** 壁展開はモデルを複製して要素を追加する
   操作で、`Analysis::prepare(model: &'m Model) -> Self` はその複製をローカル変数として
   作って `&'m` で返すことができない（借用チェッカーが拒否する）。そのため展開は
   「`Analysis::prepare` を呼ぶ全ての箇所」で個別に行う必要があり、単一の choke point
   には集約できなかった（上記のとおり複数箇所に展開を追加した理由）。

**検証**: `crates/squid-n-app/tests/wall_model.rs::wall_bay_model` を、生の
`ElementData`＋`WallAttr` から `WallPlate`＋`rebuild_wall_regions` 呼び出しへ移行した
（`crates/squid-n-load/src/story_gen/tests.rs::wall_model` も同様に移行）。これにより
既存の回帰テスト（`snapshot_wall_bay_scalars`・`snapshot_wall_ds_group_and_holding_capacity`）が
実際に「取り込み→壁版→壁領域帰属→壁展開→要素生成→解析」という新しい経路全体を
通るようになった。**両スナップショットとも既存の固定値と完全一致した**（`insta`の
差分なし）。これは dig Q5 が狙った「型移行で計算根拠を変えない」ことの直接の裏付けで、
値が変わっていた場合はどこかに計算根拠の変化（バグ）が入り込んだことを意味していた。

`cargo test --workspace`（全20バイナリ）・`cargo clippy --workspace --all-targets
--locked -- -D warnings`・`cargo clippy -p squid-n-app --all-targets --features gui
--locked -- -D warnings`・`cargo clippy -p squid-n-mcp --all-targets --features mcp
--locked -- -D warnings`・`cargo fmt --check` をすべて通過した。

**残課題（次の一手）**:

- GUI の断面力表示・壁属性編集タブ・3Dビューアの壁対応（上記参照。UI層、Step 9）
- 取り付き壁版（`WallPlateShape::Attached`。パラペット・腰壁・垂れ壁・自立壁）の
  ST-Bridge 取り込み・UI 入力経路は依然として存在しない（型は §5.8 で実装済みだが
  未到達のまま）。D16・D17（荷重の出口）の実装もまだ
- `wall_clear_area_factor`（自重の内法寸法補正）は引き続き `elem.nodes.len() == 4`
  のときのみ有効（5節点以上は1.0固定）。今回 Q6=C により5節点以上の壁は
  要素そのものを生成しないため、この制限に触れる機会はなくなったが、関数自体は
  無改修のまま残っている
- T字取り付き境界（Q6 該当。実測3.6%）を按分等で一般化し、耐震壁として生成する
  経路（残課題として明記のみ。実装は未着手）
- `squid-n-app` の一括解析・荷重組合せが `squid_n_job::compute` を経由しない重複実装を
  持つ問題（上記「実測で見つけた既存の想定違い」1点目）の解消
- `expand_wall_elements` の性能は、900本規模の柱を持つモデルでの実測がまだない
  （境界検出〔`region_gen::wall`〕の重い部分は `rebuild_wall_regions` 側で既に
  実測・最適化済みだが、展開自体を大規模モデルで計測したことはない）

---

### 5.15 壁展開モデルを見ていなかった4箇所の是正（2026-08-27、敵対的レビュー起点）

**対象**: `squid_n_job::prepare::apply_rigid_zones_and_panels`（剛域自動算定）が
壁展開（§5.14）より前のタイミングで走っており、`rigid_zone_consider_walls`
（既定 true）が壁版経由の壁に対して恒久的に無効化されていた不具合の是正。
**区分**: §5.14 の実装レビューで発覚した回帰の是正（新機能ではない）。

§5.14 の実装後、レビュースキルを使わない敵対的レビュー（利用者の指示）で以下が
発覚した。

- `squid_n_job::prepare::apply_rigid_zones_and_panels`（GUI の
  `App::apply_rigid_zones_for_analysis` ・job/MCP の `prepare_model_for_analysis`
  が呼ぶ、本番の呼び出し元はこの1箇所のみ）は、**壁展開前の生モデル**（D5に
  より `ElementKind::Wall` を1件も持たない）に対して
  `squid_n_element::beam::apply_auto_rigid_zones` を直接呼んでいた。
- 内部の `collect_rigid_zone_walls`（`squid_n_element::wall::misc_wall`）は
  `model.elements` を `ElementKind::Wall` で走査するだけなので、常に空を返す。
- 算定結果（`ElementData::rigid_zone`）はモデルへ直接書き込まれ、
  `Analysis::prepare` はこれを再計算せずそのまま読む。壁展開
  （`expand_wall_elements`）は既存要素をそのまま複製するだけで、`rigid_zone`
  を再算定しない。

結果として、`Model::stress_cfg.rigid_zone_consider_walls`（技術基準「柱には
袖壁、梁には腰壁・垂壁を含めた寸法で算定する」既定 true）が、壁版
（`WallPlate`）経由で壁が入るモデル全てで恒久的に無効化されていた。全断面材料を
RC に統一し `all_rc_src_at`（節点に集まる柱・大梁が全て RC/SRC であること。
1本でも S 系があると剛域そのものを設けない規則）が成立する検証モデルで実測し、
側柱の剛域長が壁なし 125.0mm・壁あり（`length_j`）1262.5mm、壁頂部大梁が壁なし
100.0mm・壁あり 1500.0mm と、壁の有無で剛域長が桁違いに変わることを確認した。
全解析（線形静解析・固有値・地震・増分解析・時刻歴）の剛性を静かに誤らせる、
§5.14 の実装レビューでは見落とした回帰だった。

既存の `wall_bay_model` 回帰網（§5.14）がこれを検知できなかった理由: 同フィクスチャは
「耐震壁と周辺架構の構造種別食い違い」検定用に周囲の柱・梁を意図的に RC/鋼混在
にしており、各節点が必ず1本は鋼部材と接する。`all_rc_src_at` が全節点で false に
なるため、壁の有無に関わらず剛域長が常に0になり、この不具合を偶然素通りしていた。
「回帰検知網が新しい経路を実際に通っている」ことの確認は、通過そのものだけでなく
検知対象の値が実際に動きうる構成かどうかも見る必要がある、という教訓。

副次的に、`crates/squid-n-app/src/app/preparation.rs`
（`build_prep_member_stiffness` の `has_wall_element` ゲート）も同じ理由で
「準備計算」タブの壁上下大梁剛性割増し確認表が機能しなくなっていた。ただし実害は
表示のみで、実際の解析（`construct.rs` が壁展開済みモデルに対して
`stiffness_breakdown` を呼ぶ）は正しく壁上下大梁の剛性割増しを適用していた。

**dig で確定した是正方針**:

- **是正方針**: `apply_rigid_zones_and_panels` 自身が内部で壁展開し、算定した
  `RigidZone` を既存要素のインデックス対応で非展開モデルへ書き戻す（仕口パネルは
  壁に依存しないため従来どおり非展開モデルへ算定する）。`squid-n-element` への
  `squid-n-load` 依存追加（自重算定と同じ「クレート内で完結」パターン）は採らない
  ——`squid-n-element` は `squid-n-load` に依存しておらず、新しい依存辺を作ると
  将来の循環依存リスクになる。`squid-n-job` は両方に依存済みのため、
  `apply_rigid_zones_and_panels`（本番の唯一の呼び出し元）を直すだけで
  GUI・job/MCP の両方が無改修で恩恵を受ける単一チョークポイントである。
- **`has_wall_element` 確認表**: 同じ波で是正する。`build_prep_member_stiffness`
  も内部で壁展開モデルを都度組み立てて判定する（`App` にキャッシュは持たせない。
  §5.14 の「都度計算」方針を踏襲）。
- **回帰テスト**: `squid-n-job` 層に全周 RC の最小フィクスチャを新設し
  （`crates/squid-n-job/src/prepare.rs::tests::apply_rigid_zones_considers_wall_plates`）、
  `apply_rigid_zones_and_panels` を直接呼んで壁あり/なしで剛域長が異なることを
  アサートする。是正前のコードに戻すと実際に失敗することを確認済み（意図せず
  素通りするテストになっていないことの検証）。既存 `wall_bay_model`（RC/鋼混在の
  構造種別食い違い検定用フィクスチャ）には手を入れない。

**実装**:

- `squid_n_job::prepare::apply_rigid_zones_and_panels` を、壁展開モデルで
  `apply_auto_rigid_zones` を実行し、結果の `RigidZone` を先頭
  `model.elements.len()` 件（壁展開は既存要素の末尾へ追記するだけなので
  展開前後で 1:1 対応する）だけ非展開モデルへ書き戻す実装へ変更した。
- `crates/squid-n-app/src/app/preparation.rs::build_prep_member_stiffness` が
  `self.model` の代わりに都度組み立てた壁展開モデルを走査するよう変更した。
- 回帰テスト `apply_rigid_zones_considers_wall_plates`
  （`crates/squid-n-job/src/prepare.rs`）を新設した。
- **性能ガード（advisorのレビューで指摘）**: 上記2箇所は解析エントリの都度
  （`ensure_preparation` 経由で）呼ばれるため、無条件に `expand_wall_elements`
  （`model.clone()`。要素数比例のコスト）を呼ぶと、壁を1枚も持たないモデル
  （実 ST-Bridge フィクスチャは現状すべて該当する）でも常に複製を払うことになる。
  `squid_n_load::wall_expand::model_has_wall_plates_to_expand`（`model.wall_regions`
  のいずれかが空でない `wall_plate_ids` を持つか、を見る安価な事前判定）を新設し、
  壁を持たないモデルでは複製を経ずに従来どおり非展開モデルへ直接算定する。

**検証**: 是正前のコードに一時的に戻して新規回帰テストが実際に失敗することを
確認（`length_i: 125.0 == 125.0` で `assert_ne!` が落ちる）。是正後は
`cargo test --workspace`（全バイナリ 0 failed）・`cargo clippy --workspace
--all-targets --locked -- -D warnings`・`cargo clippy -p squid-n-app
--all-targets --features gui --locked -- -D warnings`・`cargo clippy -p
squid-n-mcp --all-targets --features mcp --locked -- -D warnings`・
`cargo fmt --check` をすべて通過した。`wall_bay_model` の既存スナップショット
（`snapshot_wall_bay_scalars`・`snapshot_wall_ds_group_and_holding_capacity`）は
無変化（同フィクスチャは `all_rc_src_at` が常に false のため、この是正の影響を
受けない構成であることを再確認した）。

**残課題**: §5.14 の残課題（GUI の断面力表示・壁属性編集タブ・3Dビューアの壁対応・
取り付き壁版の入力経路・T字取り付き境界の一般化・`squid-n-app` の重複実装解消）
は変わらず残っている。

**是正後のadvisorレビューで新たに発覚し、追って是正した同種の不具合（3件）**:
剛域の是正を終えた後のadvisorレビューで、「`self.model`（壁展開前の生モデル）を
直接走査するせいで生成された壁要素が見えない」という同じ不具合パターンが、他にも
複数の消費者に残っていることが分かった。3件とも実行して不具合を再現・確認した上で
是正し、回帰テストを追加した（是正前のコードで実際にテストが失敗することを
確認済み。§5.15 の剛域是正と同じ検証手順）。

1. **`App::run_design_check` → `squid_n_design_jp::run_member_design_checks` →
   `joint_wiring::wall::check_walls`（耐震壁のせん断断面検定）。**
   `run_design_check`（`crates/squid-n-app/src/app/actions/design/check.rs`）は
   `&self.model`（生モデル）を渡していたが、渡す `member_forces`
   （`results.member_forces`）は壁展開済みモデルで解いた結果のため壁の `ElemId`
   を含む。`check_walls` はこの `ElemId` を `model.element(*eid)` で引き直すため、
   生モデルには存在しない壁の `ElemId` に対して毎回 `None` となり `continue`
   していた——**耐震壁のせん断断面検定が常にスキップされていた**（安全性に
   直結する検定が実行されないまま「検定結果」として GUI・MCP へ返っていた）。
   `run_design_check` の冒頭で壁を持つモデルなら壁展開モデルを組み立て、
   `run_member_design_checks` へ渡すよう是正した。MCP側の同等経路
   （`crates/squid-n-mcp/src/job/design_check.rs::compute_design_check_job`）も
   同じ理由（`work`＝壁展開前の準備済みモデルをそのまま渡していた）で同様に
   是正した。回帰テスト `test_wall_shear_check_appears_after_run_design_check`
   （`wall_model.rs`）で `joint_checks` に "耐震壁" ラベルの検定結果が現れる
   ことを固定した。
2. **`squid_n_design_jp::quantity::compute_quantity_takeoff`（数量拾い）。**
   GUI（`quantity_view.rs`）・レポートCSV（`summary.rs::build_quantity_csv`）・
   MCP（`squid-n-mcp/src/query.rs::quantity_takeoff_json`）のいずれも生モデルを
   渡しており、壁の数量（コンクリート体積・型枠面積等）が積算から静かに
   欠落していた。`compute_quantity_takeoff` 自身が内部で壁展開するよう是正した
   （自重算定 `enumerate_self_weight` と同じパターン。単一のチョークポイントで
   直せるため、3つの呼び出し元は無改修）。回帰テスト
   `test_wall_quantity_via_wall_plate_is_included`（`squid-n-design-jp`）を新設。
3. **`App::compute_holding_capacity`（保有水平耐力の部材ランク自動判定）。**
   自動判定（`design_rank_auto == true` のときのみ経路に入る）が
   `self.model.elements`（生モデル）を走査していたため、壁が
   `ElementKind::Wall` として分類されず、告示「耐力壁の種別」表（τu/Fc）による
   耐力壁のランク判定が一度も実行されなかった（`design_frame ==
   FrameType::RcWall` を宣言していても、耐力壁が層内に1枚もない扱いになり
   βu を使わない簡易 Ds 表へフォールバックしていた）。既存の
   `snapshot_wall_ds_group_and_holding_capacity` は `design_rank_auto` の既定値
   `false` のままこの分岐を一度も通っておらず検知できなかった（§5.15 の
   `wall_bay_model` が `all_rc_src_at` 常時 false で剛域の不具合を検知
   できなかったのと同じ「回帰網が経路自体を通っていない」パターン）。壁展開
   モデルを都度組み立てて走査するよう是正した。回帰テスト
   `test_holding_capacity_auto_rank_detects_wall`（`wall_model.rs`）を新設し、
   `design_rank_auto=true` で `ds_beta_u_unavailable`（βu 算定不能フラグ）が
   false になる（＝耐震壁が検出される）ことを固定した。

### 5.16 レビュー指摘の是正（2026-08-27）

PR #226 のレビューで確認した 2 点（剛域のインデックス書き戻し、保有水平耐力の
`model` 張り替え）は実装どおり成立していた。そのうえで次を是正した。

**設計判断（既存 Q3・Q6 に揃える）**:

- **ST-Bridge 書き出しの正は壁版。** Q3 は「呼び出し元に展開を要求すると壁が
  静かに消える」ことへの防御だった。書き出しが解析要素ではなく `WallPlate`
  （入力の正）を走査すれば展開は不要になり、4 節点でない壁版も往復する。
  シェルは従来どおり要素から出す。
- **4 節点以外は解析要素にしない（Q6=C のまま）。** 無言では落とさず、
  取り込み警告と `model_issues` の警告（解析は止めない）で知らせる。
  一般化は残課題のまま。

**実装**:

- `apply_rigid_zones_and_panels` の書き戻し直前に、先頭 N 件の `ElemId` 一致を
  `debug_assert!` する。
- `expand_wall_elements_owned` を新設し、すでに所有している `Model` を解析入口が
  二重 `clone` しないようにした。
- `export_stbridge` は壁展開をやめ、囲まれた壁版（3 節点以上）から `StbWall` を出す。
  部材 id はスラブの次から連番（壁版 id と柱要素 id の衝突を避ける）。
- 取り込みは 4 節点でない壁版・断面未割当の壁版を警告する。
- `model_issues` も同じ条件を警告する（診断と解析前チェックの共有関数）。
- 利用者向け `docs/`（ST-Bridge・準備計算・4.5・剛域・数量・線形静解析の入力チェック）
  に、壁版が入力の正であることと生成条件を追記した。

---

### 5.17 3Dビューア・応力図・検定比図・モデル化図の壁対応（2026-08-27）

**対象**: §5.14・§5.15 の残課題のうち「3Dビューアで壁が見える」こと
（Step 9 の一部。成功条件は形状・検定比ポリゴン・モデル化図の表示まで）。
壁属性編集タブ（`tables/wall_attrs.rs`）・部材表（`tables/members.rs`）は今回の
スコープに含めない。後者はいまも `app.model.elements` を走査するため生成壁は
行に出ない。

**問題**: §5.14 の指摘どおり、`crates/squid-n-app/src/viewer/` 配下の描画関数は
軒並み `app.model.elements` を直接走査しており、壁の解析要素（`ElementKind::Wall`。
D5 により生成専用で `app.model` には存在しない）を一切表示・参照できなかった。
実際に壊れていた箇所（実行して確認済み）:

- 3D ビュー本体（`viewer/mod.rs` の形状描画・選択ハイライト）: 壁がどの表示
  モードでも一切描かれない。
- 応力図（`viewer/diagram.rs`）: `results.member_forces` に含まれる壁要素の
  結果が `app.model.element(*elem_id)` の `None` で毎回無視される
  （参照は壁展開モデルへ差し替えた。描画材軸は先頭 2 節点のままで、囲まれた壁では
  下辺大梁に重なる。壁柱幾何への載せ替えは残課題）。
- 検定比図（`viewer/check_ratio.rs`）: 同様に `results.member_checks` の壁の
  検定結果が着色対象に入らない。
- モデル化図（`viewer/modeling.rs`）: 壁エレメント専用の描画関数
  （`draw_wall_element`・`draw_wall_polygon`・`show_wall_modeling_detail` 等）は
  既に実装済みだったが、入力が常に空の `app.model` だったため一度も実行されず
  死んでいた。

**是正方針**: `squid_n_job::prepare::apply_rigid_zones_and_panels`（§5.15）と同じ
「壁版を持たないモデルでは複製を経ずに借用する」性能ガードを、ビューア用に
`viewer::wall_expanded_view_model(&Model) -> Cow<Model>` として新設した。
ビューアは毎フレーム再描画されるため、`expand_wall_elements` の `model.clone()`
を無条件に払うと壁版を持つ大規模モデルでフレームレートが落ちる懸念があるが、
壁版を1枚も持たないモデル（既定・現状の実 ST-Bridge フィクスチャは全件該当）では
`model_has_wall_plates_to_expand` の安価な判定でガードし、複製自体を回避する
（壁版を持つモデルでは毎フレーム展開し直す。展開処理自体〔壁版数に比例〕は
軽く、重いのは境界検出＝`rebuild_wall_regions` 側でこちらは都度実行しないため
許容する。dig で確定した方針）。

**実装**:

- `wall_expanded_view_model` を `viewer/mod.rs` に新設し、以下の描画・ホバー経路を
  この関数が返すモデルへ差し替えた。
  - `viewer/mod.rs`: 形状描画メインループ・選択ハイライト・モデル化図/検定比図/
    ヒンジ図のホバーピック。
  - `viewer/scene.rs::draw_mode_rest_ghost`（モード形の静止形状ゴースト）。
  - `viewer/diagram.rs::draw_force_diagram`/`draw_component`（応力図。
    `viewer_panel` が渡す `display_model` を参照する。壁の内力は描くが材軸は
    `nodes[0]`–`nodes[1]` の線材前提）。
  - `viewer/check_ratio.rs::draw_check_ratio`（検定比図）。
  - `viewer/modeling.rs::draw_modeling`・`show_modeling_tooltip`・
    `show_wall_modeling_detail`（モデル化図。既存の壁専用描画ロジックがそのまま
    活きる）。
- **意図的にスコープ外としたもの**（据え置き。壁展開モデルへ切り替えても壁を
  正しく扱えない、または既存の入力経路が壁を対象にしていないため）:
  - **クリックによる部材選択・作成モード**（`viewer/mod.rs` の
    `pick_nearest_member(&app.model, ...)` 2箇所）は据え置いた。選択結果は
    `app.selection.members`・`app.hinge_detail_elem`・`app.th_detail_elem` へ
    入り、後者2つは `viewer/hinge.rs::draw_hinge_detail_content`・
    `viewer/th_detail.rs` の詳細ウィンドウ（`app: &mut App` で `app.model` を
    走査する）につながるため、クリック側だけ壁展開モデルにしても詳細ウィンドウ
    側が対応できず中途半端になる。今回は「壁が見える・結果を追える」までとし、
    「壁をクリックして詳細を開く」は別の一手とした。
  - **ヒンジ図のマーカー描画**（`viewer/hinge.rs::draw_hinge`）は据え置いた。
    2節点線材前提で `elem.nodes[0]`/`[1]` を材端とみなす実装だが、壁エレメントは
    4節点の境界多角形で「壁柱」の実体は `wall_element_geometry` 等で別途導出する
    必要があり（`modeling.rs` の壁描画と同じ導出が要る）、素の座標で描くと
    誤った位置に描画してしまう。壁展開モデルを渡しても新たに見えるようになる
    要素は壁（Wall/Shell。展開で追加されるのはこの2種のみ）だけなので、
    この画面に限っては据え置いても取りこぼしは増えない。ホバーピックは
    `display_model` を渡すが、`pick_nearest_member` は先頭 2 節点の線分距離だけを
    見る。囲まれた壁の下辺は大梁と一致し同距離は先勝ちのため、面内を指しても
    壁のツールチップは出ない（クリック選択と同じく残課題）。
  - CMQ 図（`viewer/cmq.rs`）は変更していない。対象を `ElementKind::Beam` の
    2節点部材に絞る既存の判定（`is_primary_beam_for_cmq`）が壁を最初から除外する
    ため、モデルを差し替えても表示は変わらない。
  - 断面ソリッド表示（`viewer/solid.rs::draw_section_solids`）も変更していない。
    `is_line_member` 判定が `ElementKind::Wall` を除外するため、モデルを
    差し替えても壁は描かれない（壁の板厚を立体表示する機能自体が未実装のため、
    表示すべきものがそもそもない）。

**docs/ の裏付け**: `docs/result_view/06_検定比の表示.md`・`07_モデル化の表示.md`・
`12_構面の表示.md` は、いずれも壁の描画（半透明ポリゴン塗り・「エ」状の壁エレメント
表示・4節点構面判定への算入）をすでに仕様として記述済みだった（`git log` で確認した
限り §5.14〔壁が生成専用になった変更〕より大幅に古い）。つまりこれらのページは
壁がまだ `app.model.elements` に直接存在した設計時点の記述で、§5.14 が黙って
その前提を壊し、記述どおりに動かなくなっていた。今回の是正はこの既存の docs/ の
記述を実装に追いつかせるものであり、**docs/ 自体の追記・修正は不要**（記述内容が
先に存在し、実装がようやく追いついた）。

**検証**: `wall_expanded_view_model` の単体テスト（壁版なしモデルは `Cow::Borrowed`
で複製されないこと、壁版ありモデルは `Cow::Owned` で `ElementKind::Wall` が
現れること）を `viewer/mod.rs` に追加した。GUI 描画そのもの（`egui::Painter` 依存）は
既存方針どおり単体テストの対象にしていない。`cargo test --workspace`・
`cargo test -p squid-n-app --features gui`・`cargo clippy --workspace --all-targets
--locked -- -D warnings`・`cargo clippy -p squid-n-app --all-targets --features gui
--locked -- -D warnings`・`cargo clippy -p squid-n-mcp --all-targets --features mcp
--locked -- -D warnings`・`cargo fmt --all -- --check` をすべて通過した。

**レビュースキルを使わない敵対的レビュー（利用者の指示）で追加で発覚・是正した3件**:

1. **構面表示（通り・階）では壁が対象外のままだった。** `viewer_panel` が
   `squid_n_core::frame::build_frame(&app.model, t)` を壁展開前の `app.model` で
   呼んでいた。`build_frame` 自体は壁対応済み（4節点すべてが通り上にあれば含める。
   `docs/12_構面の表示.md` の記述どおり）だが、入力モデルに壁が存在しないため
   `Frame::elem_on`（配列添字ベースのビットマスク）に壁の添字が生まれず、
   `FrameFilter::shows` は範囲外の添字を「描かない」に倒す規約のため、壁の要素は
   構面を選んだ瞬間に黙って全滅していた（全体表示のみで直る「まだら」な修正に
   なっていた）。上記の主要な描画差し替えだけでは検知できない、`viewer_panel`
   自身の別経路にある同型の不具合。
2. **壁展開が1フレームに複数回走っていた。** `viewer_panel` の `display_model` に
   加えて、`draw_check_ratio`・`draw_modeling`・`show_modeling_tooltip`・
   `draw_force_diagram`・`draw_mode_rest_ghost` がそれぞれ独立に
   `wall_expanded_view_model(&app.model)` を呼んでいたため、モードによっては
   壁版を持つモデルで1フレームに2〜3回展開・複製していた（ID割当は決定的で
   結果は一致するため誤表示にはならないが、フレームレート低下の懸念を
   自ら生んでいた）。上記の関数群を `model: &Model` を受け取る形に変更し、
   `viewer_panel` が算出した `display_model` を1箇所から配るよう是正した
   （`draw_force_diagram`→`draw_component` で先に行っていた「1フレーム1回化」を
   `viewer_panel` 全体に広げた形）。

1・2 の是正方針: `viewer_panel` は構面選択（`frame_range_controls`）→ `build_frame`
（構面解決）→ 節点投影（`pts`/`coords3`）→ クリック処理（部材作成ツールが
`app.undo.run(&mut app.model, ...)` で要素を追記しうる）→ 以降の描画、という順で
進む。クリック処理より前の `build_frame`/`pts` は投影・クリック判定に必要なため
`app.model`（クリック前の状態）から作らざるを得ず、ここは壁対応不要（`frame_bbox`・
クリック判定とも実部材のみ見る。実部材のIDは壁展開で増減しないため、この時点の
`app.model` ベースで一貫して正しい）。**クリック処理より後の描画・ホバーだけを
`display_model` から作り直した構面で絞り込む**ことで、投影・クリック判定の
タイミング要件を崩さずに壁を構面対応させた（`filter` をクリック処理の直後で
`display_model` 由来のものへ差し替え。`frame` 自体は `.normal`・構面基準線描画にしか
使わず要素IDを見ないため据え置き）。クリック前の `build_frame` 自体は壁展開しない
（Some/None が要素に依存しないため。構面表示中の二重 `clone` を避ける）。
空構面の注記は `frame_for_draw.elem_count()` を見る（壁だけの通りで
「部材はありません」と出さない）。
3. **クリックで部材を追加した直後の1フレームだけ、壁の構面判定がずれうる
   （残る軽微な指摘、後日利用者の指摘で追加是正）。** 1の是正直後は、
   `build_frame` の呼び出し自体をクリック処理の“前”（`frame_target` の解決と
   同じタイミング）で壁展開して済ませていたため、同じフレーム内でクリックにより
   `app.model` へ要素が1本追加されると、壁の要素ID（`max(既存要素ID)+1` 起点で
   採番）が「クリック前に展開した `frame`」と「クリック後に展開した
   `display_model`」とで1つずれ、その1フレームだけ壁の表示/非表示が誤りうる
   （次フレームで自己修復）。上記のとおりクリック処理の"後"（`display_model` と
   同じタイミング）で構面を作り直すことで解消した。

**残課題**:

- 部材表（`tables/members.rs`）の壁対応。いまも `app.model.elements` を走査する
  ため、生成壁の断面力・諸元は表に出ない。
- 応力図の材軸。壁は `nodes[0]`–`nodes[1]`（囲まれた壁では下辺大梁）に線材図を
  重ねる。モデル化図と同じ `wall_element_geometry` への載せ替えは未着手。
- ホバーピック。`pick_nearest_member` は先頭 2 節点の線分のみ。壁ポリゴン面内
  では壁を一意に拾えず、大梁が勝つ。
- 壁属性編集タブ（`tables/wall_attrs.rs`）の `WallPlate` 対応（`WallAttr`/
  `ElementKind::Wall` を直接編集する現行実装は、壁が生成専用になった今は
  対象が常に空で機能しない。`WallPlate` の開口・断面・三方スリットを編集する
  `squid-n-edit` コマンドの新設が要る）。
- 壁のクリック選択・ヒンジ詳細ウィンドウ・時刻歴詳細ウィンドウの壁対応
  （上記「意図的にスコープ外としたもの」参照）。
- ヒンジ図のマーカー描画の壁対応（壁柱の材端位置を `wall_element_geometry` 等から
  導出する必要がある）。
- 壁の板厚を断面ソリッドとして立体表示する機能自体が未実装。
- ビューアの壁作成モード（`wall_draw_mode`）はいまも `AddMember` で
  `ElementKind::Wall` を `app.model` へ書く。壁版経由と混在すると
  `expand_wall_elements` の二重展開 `debug_assert!` に触れる。
- 取り付き壁版（`Attached`）の入力経路・T字取り付き境界の一般化・
  `squid-n-app` の重複実装解消は変わらず残っている（§5.14・§5.15 参照）。

---

### 5.18 取り付き壁版（`Attached`）の入力経路（2026-08-27、dig 5問）

§9 の残課題「取り付き壁版の入力経路が未整備（型は実装済みだが作る手段がない）」と、
壁側 D20 相当（`wall_region_rebuild` が壁版をどの壁領域にも収まらない場合に
「帰属なし」の件数報告しかしない問題）を、利用者との dig（5問）で確定し実装した。

**決定事項**:

- 対象は ST-Bridge 取り込み（自動変換）と `squid-n-edit` の編集コマンドの両方。
  `squid-n-app` の GUI 配線・MCP ツールは対象外（§5.17 の Step 9 GUI残・
  残課題一覧の床側 MCP 未整備と同じ位置づけのまま据え置く）
- 自動変換の判定は `RegionAnchor::Line` のみ。床側 D20
  （`region_rebuild::try_convert_cantilever`）をそのまま 90 度回転させた判定で、
  **水平な辺（上辺・下辺）のみ**を対象にする。鉛直な辺（柱沿い）は判定対象に
  しない（D13/D14 に「柱だけに取り付く壁」の用例がないため）
- `RegionAnchor::FloorRegion`（自立壁）の自動検出はしない。ST-Bridge の
  `StbWall` に自立壁を判別できる情報源がなく（`OutOfFrameMiscWall` の実データが
  0 件だったのと同じ理由）、常に手入力専用とする

**実装**:

- `squid-n-core::region_rebuild` の `GirderSeg`/`horizontal_girders`/
  `edge_fully_covered`/`delete_unref_nodes` を `pub(crate)` にして
  `wall_region_rebuild` と共有した（床の水平辺判定は壁の水平な辺の判定と
  数学的に同一のため、複製せず再利用した）
- `wall_region_rebuild::rebuild_wall_regions` に `try_convert_wall_attached` を
  追加。どの壁領域にも収まらない `Enclosed` 壁版のうち、水平な辺がちょうど 1 つ
  同一レベルの大梁に全長覆われていれば、その辺を取付き線とする `Attached` へ
  変換する。張り出し量はその辺からの鉛直方向の符号つき高さ差（下向きが負。
  垂れ壁を表現できる）
- **見つけた既存の抜け**: `node_has_structural_ref`（床側 D21 の参照判定）が
  `model.wall_regions`/`model.wall_plates` を一切見ていなかった。壁版
  （`WallPlate`）は床側のリビルドをまたいで存続するため、床側の D21 節点削除が
  壁版の参照する節点を誤って削除しうる欠落だった（`visit_node_ids` 側は
  `wall_plates` を正しく見ており、実害はこの判定関数だけに限定されていた）。
  `wall_plate_refs_node` を新設し、`node_has_structural_ref` へ組み込んで是正した
  （防御的に `wall_regions.boundary` も見るようにした。壁領域は
  `rebuild_wall_regions` のたびに作り直されるため実害はないが、呼び出し順の
  前提が崩れたときの事故を防ぐため）
- 壁版の変換で境界が取付き線 2 点だけに縮んだぶん、参照が 0 になった節点は
  床側と同じ規約（D21）で削除する（`WallRegionRebuildReport::deleted_nodes` を新設）
- `squid-n-edit::wall_misc` に `AddAttachedWallPlate`/`SetAttachedWallPlateExtent`/
  `SetAttachedWallPlateAnchor`/`DeleteWallPlate`/`InsertWallPlate` を新設した
  （床側の `AddAttachedSlab`/`SetAttachedExtent`/`SetAttachedAnchor`/`DeleteSlab`/
  `InsertSlab` と対称。`WallPlate` の Add/Delete が一切なかったため、削除・挿入も
  合わせて用意した）。取付き先の検証（`wall_anchor_ok`）は `Line`/`FloorRegion` の
  みを認め、壁の取付き先としては使わない `Point` を弾く
- `crate::refs::floor_region_exists` を新設（`FloorRegion` アンカーの参照検証に使う）

**V&V**: `squid-n-core`（新規単体テスト 3 件）・`squid-n-edit`（新規単体テスト 4 件）・
`squid-n-io`（ST-Bridge 取り込みの end-to-end テスト 1 件）を追加し、全体
（`cargo test --workspace`）・clippy（`--all-targets -D warnings`）・fmt を実行済み。

**残課題**: 取り付き壁版の `squid-n-app` GUI 配線・MCP ツールは §5.17・残課題一覧の
とおり未着手のまま。T字取り付き境界の一般化（項目2）・
`wall_region_rebuild` の D20 相当を「囲まれた壁版から取り付き版への変換」だけでなく
「取り込み時点で最初から自立壁として作る」経路（MCP・GUI）も引き続き残る。
取り付く壁版の自重を取付き線へ分配する経路は未実装（§5.22）。

---

### 5.19 T字取り付き境界（5節点以上）の一般化を見送り、判定を集約（2026-08-27）

§9 の残課題「T字取り付き境界（5節点以上、実測3.6%）を按分等で一般化し、耐震壁として
生成する経路」について、利用者との対話で方針を確定した。

**発端**: §5.14 の Q6=C（境界が4節点でない壁版は要素を生成しない）に対して、
利用者から「壁エレメントは4節点を前提としたモデル化だと認識している。一般化は
本当に可能なのか」という指摘があった。

**調査**: 「実測55件中2件・3.6%」の根拠を再現しようとしたが、この数値に対応する
再現可能なフィクスチャ・テストがリポジトリに存在しないことが判明した
（`crates/squid-n-app/tests/fixtures/model.stb` は壁版を1枚も持たない。「55件」は
実際には `region_gen_finds_wall_bounded_regions` が検出する**壁領域（構面）の境界数**
であり、壁版の枚数の実測ではなかった）。そのため、非4節点の壁版が実際どのような
輪郭（T字取り付きによる共線の余分な節点なのか、真に5角形以上の非矩形輪郭なのか）を
持つのか、今のリポジトリのデータからは検証できない。

**決定**: **一般化しない（Q6=C を維持）。** 壁エレメントは壁柱＋剛梁変換による
4節点24自由度モデル（`docs/calc_basis/04_要素剛性/05_壁エレメントモデル.md`）であり、
下辺2節点・上辺2節点を前提とした定式化そのものを、任意の多角形へ拡張することは
できない。実データの裏付けがないまま「按分」等で無理に4節点へ落とし込むと、
根拠不明な近似を耐力評価へ持ち込むことになるため、これを避けた。実際に非4節点の
壁を持つ利用者のモデルが見つかった時点で、その具体的な輪郭を見てから設計する
（一般化するとしても、それは「壁エレメントの定式化を拡張する」ことではなく、
「境界に含まれる余分な節点（T字取り付きの接続点等）を除いた真の4隅を選び直す」
か、「輪郭分割で複数の4節点壁版に分ける」かのいずれかになる見込み）。

**実装**: 一般化は見送ったが、「境界が4節点か」の判定が次の3箇所に重複して
実装されていたため、`squid_n_core::model::WallPlate::has_quad_boundary` へ集約した。

- `squid_n_load::wall_expand::expand_wall_elements`（解析要素の生成対象判定）
- `squid-n-solver::statics::analysis::precheck::model_issues`（解析前診断の警告）
- `squid-n-io::stbridge::import::assemble::build_walls`（ST-Bridge 取り込みの警告）

ST-Bridge 取り込みだけは「断面未割当」と「非4節点」を独立にカウントする既存の
挙動（1枚の壁版が両方に該当すれば両方の警告件数に計上される）を変えていない。
一方 `wall_expand`/`precheck` は「断面未割当を先に判定し、該当すれば非4節点の
判定はスキップする」という排他的な優先順位を従来どおり保っている（挙動は変えず、
判定式だけを共有関数へ差し替えた）。

**V&V**: `WallPlate::has_quad_boundary` の単体テストを新設（4節点・5節点・3節点・
`Attached` の4パターン）。`cargo test --workspace`・clippy（workspace/gui/mcp）・fmt を
すべて実行し、既存の回帰テストが全て通ることを確認した（挙動を変えない集約のため）。

---

### 5.20 敵対的レビューで発覚した2件の是正（2026-08-27、レビュースキル不使用）

§5.18・§5.19 の実装を、コードレビュー用スキルを使わない敵対的レビュー（利用者の
指示）で検証したところ、`Model::visit_node_ids` が `wall_regions` を走査していない
ことに起因する回帰が2件見つかった。いずれも§5.18で`rebuild_wall_regions`へ
新設した「取り付く壁版への変換で参照0になった節点を削除する」処理（D21）が、
初めて`wall_regions`の存在下でこの種の節点削除・ID繰り上げを引き起こす経路に
なったことで顕在化した（あるいは、以前から潜在していた）。

1. **`rebuild_wall_regions`自身の中で壁領域の境界がダングリング参照へ壊れる
   （重大）**: `rebuild_wall_regions`は「壁領域を検出して`model.wall_regions`へ
   書き込む」→「取り付く壁版への変換で参照0になった節点を`delete_unref_nodes`で
   削除する」の順に進む。`delete_unref_nodes`は内部で節点を圧縮し
   `Model::visit_node_ids`で全`NodeId`参照を新しい添字へ張り替えるが、
   **`visit_node_ids`は`wall_regions[].boundary`を一切走査していなかった**。
   そのため、削除される節点より低い添字の節点が1つでもあると、圧縮後の
   `wall_regions[].boundary`は古い（張り替え前の）添字のまま取り残され、
   存在しない節点を指す（ダングリング）か、圧縮でずれた別の節点を指す
   （サイレントな取り違え）状態になる。再現テストでは実際にpanic
   （`index out of bounds`）を確認した。`Model::validate()`はこれを検出しない
   （添字自体は範囲内に収まりうるため、構造的な妥当性検証だけでは壊れた参照を
   見分けられない）。
   **是正**: `Model::visit_node_ids`に`wall_regions`の走査を追加した
   （`crates/squid-n-core/src/model/aggregate.rs`）。
   **V&V**: 低インデックスの節点（パラペットの自由端）が削除・圧縮される一方、
   高インデックスの節点（既存の壁領域の4隅）を持つモデルで、圧縮の前後で
   壁領域の境界座標が保たれることを固定するテストを新設した
   （`wall_region_rebuild::tests::test_wall_region_boundary_survives_node_compaction_from_attached_conversion`）。
   修正前にこのテストが実際に失敗する（境界が破損する）ことを確認してから修正した。

2. **`squid-n-edit::DeleteNode`が壁領域だけが参照する節点を削除できてしまう
   （同種・軽微）**: `Model::node_in_use`（`DeleteNode`の削除ガード）が内部で呼ぶ
   `node_referenced_outside_elements`も、同じ理由で`wall_regions`を見ていなかった。
   壁領域の境界節点は通常は柱・梁の要素ノードと一致するため、`elements`側の判定で
   既に保護されるのが通例で、実運用モデルでは顕在化しにくい（項目1ほど重大では
   ない）。それでも、壁領域だけが参照する節点を作れる限り、同じ種類の破損を
   招きうるため、防御的に修正した。
   **是正**: `node_referenced_outside_elements`に`wall_regions`のチェックを追加した。
   **V&V**: `squid-n-edit::tests::test_delete_node_used_by_wall_region_boundary_is_noop`
   を新設し、修正前に実際に失敗する（節点が削除されてしまう）ことを確認してから
   修正した。

**根本原因の考察**: 両者とも、`WallRegion`が壁側Step 7+8（§3.2・§5.9）で
`Model`へ追加された際、`NodeId`を持つ既存の横断的な仕組み（`visit_node_ids`・
`node_referenced_outside_elements`）への追随が漏れていたことが原因である。
`region_rebuild::node_has_structural_ref`（§5.18で同様の抜けを是正済み）と
合わせて、**`NodeId`を持つフィールドを追加したら追随すべき箇所が実質4箇所ある**
（`visit_node_ids`・`node_referenced_outside_elements`・
`region_rebuild::node_has_structural_ref`・`Model::validate`のダングリング検査）。
`visit_node_ids`の doc コメントには追随を促す注記が既にあったが、機械的な強制力
（コンパイルエラー等）がないため見落としが起きた。横断的な仕組みが増えるたびに
この種の見落としリスクが増える点は、根本的な解決（例えば単一の「NodeId を持つ
全フィールドを列挙する」central registryへの統合）を将来検討する価値がある
（今回のスコープでは見送り。残課題）。

**V&V（全体）**: `cargo test --workspace`・clippy（workspace/gui/mcp、
`--all-targets -D warnings`）・fmt をすべて実行し、既存の回帰テストを含め全て
通過することを確認した。

---

### 5.21 §5.20の根本原因を是正（2026-08-27、利用者の指示）

§5.20で「`NodeId`を持つフィールドを追加したときに追随すべき箇所が3箇所
（`Model::visit_node_ids`・`Model::node_referenced_outside_elements`・
`region_rebuild::node_has_structural_ref`）に分散しており、機械的に強制されない」
という根本原因を記録した。利用者の指示でこれ自体の是正に着手した。

**方針**: `visit_node_ids`（書き換え用）は残す（節点の削除・挿入に伴う ID
繰り上げ／繰り下げに使う、可変参照が要る処理のため、読み取り専用の判定とは
本質的に異なる走査であり、無理に1つの関数へ統合すると却って複雑になる）。
一方、`node_referenced_outside_elements`と`node_has_structural_ref`は、
「節点`id`がある種の実体（床領域・床板・壁領域・壁版・二次部材・拘束・節点荷重）
から参照されているか」という**同じ問い**を、2箇所で独立に走査していただけである
（実際、`region_rebuild.rs`は`slab_refs_node`/`wall_plate_refs_node`という
専用のヘルパーまで別途持っており、`model/aggregate.rs`の`slab_node_refs`/
`wall_plate_node_refs`と実質重複していた）。この共通部分を
`Model::node_referenced_by_regions_or_plates`という1つの`pub(crate)`関数へ
集約し、両呼び出し元はそれぞれの追加分（前者は`stories`、後者は節点自身の
支点・質量・剛床マスター）だけを追加で見るようにした。

**実装**:
- `crates/squid-n-core/src/model/aggregate.rs`: `node_referenced_by_regions_or_plates`
  を新設。`node_referenced_outside_elements`はこれ＋`stories`チェックへ簡潔化
- `crates/squid-n-core/src/region_rebuild.rs`: `node_has_structural_ref`が
  `model.node_referenced_by_regions_or_plates(id)`を呼ぶよう書き換え、重複していた
  `slab_refs_node`/`wall_plate_refs_node`を削除（未使用になった`Constraint`・
  `WallPlate`・`WallPlateShape`のインポートも削除）

**これで解決したこと・解決していないこと**: 「同じ実体集合を2箇所で判定する」
重複は解消され、床領域・床板・壁領域・壁版・二次部材・拘束・節点荷重に関する限り、
今後フィールドを追加してもどちらかだけ更新すれば足りる（更新箇所が1箇所に減った）。
**ただし`visit_node_ids`との整合は今回も機械的に強制されるわけではない**
（`visit_node_ids`は書き換え用で性質が異なるため統合していない。Rust には
「`Model`のどのフィールドが`NodeId`を持つか」を型レベルで列挙し全消費箇所を
コンパイルエラーで突き合わせる仕組み（リフレクション相当）がなく、
`#[derive(...)]`マクロで独自に用意しない限り完全な機械的強制は難しい）。
この残る差分を検出できるよう、`node_referenced_by_regions_or_plates`が対象とする
全フィールド種別（節点荷重・床領域境界・床領域小梁支持・床板Enclosed・
床板Attached・壁領域境界・壁版Enclosed・壁版Attached・二次部材・拘束）を
1つずつ参照させた節点で判定が真になることを固定する網羅テストを新設した
（`model::aggregate::node_reference_tests::test_node_referenced_by_regions_or_plates_covers_every_field_kind`）。
このテストは、意図的に`wall_regions`のチェックを外して実際に失敗することを
確認してから正しい状態へ戻している。今後同種のフィールドが追加された際、
このテストへ1行足し忘れると（他のフィールドと合わせて）検出できるが、
新設フィールドの追加自体を強制する仕組みではない（対症療法であり、
完全な根本解決＝コンパイル時の網羅性チェックは、今回のスコープでは
見送り。将来 derive マクロ等で実現する余地は残る）。

**V&V**: `cargo test --workspace`・clippy（workspace/gui/mcp、`--all-targets
-D warnings`）・fmt をすべて実行し、既存の回帰テスト（§5.20 で追加した
`test_wall_region_boundary_survives_node_compaction_from_attached_conversion`・
`test_delete_node_used_by_wall_region_boundary_is_noop` を含む）が全て通過する
ことを確認した（挙動を変えない集約のため）。

---

### 5.22 PR #229 の敵対的レビュー（2026-08-27）

§5.18〜§5.21 を再レビューし、次を是正した。

**誤変換**: `try_convert_wall_attached` が自由端を「取付き辺と同じ高さの頂点」で除外していた。
床側 D20 は辺上距離（`point_segment_dist`）で除外するため、梁レベルに折れ曲がった頂点を
持つ輪郭を誤って変換しうる。壁側も取付き辺上（高さ一致かつ XY が辺上 1 mm 以内）だけを
除外するよう揃えた。垂れ壁（負の張り出し）と折れ曲がり輪郭の回帰テストを追加した。

**ドキュメントの虚偽**: 利用者向け ST-Bridge 節が、取り付く壁版の「荷重の出口は取付き線への分布」
と床側の文言を写していた。実装は `expand_wall_elements` が `Attached` を生成せず、
床の `distribute_attached` に相当する壁側経路もない。地震用重量・DL・数量にも入らない。
計算根拠・準備計算・ST-Bridge 変換表・診断項目を現状どおりに直し、診断へ取り付く壁版の警告を足した。

**その他**: `Model::eq_ignoring_dofmap` が `wall_plates` を比較していなかった抜けを直した。
網羅テストへ `Attached`／`FloorRegion`（自立壁）の節点参照を足した。

**残課題**: 取り付く壁版の自重を取付き線へ分配する経路は未実装（床の取り付く床板との非対称）。
`OutOfFrameMiscWall` 吸収の一環として別途行う。層内の腰壁・垂れ壁は壁領域へ帰属した
`Enclosed` のまま（フレーム内雑壁の既存経路）であり、自動変換の対象ではない。

---

### 5.23 取り付く壁版の自重配分を実装（2026-08-27、dig 3問）

§5.22 の残課題（取り付く壁版の自重が地震用重量・DL・数量拾いのどこにも入らない）を解消した。

**設計判断（dig）**:

- **Q1**: 残課題13項目のうち本件を最優先とした。`Model::wall_plate_self_weight`
  （開口控除後の正味面積×板厚×材料密度）はすでに実装済みで呼び出し元がなく、
  「重量の算定式」ではなく「配分」だけが欠けていたため、他の残課題より安全性への
  影響が明確でスコープも1セッションで閉じる大きさと判断した。
- **Q2**: 「線」アンカー（`LoadTransfer::Anchor`、梁への分布荷重）は、床の取り付く床板が
  使う幾何解決パイプライン（`squid-n-job::auto_loads::slab_load_case_content` の
  `LoadTarget::Span`）にそのまま合流させ、`span`（部分区間）を梁全長へ薄めず正確に扱う
  方式を採った。危険側の簡略化（梁全長へ薄めて希釈する近似）は選ばなかった
  （利用者の指摘により `AGENTS.md`「実装方針」へ「安全側か危険側かをまず判定する」旨を追記）。
- **Q3**: 数量拾い（`compute_quantity_takeoff`）も今回まとめて直した。

**実装**: `squid_n_load::wall_attached`（新規モジュール）が配分を担う。

- `attached_wall_beam_loads` — 「線」アンカーの壁版を `BeamLoad` へ変換する。
  `LoadTransfer::Anchor` は取付き線の区間 `span` への分布荷重（`LoadTarget::Span`。
  矩形は等分布、台形は張り出し高さに比例する `LoadShape::Linear`）、
  `LoadTransfer::Columns` は合力作用点（矩形は区間中点、台形は面積重心）による
  単純梁反力按分で取付き線両端の柱へ（`LoadTarget::Node`）。
  `squid-n-job::auto_loads::compute_gravity_auto_load_cases` が
  `dl_nodal`/`dl_member` へ合流させる。CMQ 図のソース（`compute_dl_beam_loads`）
  には線アンカーをまだ載せていない（意図した設計ではない。CMQ 図の改修として
  [`CMQ図を荷重ケースの全荷重へ_申し送り.md`](CMQ図を荷重ケースの全荷重へ_申し送り.md)・§5.26）。
- `floor_region_wall_extra_intensity` — 「床領域」アンカー（自立壁、D17）の壁版の自重を、
  所属する床領域の**全床板へ同一の追加面荷重強度**（総重量÷床板合計面積）として上乗せする。
  面積は床の分配と同じ XY 投影（`floor::polygon_area`）を使う。3 次元面積で割ると分配側が
  XY 面積に強度を掛けるため総重量が縮小し、傾斜床では危険側になる。XY 面積が 0 の床板
  （鉛直に近い版）はならす先がないため、取付き線の両端節点へフォールバックする。
  `squid-n-job::auto_loads::compute_dl_beam_loads` の `w_of` へ組み込むことで、床の
  スラブ分配（CMQ図用の一覧を含む）にもそのまま反映される。床板を1枚も持たない
  床領域（版なし領域）では等価面荷重へならす先がないため、総重量を保存する目的で
  取付き線の両端節点への集中荷重（矩形は半分ずつ、台形は面積重心比）にフォールバックする。床領域内で床板ごとに
  面荷重強度が異なる場合の按分は行わない（§8.1 の残課題と同じ性質の近似）。
- `accumulate_attached_wall_seismic_weight` — DL ケースが無く密度から地震用重量を直接
  算入する経路（`include_density_self_weight = true`）でも、取り付く壁版の自重が階重量へ
  入るようにする。標準構成（DL 同期後に `include_density = false`）では呼ばない
  （二重計上防止。フレーム外雑壁と同じ）。

数量拾い（`squid-n-design-jp::quantity`）には `attached_wall_plate_quantity` を新設し、
`WallPlate` から直接（解析要素を介さず）算定する。カテゴリは `OutOfFrameMiscWall` の
後継という位置づけに合わせ `MemberCategory::MiscWall` を流用した。

**残課題**: 床領域内で床板ごとに面荷重強度が異なる場合の按分は総和保存のみ（位置は無視）。
取り付く壁版の台形（`extent` 非対称）の位置は §5.24 で高さ比例の線形分布・面積重心按分へ直した。
床側 `distribute_attached` の台形 footprint は区間中点近似のまま。

### 5.24 台形の取り付く壁版の位置を面積重心へ揃える（2026-08-27）

§5.23 の既知近似（台形でも等分布・区間中点）をやめた。厚さ・密度が一様なら線荷重強度は
張り出し高さに比例し、柱按分・密度経路の節点集中は同じ台形の面積重心

\\( s = (h_i + 2 h_j) / (3(h_i + h_j)) \\)

に置く。`LoadShape::Linear { w_i, w_j }` を追加し、`slab_load_case_content` が
`MemberLoadKind::Distributed` の線形区間として DL へ載せる。開口は総重量のスケールに
だけ効き、開口位置は見ない。床の取り付く床板（跳ね出し長さが両端で異なる footprint）は
対象外（`distribute_attached` の既知近似のまま）。

### 5.25 台形壁位置の敵対的レビュー（2026-08-27）

§5.24 を危険側・総和保存・経路間の食い違いで見直した。

**総和・形状**: 鉛直な台形（水平な取付き線、鉛直押し出し）では、線荷重強度が
張り出し高さに比例し、`∫w dx = W` は Newell 面積の自重と一致する。柱按分・密度経路の
重心 `s = (h_i+2h_j)/(3(h_i+h_j))` は同じ台形の面積重心であり、DL の `Distributed`
を地震用重量へ戻す `static_reactions` とも一致する。`slab_load_case_content` 経由の
結合テスト（強度比 `w_i:w_j = h_i:h_j` と総和）を追加した。

**残る近似（仕様）**:
- 開口は総重量のスケールにだけ効く。開口が片側に寄っていると、残コンクリートの
  真の重心からずれる（高い側に開口があると、高い側へ過大に載せる）。
- 床の取り付く床板の台形 footprint、および自立壁を床へならす経路は、壁の台形位置を見ない。

線アンカー分布が CMQ 図に出ないことは仕様ではなく残課題（§5.26）。

### 5.26 CMQ 図は荷重ケースの全荷重を描くべき（2026-08-27）

取り付く壁版の線アンカーを `compute_dl_beam_loads`（CMQ 図ソース）に載せなかったのは、
囲まれた壁自重と同じ経路にした実装上の都合であり、意図した設計ではない。
CMQ 図は床分配の検証図として先に配線されており、表示中の荷重ケースの部材荷重全体
（床分配・梁自重・取り付く壁版・手入力）を重ねていない。改修は
[`CMQ図を荷重ケースの全荷重へ_申し送り.md`](CMQ図を荷重ケースの全荷重へ_申し送り.md) に切り出した。

**2026-08-27 追記（実装済み）**: CMQ 図のソースを表示中荷重ケース（`nav.focus_load_case`）の
`member` へ付け替えた。dig により、強軸(ey)・弱軸(ez)の面選択（応力図の
`ForceComponent::plane` と同じ区別）も追加している。詳細は
[`CMQ図を荷重ケースの全荷重へ_申し送り.md`](CMQ図を荷重ケースの全荷重へ_申し送り.md) 参照。

---

### 5.27 壁版の編集 UI（旧「壁属性」タブの作り替え、2026-08-28、dig 5問）

§5.17・§5.18 の残課題「壁属性編集タブ（`tables/wall_attrs.rs`）の `WallPlate` 対応」と
「取り付く壁版の GUI 配線が未着手」を、利用者との dig（5問）で範囲を確定して実装した。

**何が壊れていたか**: §5.14 で壁が生成専用（D5）になったとき、モデルタブ「壁属性」は
`app.model.elements` から `ElementKind::Wall`/`Shell` を集めて `WallAttr` を編集する実装の
まま残された。壁要素は `Model.elements` に存在しなくなったため、このタブは
**通常の経路（ST-Bridge 取り込み）では対象が常に空**（「壁要素(Wall/Shell)がありません」
だけを表示する）になっていた。唯一の例外はビューアの壁作成モード（`wall_draw_mode`。
`AddMember` で `ElementKind::Wall` を直接書く §5.17 の残課題）だが、この経路自体が
`expand_wall_elements` の二重展開 `debug_assert!` に触れる壊れた経路である。
なお `ElementKind::Shell` を生成する production コードは存在しない（取り込みにも
生成コードにも無く、テストの合成データのみ）ため、対象から外しても取りこぼしは無い。
`squid-n-edit` 側にも `WallPlate` の断面・開口・三方スリットを編集するコマンドは無く、
`AddAttachedWallPlate` 等（§5.18）には GUI からの呼び出し口が無かった。つまり
**ST-Bridge を取り込んだ後、利用者が壁の仕様を変更する手段は GUI にもコマンドにも
存在しない**状態だった（開口を入れる、板厚〔断面〕を変える、垂れ壁を足す、のいずれも不可）。

**dig で確定した設計**:

- **Q1**: 今回は本件（入力経路の欠落）に着手する。応力図の壁柱幾何・ホバーの多角形ピック・
  部材表の壁対応・ヒンジ図マーカーはいずれも表示改善で計算結果を変えないのに対し、
  本件だけは入力経路そのものが塞がっているため。
- **Q2**: 囲まれた壁版（`Enclosed`）と取り付く壁版（`Attached`）の両方を対象にする。
  `Attached` は作成時（`AddAttachedWallPlate`）にしか値を与えられず、事後編集手段が
  無いという同型の欠落だったため、同じ一覧・同じコマンド体系でまとめて塞ぐ。
- **Q3**: 断面は一覧セルで即時反映（`SetWallPlateSection`。床板の `SetSlabSection` と同じ流儀）。
  開口（`opening_area`・`openings`・`opening_weight`）と三方スリットは
  「対象選択 → フォーム → 適用」で 1 コマンド（`SetWallPlateAttrs`）にまとめる。
  `openings` は自由入力の複数行パースで入力途中に不正な状態を経由するため、
  既存のフォーム＋検証の流れをそのまま活かす。また `openings` が非空のとき
  `opening_area` が無視されるという相互依存があり、別コマンドに割ると undo の途中に
  「個別開口だけ戻って面積が残る」という利用者が一度も入力していない組み合わせが現れる。
- **Q4**: **三方スリットは取り付く壁版では入力欄自体を出さない**（当初案の「無効化して
  ホバーで理由を出す」から、利用者の指摘で変更）。三方スリットは「自重を上下に分けず
  頂部へ寄せる」指定だが、取り付く壁版（腰壁・垂れ壁・パラペット・自立壁）には
  分ける相手方の下端がそもそも無く、`squid_n_load::wall_attached` もこのフィールドを
  読まない。`OutOfFrameMiscWall`（雑壁）がこのフィールド自体を持たなかったのと
  同じ扱いにする。一覧の当該列も `Attached` では「―」（淡色＋理由のホバー）にする。
  データ型としては `WallPlate` が形によらず同じフィールドを持つ設計（D3）を崩さないため、
  `SetWallPlateAttrs` 自身は形を問わず値を保存する（GUI が見せないだけ）。
  フォームの「適用」は、取り付く壁版に対しては既存値をそのまま書き戻す。
- **Q5**: 取付き先（`anchor`）・立ち上がり高さ（`extent`）の編集と、取り付く壁版の
  新規追加フォームも今回まとめて配線する（当初は別課題への先送りを推奨したが、
  利用者の指示で一括対応とした）。

**実装**:

- `squid-n-edit`（`wall_misc.rs`）に 2 コマンドを新設した。
  - `SetWallPlateSection` — 壁版の断面割当。実在しない断面・存在しない `WallPlateId` は
    Noop（`SetSlabSection` と同じ参照検証の規約）。
  - `SetWallPlateAttrs` — 開口面積・開口部重量・個別開口・三方スリットの一括変更。
- **`SetWallAttr`/`RemoveWallAttr` を削除した。** 唯一の呼び出し元が旧「壁属性」タブで、
  そこを作り替えると production の呼び出し元が 0 になる。加えてこの 2 つは
  `Model.wall_attrs`（いまや壁展開モデルが合成する内部表現であり、入力の正ではない）へ
  利用者が値を書き込め、保存もされてしまう経路だった。`WallAttr` 型そのものと
  `Model.wall_attrs` フィールドは、壁展開の合成先として引き続き残る（真の廃止は §6 の残課題）。
- `squid-n-app`: `tables/wall_attrs.rs` を `tables/wall_plates.rs` へ作り替えた
  （`ModelTab::WallAttrs` → `ModelTab::WallPlates`、タブ名「壁属性」→「壁版」、
  `WallAttrDraft` → `WallPlateDraft`）。用語集の「壁版」に名前を合わせるための改名で、
  `ModelTab` は永続化されないため移行処理は不要。構成は次の 3 段。
  1. **一覧**（`standard_table`）: ID・種別（囲まれた／取り付く）・所属壁領域（`wall_plate_ids`
     からの逆引き）・境界／取付き先・断面（即時反映のコンボ）・面積 [m²]・開口・
     三方スリット・削除。取り付く壁版の行では取付き先（線なら節点 2 つ＋区間＋荷重の出口、
     床領域なら床領域＋壁自身の始終点）と立ち上がり高さをセル内で即時編集する
     （床板の `slabs::attached_boundary_cell` と同じ流儀）。
  2. **開口・三方スリットのフォーム**: 対象を選び、`opening_area`/`opening_weight`/
     個別開口（既存の `parse_openings`/`format_openings` をそのまま流用）/三方スリットを
     入力して「適用」。
  3. **取り付く壁版の追加フォーム**: 線アンカー（大梁・柱頭）と床領域アンカー（自立壁）の
     両方に対応する。開口は追加後に 2. で与える（床板の追加フォームが版の仕様を後から
     与えるのと同じ流儀）。
- 削除（`DeleteWallPlate`）は後続の壁版 ID を 1 つ繰り上げるため、削除を実行したフレームで
  フォームの編集対象を外す。対象が消えている場合も毎フレーム検出して外す
  （繰り上げによって「別の壁版を編集していた」状態を作らないため）。

**docs**: `docs/calc_basis/04_要素剛性/05_壁エレメントモデル.md` に「壁版の入力」節を追加し、
入力場所と既定値（断面未割当・開口面積 0・個別開口なし・開口部重量 0・三方スリットなし）、
個別開口が開口面積に優先すること、三方スリットが取り付く壁版にはないことを明記した。

**検証**: `squid-n-edit` に新コマンドの往復・Noop テストを 3 件追加
（`test_set_wall_plate_attrs_roundtrip`、`test_set_wall_plate_attrs_missing_id_is_noop`、
`test_set_wall_plate_section_roundtrip_and_rejects_dangling`）。既存の `parse_openings`/
`format_openings` のテスト（9 件）は新モジュールへそのまま移した。GUI 描画そのもの
（`egui::Painter`/`Ui` 依存）は既存方針どおり単体テストの対象にしていない。
`cargo test --workspace --locked`・`cargo clippy --workspace --all-targets --locked
-- -D warnings`・`cargo clippy -p squid-n-app --all-targets --features gui --locked
-- -D warnings`・`cargo fmt --all -- --check` を通過した。

**残課題**:

- ~~囲まれた壁版（`Enclosed`）を GUI から新規に作る手段がない（ST-Bridge 取り込みのみ）。
  境界 4 節点を選ばせる `AddEnclosedWallPlate` 相当のコマンドが要る。~~ **§5.30 で実装済み**
- 壁版を編集する MCP ツールは未整備（床板と同じ位置づけの残課題）。
- ビューアの壁作成モード（`wall_draw_mode`）はいまも `AddMember` で `ElementKind::Wall` を
  `app.model` へ書く（§5.17 の残課題のまま）。壁版経由と混在すると
  `expand_wall_elements` の二重展開 `debug_assert!` に触れる。今回のタブは
  `Model.wall_plates` しか見ないため、この経路で作った壁は一覧に出ない。
- 一覧の「面積」は境界多角形の生の面積（ニューエルの公式）で、自重算定が耐震壁に適用する
  周辺柱梁の内法による低減（`wall_clear_area_factor`）を含まない（列のホバーに明記した）。
  §9 の「`WallPlate::area` に低減をどこで持たせるか未決」と同じ論点。
- **自立壁の `RegionAnchor::FloorRegion { region }` が床領域の付け直しで追随しない**
  （既存の設計上の穴。§5.27 の敵対的レビューで発見。詳細と対処案は §9 の該当行）。
- 取付き先・立ち上がり高さの `DragValue` はドラッグ中フレームごとにコマンドを発行するため、
  1 回のドラッグが多数の undo エントリになる（床板タブの取付き先セルと同じ既存挙動）。

### 5.28 自立壁の床領域参照を廃し、床領域をまたぐ壁を内部分割する（2026-08-28、dig 9問）

§5.27 の敵対的レビューで見つけた「自立壁の `RegionAnchor::FloorRegion { region }` が
`rebuild_floor_regions` の ID 振り直しに追随しない」問題を、dig（9問）で方針を確定して是正した。
**§5.18/§5.23 で入った穴であり §5.27 の実装が作ったものではないが、§5.27 で GUI から
自立壁を作れるようにしたことで初めて利用者の手が届くようになった**ため、続けて対処した。

**問題**: `rebuild_floor_regions` は毎回スキャン順で `FloorRegionId` を振り直す
（`r.id = FloorRegionId(i)`）。旧領域との対応付けは名前と小梁ラインの引き継ぎにしか
使っておらず、`model.wall_plates` のアンカーは remap されない。モデルの位相が変わって
スキャン順がずれると、自立壁の自重が**別の階の床板へならされる**（総重量は保存されるが
階の配分が狂い、失った側の階は危険側になる）。

**さらに深掘りで見つけた、より重大な問題**: 分配先が見つからないときの
フォールバック（取付き線両端の節点への集中荷重）が**危険側**だった。自立壁の両端は
どの部材にも接続されない自由節点でありうる。`DofMap::build` は非構造節点への節点荷重を
無視する（同関数のコメントに明記。「荷重は同期側で主架構へ変換する規約」）ため、
**地震用重量には算入されるのに長期 DL の応力解析からは黙って消え、梁がその重量を
負担しないまま設計される**。しかも `precheck` の孤立節点チェックは壁版の取付き先節点を
意図的に除外しているため警告も出ない。2 つの経路が食い違うので階重量の合計を見ても
気づけない。地震用重量側は `story_gen` が全節点を対象にするため影響を受けない。

**dig で確定した設計**:

- **Q1・Q4**: **`region` フィールドを廃する**。`RegionAnchor::FloorRegion { region, nodes }`
  → `{ nodes }`。Q1 の選択肢には「`rebuild_floor_regions` に ID remap を足す」もあったが、
  同じ rebuild の中で床板も小梁も幾何から都度決め直している（D7）以上、自立壁だけ ID を
  保存して追随させるのは一貫性を欠き、`visit_*_ids` 系の追随漏れ（§5.20・§5.21 で 2 度
  起きた事故）の余地を残す。また remap は §8.4 で「弱い」と自認済みの重心キーに荷重の
  帰属先を委ねることになる。保存しなければ陳腐化は構造的に起こりえない。
- **Q2・Q3**: 床板（最小面積）と小梁（ちょうど 1 つ）で内包判定の規則が食い違っている
  ことが判明したが、**実フィクスチャで測ると床板 82 枚・小梁 56 本すべてがちょうど 1 つの
  床領域に入り、両規則の差は 1 件も現れない**（差が出るのは非連結の内側閉路という
  病的な入力だけ。面走査は平面グラフの最小面を返すため入れ子は原理的に起きず、
  例外は `region_gen/floor.rs` の doc が挙げる「穴を持つ面」だけ）。規則の統一は
  別作業として残し、今回の自立壁は最も単純な `find`（同一レベルかつ内包する最初の
  床領域）を使う。
- **Q8**: **床領域をまたぐ壁は境界で内部分割する**（利用者の指摘。「またぐときは内部的に
  分割すればよい」）。分割方式は 0.5 m サンプリング（`misc_wall.rs` の先例）ではなく
  **厳密クリップ**とした。今回の目的がまさに「またぐ壁を正しく分ける」ことなので、
  境界と分割点が一致しない方式は本末転倒。床領域は L 形など非凸になりうるため、
  線分と全候補多角形の交点で区間へ分け、各区間の中点の内包で帰属を決める。
- **Q9**: **不変条件を「自立壁は、床板を持つ床領域の上にのみ存在する」と立てる**
  （利用者の指摘。「力を流す床板がないのに壁が作れてしまうこと自体がおかしい」）。
  フォールバックは削除し、覆われない部分が残る自立壁は**解析前チェックのエラー**にする。
  検査は `model_issues` の**1 か所だけ**に置く。床領域の作り直しと `model_issues` は
  解析のたびに必ずこの順で走る（`sync_auto_load_cases_action` → `rebuild_floor_regions`、
  その後 `run_diagnostics`／`precheck_model`）ため、作成時チェックは正しさに寄与せず、
  幾何判定を 2 か所に持つ重複（§5.21 の教訓）だけを生む。既存の浮き床板
  （`floating_slab_count`）と同じ「作らせはするが検査で捕まえる」流儀に揃えた。
  重大度は、浮き床板が荷重を失わない（`slab_beam_loads_with` の第 2 ループが拾う）ため
  警告なのに対し、**自立壁は重量の行き先が無いためエラー**とする。

**実装**:

- `squid-n-core`: `Model::self_standing_wall_coverage` を新設
  （`model/wall_plate.rs`）。壁の下端線分を、同レベルかつ床板の XY 投影面積が正である
  床領域の多角形で厳密クリップし、区間ごとの台形面積比を返す
  （`SelfStandingWallCoverage`。`per_region` と `uncovered`）。開口は総重量のスケールに
  だけ効く（§5.25 の既知の近似と同じ）。判定はここ 1 か所に置き、荷重分配と診断が共有する。
- `squid-n-load`: `floor_region_wall_extra_intensity` の戻り値から
  フォールバックの `Vec<BeamLoad>` を削除し、`HashMap<SlabId, f64>` だけにした。
- `squid-n-solver`: `model_issues` に「荷重を流せる床の上に載っていない自立壁」を
  Error として追加した。
- `squid-n-edit`: `wall_anchor_ok` の `floor_region_exists` 検証を削除
  （検証すべき参照が無くなった）。使われなくなった `refs::floor_region_exists` も削除。
- `squid-n-app`: 一覧セルと追加フォームから床領域のコンボを削除した。

**§5.27 で書いた指摘の訂正**: 「`Model::validate` もこの参照を検査していない」と
書いたが**誤り**だった。`validate` は `WallPlate -> FloorRegion` のダングリング参照を
検査していた（`wall_plate_node_refs` が節点しか返さないのを見て早合点した）。ただし
検査していたのは「その ID が実在するか」だけで、**振り直しで別の実在する床領域を
指すようになるケースは素通り**するため、指摘の本体（remap されない）は変わらない。
フィールドの廃止に伴い、この検査は不要になったので削除した。

**用語**: 用語集が「床領域／区画」と 2 つの呼び名を併記していたのを「床領域」へ一本化し、
「区画」を**補語必須の語**として禁止語の節へ追加した（裸の「区画」は床領域・壁領域・
通り芯の升目のどれとも読めてしまう。実際 165 箇所が裸で使われており、指す先は 3 種類に
分かれていた）。`docs/`・`dev_docs/`・コード内コメントの裸の「区画」をすべて解消した。

**検証**: `self_standing_wall_coverage` の単体テスト 7 件（単一床領域・またぎの長さ比・
台形の面積比・はみ出しの `uncovered`・版なし床領域を候補にしない・レベル不一致・
囲まれた壁版は対象外）、`wall_attached` の分配テスト 5 件（等価強度・またぎの総重量保存・
版なしで分配しない・覆われた分だけ配る・XY 投影面積で割る）、`precheck` のエラーテスト
1 件を追加。`cargo test --workspace --locked`（43 バイナリ）・clippy 3 種・fmt を通過した。

**残課題**:

- 床板（最小面積）と小梁（ちょうど 1 つ）の内包判定規則の統一（Q2。実データで差が 0 件のため急がない）。
- 厳密クリップは、壁が床領域の境界の辺と重なる場合を「覆われていない」と判定する
  （`polygon_contains_strict` が辺上 1 mm 以内を外とみなすため）。大梁の真上に立つ壁は
  取付き先を梁（線アンカー）にするのが正しいモデル化であり、エラーメッセージでもそう案内する。

### 5.29 Composer 共同レビュー（2026-08-28）

`claude/bed-wall-remaining-tasks-yynhp1`（§5.27・§5.28）を、Composer と共同で
コードレビューした。解析前チェックを通過した通常モデルで、旧フォールバック相当の
危険経路は残っていないことを確認した。レビューで直した点:

- `wall_attached.rs` のモジュール doc / `region_slab_area` が、削除済みの両端節点
  フォールバックをまだ説明していた。現実装（分配しない・`model_issues` が Error）へ直した。
- `RegionAnchor::FloorRegion` の doc がまたぎ配分を「長さに応じた」と書いていた。
  実装は台形面積比なので文言を揃えた。
- 壁版 UI のホバーが「所属する床領域」と書いていた。保存しない設計と矛盾するため、
  「載っている床領域（位置から都度解決）」へ直した。
- L 形非凸・辺上配置の `self_standing_wall_coverage` テストを追加した。
- 利用者向け `docs/` に、辺上 1 mm を内側とみなさないことと、未覆い 0.1% しきい値
  （`SelfStandingWallCoverage::has_uncovered`）を明記した。
- `model_issues` の断面未割当カウントが `Enclosed` だけを見ていたため、取り付く壁版は
  断面未割当でも何も報告されなかった。自重は断面から求めるので重量が黙って落ちる
  （危険側）。囲まれた壁版・取り付く壁版の両方を数え、警告文に自重を分配できない旨を
  明示した。4 節点でない判定は囲まれた壁版のまま。
- 立ち上がり高さの符号が両端で反転する壁で、重量比の合計が 0.5 になる欠陥を是正した。
  端点の `|h|` を結んだ台形を分母にすると、ゼロ交差を含む真の ∫|h| の 2 倍になり、
  区間ごとの分子（折れを含む）と釣り合わず半分が消える。加えて 4 点 Newell 面積は
  蝶ネクタイで打ち消し 0 になる。`abs_lerp_integral` / `abs_lerp_centroid` に集約し、
  自重面積・カバレッジ・線アンカー分割・柱按分重心が共有する。

**残課題（レビューで確認し、今回は設計変更しない）**:

- 既存壁版の取付き先種別（線 ↔ 床領域）の UI 切替は**意図的に実装しない**（§5.30 dig）。
  荷重経路が別物のため床板と同様、削除＋再作成とする。§5.29 の「不便」は残課題としない。
- DL なし経路の `accumulate_attached_wall_seismic_weight` は幾何カバレッジを見ず、
  自立壁の総重量を両端節点へ集中させる（用語集どおり。標準構成は DL 同期で呼ばない）。
- `has_uncovered` の 0.1% 未満は DL 等価面荷重から落ちる（丸めとして意図した危険側の微小漏れ。
  docs に開示した）。

### 5.30 囲まれた壁版の GUI 新規作成（2026-08-29、dig 3問）

§9 の残課題「囲まれた壁版を GUI から新規作成する手段がない」を実装した。
「既存壁版の取付き先種別（線 ↔ 床領域）を UI で切り替え」は dig で**不要と確定**
（荷重経路が別物。床板と同様削除＋再作成。残課題としない）。

**dig で確定した設計**:

- **Q2**: 囲まれた壁版の境界は **4 節点固定**（床板の 3〜N 可変とは非対称。解析要素は
  4 節点定式化のため GUI では典型形だけ作らせる。非 4 節点は ST-Bridge 取り込み経路）。
- **Q3**: 取付き先種別の切替 UI は**入れない**（作成時に種別を選び、変更は削除＋再作成）。

**編集コマンド**:

- `AddEnclosedWallPlate` — 柱・梁で囲まれた壁版（`WallPlateShape::Enclosed`）の追加。
  所属する壁領域は `rebuild_wall_regions` が自動で結びつける（`AddSlab` と同じ流儀）。
  境界が空、実在しない節点、実在しない断面は Noop。

**GUI**（`tables/wall_plates.rs`）:

- 「囲まれた壁版を追加」フォーム — 境界 4 節点（反時計回り）と断面を指定して追加。

**検証**: `squid-n-edit` に `test_add_enclosed_wall_plate_roundtrip` を追加。
`cargo test --workspace --locked`・clippy（`--all-targets`）・fmt を通過した。

**残課題**: MCP 配線（床板と同じ位置づけ）。部材表・応力図の壁柱幾何・ホバー多角形ピック・
クリック選択等（§5.17 の Step 9 残）。

---

## 6. 廃止・削除の対象

| 対象 | 理由 |
|---|---|
| ~~`Model.slabs` / `Slab`~~ | **取り消し（§3.1）。** `Slab` は「床領域内の版」という意味で存続する。廃止したのは PR #218 時点の「`Slab` ＝ 床領域そのもの」という設計 |
| ~~`JoistLine`（旧 `Slab.joists`）と `distribute_rect_with_joists`~~ | **取り消し（§3.1）。** `JoistLine` は `FloorRegion.joists`（交差小梁の格子解析・二段階伝達専用の手入力）として存続する |
| `Model.secondary_members`（横断リスト） | 領域が実体を持つ（D6）。**未着手**（§3.1 のとおり D6 は引き続き保留） |
| `SecondaryMemberId` の「配列添字と一致」不変条件 | 横断リストの廃止で不要（D6） |
| `Model::visit_secondary_member_ids` | 同上 |
| `Model::retain_secondary_members` | 同上 |
| `Model.misc_walls` / `OutOfFrameMiscWall` | 壁領域（取り付き領域）へ吸収（D3・D13）。**E4 で確定: 自由座標入力を廃止し、
D14 のアンカー（線／点／床領域）を必須とする節点参照へ完全移行。移行ロジックは書かず、
既存の自由座標入力は単純に作れなくする（実データ 0 件のため）** |
| `MiscWallTransfer` | 伝達規則へ統合（D16） |
| `SlabKind`（`Interior` / `Cantilever` / `Corner`） | 種別と取付き先で表せる（D19） |
| `Slab.edge_supported` | 支持辺は幾何から導く（D18）。現状書き手が存在しない |
| `squid-n-edit` の `SetWallAttr` / `RemoveWallAttr` | **削除済み（§5.27）。** 唯一の呼び出し元だった旧「壁属性」タブを壁版タブへ作り替えたため呼び出し元が 0 になった。加えて、いまや壁展開モデルの内部表現でしかない `Model.wall_attrs` へ利用者が値を書き込み保存できてしまう経路だった |
| `Model.wall_attrs` / `WallAttr` | 壁領域へ吸収（D3）。**E5 で確定: `WallRegion` は `Vec<WallPlateId>`
（複数の壁版）を持つ。E7 で確定: `Element::Wall` への一時的な `elem: Option<ElemId>` つなぎは
作らず、Step 7・8（型作り替え＋要素生成）を一括実施する。§5.14 で状況が変わった: `Model.wall_attrs`
自体は依然として型が残っているが、利用者が直接作る手段は既にない（`ElementKind::Wall` を
入力できる経路が壁展開以外になくなったため）。代わりに壁展開モデルが `WallPlate` から
`WallAttr` を都度合成し（移行期の内部表現。モジュール doc「wall_attrs の合成」参照）、
既存の壁消費者（自重算定・開口低減剛性・偏心率の雑壁剛性・数量拾い等）を無改修で
動かすための橋渡しとして使っている。個々の消費者を `WallPlate` 直接参照へ書き換える
作業（`WallAttr` の真の廃止）は未着手のまま** |
| `design_secondary_joist_checks` の幾何再導出 | 分配計算から小梁の曲げが出る（D9・Step 5） |
| `auto_loads.rs` の `LoadTarget::Span` フォールバック | 境界辺は必ず主架構になる（D1） |
| `squid-n-edit` の `SetSlabSecondaryJoistIds` ほか PR #218 のコマンド群 | 領域の形が変わるため作り直し |

---

## 7. 影響範囲（着手前の棚卸し）

`Model.secondary_members` を走査する非テスト箇所（D6 で二段ループになる）:

| ファイル | 内容 |
|---|---|
| `squid-n-load/src/story_gen/self_weight_calc.rs:317` | 二次部材の自重 |
| `squid-n-app/src/app/actions/design/check.rs:298` | 小梁設計 |
| `squid-n-app/src/viewer/deform.rs:224` / `viewer/mod.rs:1429` / `viewer/scene.rs:237` / `viewer/solid.rs:437` | 3D 表示 |
| `squid-n-app/src/tables/sections.rs:127` | 断面表 |
| `squid-n-app/src/app/nav_model.rs:31,146` | ナビゲータの断面グループ |
| `squid-n-io/src/stbridge/export.rs:228,278` | ST-Bridge 出力 |
| `squid-n-io/src/stbridge/import/assemble.rs:490` | ST-Bridge 取り込み |
| `squid-n-io/src/stbridge/section_std.rs:867,898` | 標準断面の解決 |

`Slab` を参照する主な箇所（D3 で置き換え）: `squid-n-load/src/floor/`（分配本体）、
`squid-n-job/src/auto_loads.rs`（荷重ケース同期）、`squid-n-app/src/app/actions/design/check.rs`（床設計）、
`squid-n-app/src/app/panels/draw_tools.rs`（床の作図）、`squid-n-edit`（床コマンド）、
`squid-n-io/src/stbridge/`（入出力）、`squid-n-design-jp/src/quantity/`（数量拾い）。

統合テスト `crates/squid-n-app/tests/full_model.rs` は構成の固定値（`secondary_members.len() == 56`、
`slabs.len() == 82`）と代表スカラのスナップショットを持つため、Step 2〜4 で必ず更新が要る。
**スナップショットの変化は V&V の対象**であり、`dev_docs/v_and_v/` にレポートを追加すること。

---

## 8. 未決事項

### 8.1 床側（Step 1 の着手前に決める）

- **段差床のある階の面走査。** D8 は水平面ごとに閉路を採るが、段差床のある階は同一階に
  複数のレベル面ができ、段差部の梁はどちらの面にも属する。面走査の入力をレベルで切る規則が要る
- **外周面の判別。** D8 に「最も面積の大きい面を捨てる」と書いたが、これは誤り。
  外周面は符号付き面積が負（時計回りに一周する）面として判別すること。
  中庭のある建物では、面積最大の内部面が外周面より大きくなりうる
- **中庭・吹抜けの扱い。** 上記の面走査では中庭も内部面として現れ、版なし床領域になる。
  それが意図どおりか（領域として持つか、除外するか）を決める
- **`Model.slabs` を置き換えるとき、床の面荷重強度が異なる領域が隣り合う場合の小梁の負担。**
  現行は代表スラブの強度を用いており、Step 4 の分配で厳密化する

### 8.2 壁側（Step 7 の着手前に決める）

**2026-08-25、dig により全項目解消（§3.2 の E1〜E8 参照）。**

- ~~鉛直構面の検出条件。斜め構面、節点が同一平面にない構面をどう扱うか~~ → **解消（E1・E2）**。
  通り芯に依存しない新規幾何判定（芯線候補 ＋ `MEMBER_AXIS_TOL_MM`）で直交・斜めを同じロジックで扱う。
  非平面な壁面は複数の `WallRegion` へ自然に分解される
- ~~壁版の開口（`WallOpening`）を領域が持つときの、複数開口モード（`Model.multi_opening_mode`）との関係~~
  → **解消（E6）**。引き続き `Model` 全体で 1 つのグローバル設定のままでよいと確認した
- ~~柱梁に囲まれない雑壁の受け皿~~ → **解消（E4）**。`OutOfFrameMiscWall` は D14 の 3 アンカー（線／点／床領域）を
  必須とする節点参照へ完全移行するため、「囲まれない」状態自体が作れなくなる
  （§2 の残課題一覧に載っていた 3 項目目。本書 §8.2 には当初なかったが同じ dig で解消した）

### 8.3 Step 3 の着手前に必要な対応

- ~~取り付き領域の辺荷重の解決を幾何解決へ寄せる~~ → **対応済み**。線分を覆う梁を幾何で求める
  `beams_along_segment` を新設し、`Span` の解決へ組み込んだ。実建物では 122 件の節点集中荷重が
  梁の分布荷重になった（記録は
  [`v_and_v/床辺荷重の梁への幾何割付_2026-08.md`](../v_and_v/床辺荷重の梁への幾何割付_2026-08.md)）
- ~~`PanelScan::crossings` を `run_diagnostics` へ接続する~~ → **対応済み**。`region_gen::crossing_beams`
  を `precheck::model_issues` へ組み込み、警告として診断・解析前チェックの両方に出るようにした
  （解析自体は通るためエラーにはしない）。判定を 1 か所に置くため、診断と解析前チェックが
  別々の検査を持たない既存の方針に合わせている。
  交差判定は総当たり（梁の本数の 2 乗）だったため、境界矩形の走査線法で絞るようにした
  （release ビルドでの実測: 梁 32,800 本で 532ms → 3.2ms、面走査全体で 530ms → 17ms）

**Step 3 の前提条件はすべて解消した。**

なお、「点が部材の材軸上に載っているとみなす許容差」は `geom::MEMBER_AXIS_TOL_MM`（10mm）へ
一本化した。荷重の割り付け（`secondary::SPAN_TOL_MM`）と交差の診断が別々の基準を持っていると、
「荷重は載るのに診断には出ない」ずれが生じるためである（交差の判定は外積のしきい値を
用いており、長い部材ほど厳しくなっていた）。

### 8.4 横断

- **ST-Bridge 出力と D5 の整合。** 入力の正は壁版なので、出力は壁版を走査する
  （§5.16。かつては解析要素へ展開してから出していた）。4 節点でない壁版も往復する。
  解析要素にしないことは診断・取り込み警告で知らせる
- **D10 の対応付けキーの弱さ。** 重心とレベルをキーにすると、通り芯を動かすような節点座標の
  編集で全領域の重心が変わり、版仕様も二次部材も引き継げない。境界節点集合や重なり面積率を
  副次キーにする案を検討すること

---

## 9. 残課題

| 状態 | 項目 |
|------|------|
| ☑ | Step 0: `check.rs` の小梁→スラブ判定へレベル一致を追加（別階への帰属 49 本 → 0 本）。同一レベル内の複数該当は Step 4 で解消 |
| ☑ | Step 0 の追補: 境界辺に載る小梁の負担幅を、載っているスラブの幅の平均に変更（並び順依存で最悪 17% 過小になっていた） |
| ☑ | Step 4: 荷重分配を `FloorRegion`/`Slab` 単位へ作り替え（§3.1・§5.4）。`FloorRegion`/`Slab` の再分離（D3 の修正）を含む。フィクスチャの代表スカラが変化 |
| 🔶 | Step 1〜6: 床領域の再設計。Step 1〜4 は実装済み、Step 5 は一部対応。残りは Step 5（荷重の基準統一）・6（MCP のみ）と D6（引き続き保留） |
| 🔶 | Step 5（一部対応）: 境界に載る小梁の負担幅は床板境界の幾何から求めるよう直した（`joist_edge_tributary_width`）。荷重の基準（`w × spacing`）を `distribute_region` の出力から導く形へ揃えることは見送った（§5.5 参照） |
| ☑ | Step 4 の V&V レポート作成済み（`dev_docs/v_and_v/床領域の再設計_荷重分配とSlabFloorRegion分離_2026-08.md`）。`snapshot_key_scalars` の変化と、実装中に見つけた既存バグ 6 件（うち 3 件は Step 4 の型再分離自体が生んだ回帰: スラブ協力幅・手入力小梁ライン経路の複数床板取りこぼし・RC梁のスラブ取付き判定）を記録 |
| ☑ | 同 V&V レポートの `static.DL.sum_base_axial`（Step 3→4 で約 7.7% 減）の原因を特定（2026-08-25 追記）。`base_column_axials` が柱自身の材端軸力だけを合計し、同じ支点に取り付く基礎梁の負担分を含まない測定関数だったための見かけ上の変化で、荷重は失われていない |
| ☐ | Step 4 で `distribute_polygon_supported` 相当（支持辺の分割伝達）を、領域が持つ二次部材から支持辺を導く形で書き直す（未着手のまま） |
| ☑ | Step 4 で取付き線の部分区間（`span != [0, 1]`）に対応する（§5.6）。`LoadTarget::Span` に `t` を足し、既存の幾何割付（§8.3）へ乗せた。GUI の入力欄も追加した |
| 🔶 | 取り付き領域。入力経路と ST-Bridge の 1 辺大梁→片持ち変換は Step 2b・3 済み。部分区間の荷重は対応済み（§5.6）。出隅の自動 Point 変換・L 形の自動分割は未 |
| 🔶 | Step 7+8: 壁領域の再設計（`OutOfFrameMiscWall`・`WallAttr` 吸収・要素生成への転換を一括実施）。着手前の設計は dig で確定済み（§3.2 の E1〜E8）。E8（壁入り実フィクスチャ・代表スカラのピン止め）は実施済み（§5.8）。壁側 `region_gen`（E1〜E3、§5.7）・`RegionAnchor::FloorRegion`/`WallPlate` 型（§5.8）・`WallRegion` の作り替え（§5.9）・`wall_region_rebuild`（§5.10・§5.13）は実装済み。**壁版の取り込み（ST-Bridge → `WallPlate`）・要素生成（`squid_n_load::wall_expand::expand_wall_elements`）・参照張り替え（自重算定・保有水平耐力・ST-Bridge 出力・GUI診断）は実装済み（§5.14、dig 7問で設計確定）。** `wall_bay_model`（`wall_model.rs`）を新経路へ移行した上で代表スカラが型移行前後で完全一致することを確認した。**§5.14 の実装レビューで発覚した「壁展開モデルを見ていなかった」回帰4件（剛域自動算定・耐震壁のせん断断面検定・数量拾い・保有水平耐力の部材ランク自動判定）は §5.15 でいずれも是正済み（実行して再現・回帰テスト追加済み）。** **§5.16 でレビュー指摘を是正済み**（剛域書き戻しの `ElemId` 一致アサート、ST-Bridge を壁版から書き出し 4 節点以外も往復、4 節点以外・断面未割当を診断／取り込み警告、利用者向け docs 追記）。残るのは Step 9（§5.17 で 3D形状・検定比図・モデル化図は壁が見えるところまで。部材表・応力図の壁柱幾何・ホバー多角形ピック・壁属性編集タブ・クリック選択等は未）と、T字取り付き境界の一般化（残課題として §5.14・§5.15 に明記）。**取り付き壁版（`Attached`）の入力経路（ST-Bridge取り込み・`squid-n-edit`）は §5.18 で実装済み。** |
| ☐ | `region_gen::wall` の性能: グリッド索引導入後も柱本数に対し超線形（900 本で 1.8 秒、§5.7 実測）。`members_on_plane`（直線ごとに全要素を線形走査）がボトルネックと見られるが未確認。Step 7+8 の結線時に、数百〜数千本規模の実建物での実測とあわせて切り分け・追加最適化の要否を判断すること |
| 🔶 | Step 9: 壁領域の UI・編集コマンド・MCP、ST-Bridge 入出力の追随（Step 7+8 の後）。**3D形状・検定比図・モデル化図は §5.17 で壁が見える**（`viewer::wall_expanded_view_model`。壁版がなければ複製しない性能ガード付き）。**壁版の編集 UI は §5.27 で実装済み**（モデルタブ「壁版」＝`tables/wall_plates.rs`。一覧・断面割当・取付き先／立ち上がり高さ・開口／三方スリット・取り付く壁版の追加削除。`SetWallPlateSection`/`SetWallPlateAttrs` を新設し、`SetWallAttr`/`RemoveWallAttr` を削除）。**§5.30 で囲まれた壁版の GUI 新規作成を実装**（取付き先種別の作成後切替は dig で不要と確定。床板と同様削除＋再作成）。部材表・応力図の壁柱幾何・ホバーの多角形ピック・クリック選択とヒンジ/時刻歴詳細・ヒンジ図マーカーは未対応（§5.17）。MCP 配線も未 |
| ☐ | `squid-n-app` の一括解析（`compute_static_all`）・荷重組合せ（`compute_combination`）が `squid_n_job::compute` を経由しない重複実装（`Analysis::prepare` 直呼び）を持つ問題。§5.14 で壁展開を個別に追加して凌いだが、根本的には `クレート横断の重複統合_申し送り.md` の対象 |
| ☑ | 取り付き壁版（`WallPlateShape::Attached`）の ST-Bridge 取り込み・編集コマンドを実装（§5.18、dig 5問）。ST-Bridge 取り込みは水平な辺が大梁に全長載り自由端が 1〜2 点の壁版を自動変換し（床側 D20 相当）、`squid-n-edit` に `AddAttachedWallPlate` 等を新設した。`squid-n-app` の GUI 配線・MCP ツールは未着手のまま（Step 9 GUI残・MCP残と同じ位置づけ）。§5.22 で自由端判定の誤変換と、利用者向け docs の「荷重分配する」という虚偽記載を是正。取り付く壁版の自重分配は未実装 |
| 🔶 | T字取り付き境界（5節点以上）の一般化。**2026-08-27 dig で「一般化しない（Q6=C を維持）」と確定した（§5.19）。** 「実測55件中2件・3.6%」の根拠となる再現可能なフィクスチャがリポジトリに存在せず、実データの形状（共線の余分な節点か、真に非矩形の輪郭か）を検証できなかったため。壁エレメントが壁柱＋剛梁変換による4節点24自由度の定式化である以上、根拠のない「按分」で任意形状を4節点へ落とし込むことはしない。かわりに、3箇所（`wall_expand`・`precheck`・ST-Bridge取り込み）に分散していた「境界が4節点か」の判定を `WallPlate::has_quad_boundary` へ集約した（§5.19） |
| ☑ | 敵対的レビューで発覚した2件（`Model::visit_node_ids`・`node_referenced_outside_elements` が `wall_regions` を走査していなかった）を是正済み（§5.20）。**根本原因（`node_referenced_outside_elements`・`region_rebuild::node_has_structural_ref` が同じ判定を独立に重複実装していたこと）も是正済み（§5.21）**: 共通部分を `Model::node_referenced_by_regions_or_plates` へ集約し、全フィールド種別を1つずつ固定する網羅テストを新設した。`visit_node_ids`（書き換え用）との整合はなお機械的に強制されない（Rust にリフレクション相当の仕組みがないため）。完全な根本解決（コンパイル時の網羅性チェック、derive マクロ等）は将来課題として残る |
| ☑ | 取り付く壁版（`Attached`）の自重を分配する経路（§5.23、dig 3問）。入力と ST-Bridge 変換は §5.18、誤変換と docs の虚偽は §5.22 で是正済み。**§5.23 で `squid_n_load::wall_attached` を新設し、地震用重量・DL・数量拾いへ算入した**（「線」アンカーは床側と同じ幾何解決パイプラインへ合流、「床領域」アンカー（自立壁）は所属先の床板へ等価面荷重として上乗せ）。**§5.24 で台形壁の位置を高さ比例の線形分布・面積重心按分へ直した。** **§5.25 で敵対的レビュー（総和・形状は結合テストで確認。開口位置は見ない近似が残る）。** 床領域内で床板ごとに面荷重強度が異なる場合の按分は総和保存のみ（未対応のまま）。床の取り付く床板の台形 footprint は区間中点近似のまま |
| ☑ | 自立壁（`RegionAnchor::FloorRegion`）の床領域参照が `rebuild_floor_regions` の ID 振り直しに追随しない問題を是正（§5.28、dig 9問）。**`region` フィールドを廃し、壁の位置から都度解決する形へ改めた**（`Model::self_standing_wall_coverage`）。床領域をまたぐ壁は境界で厳密クリップして分割し、台形面積比で各床領域へ配る。**深掘りの過程で、分配先が無いときのフォールバック（両端節点への集中）が危険側だったことも判明した**: 自立壁の両端は非構造節点でありうるが `DofMap` は非構造節点への節点荷重を無視するため、地震用重量には残るのに長期 DL からは黙って消えていた。フォールバックを削除し、「自立壁は床板を持つ床領域の上にのみ存在する」を不変条件として `model_issues` のエラー 1 か所で検査する形にした。§5.27 で「`validate` が検査していない」と書いたのは誤りで、実在確認だけは行っていた（振り直しによる指し違いは素通りするため指摘の本体は変わらない） |
| ☐ | 段差床・傾斜小梁が黙って検定対象から外れる（Step 0 の副作用）。診断への追加は Step 5 と同時に行う |
| ☑ | `RegionBoundaryScan::crossings`（旧 `PanelScan::crossings`）を診断へ接続（§8.3。Step 3 着手前に実施済み） |
| ☐ | 3 枚の床板に載る小梁（T 字取り付き）の負担幅は平均による近似のまま（`check.rs::design_secondary_joist_checks` は `Slab` 単位への retarget のみ済み。近似の解消は未） |
| ☐ | 床板を編集する MCP ツール（`AddSlab`/`AddAttachedSlab` 等に対応するもの）は未整備。Step 4 時点では影響なし（既存の MCP ツールは `Slab`/`FloorRegion` を直接編集しない） |
| ☑ | §8.2（壁側の未決事項）を dig で決定（§3.2 の E1〜E8） |
| 🔶 | §8.1（床の段差床・非平面スラブ）・§8.4（横断）の未決事項を決める。**2026-08-27 実装調査で判明**: §8.1 の4項目のうち3項目（段差床の面走査規則・外周面の判別・中庭吹抜けの扱い）は `region_gen/floor.rs` の実装（レベルごとの面走査・符号付き面積による外周判定・中庭は版なし床領域として自然に現れる）で既に解消済みで、本節の記述が古いままだった（§8.1 の文面更新が未着手）。残る「隣接領域で面荷重強度が異なる場合の小梁の負担」も Step 4 の `Slab` 単位分配で実質解消済みと見られるが未確認。§8.4 の D10 対応付けキーの弱さは未着手のまま |
| ☑ | `specs/床領域と壁領域.md` を本書の決定（特に §3.1 の `FloorRegion`/`Slab` 再分離）に沿って全面改訂済み |
| ☑ | 壁版の編集 UI（旧「壁属性」タブの作り替え。§5.27、dig 5問）。§5.14 で壁が生成専用になって以降、モデルタブ「壁属性」は対象が常に空で機能停止しており、壁の断面・開口・三方スリットを変更する手段が GUI にもコマンドにも無かった。`SetWallPlateSection`/`SetWallPlateAttrs` を新設し、`tables/wall_plates.rs`（モデルタブ「壁版」）で一覧・断面割当（即時）・開口／三方スリット（フォーム＋適用）・取り付く壁版の取付き先／立ち上がり高さ（即時）・追加削除を扱う。三方スリットは取り付く壁版では入力欄自体を出さない（自重を上下に分ける相手方の下端が無く `wall_attached` も読まないため。dig Q4）。`SetWallAttr`/`RemoveWallAttr` は呼び出し元が 0 になったため削除した。残るのは囲まれた壁版の GUI 新規作成と MCP 配線。**§5.29 で Composer 共同レビュー**: 削除済みフォールバックを説明する doc・「長さ比」誤記・UI「所属」文言を是正し、L 形／辺上テストと 0.1% しきい値の利用者向け開示を追加した |
| ☑ | 囲まれた壁版の GUI 新規作成（§5.30、dig 3問）。`AddEnclosedWallPlate` を新設し、モデルタブ「壁版」の追加フォームで境界 4 節点（反時計回り）を指定して作成。取付き先種別（線 ↔ 床領域）の作成後切替は dig で不要と確定し実装しない（床板と同様削除＋再作成） |
| ☑ | CMQ 図を、表示中荷重ケースの全荷重（床分配・梁自重・取り付く壁版の線アンカー・手入力）から描くよう改修済み（§5.26、[CMQ図を荷重ケースの全荷重へ_申し送り.md](CMQ図を荷重ケースの全荷重へ_申し送り.md)）。dig により強軸/弱軸の面選択も追加 |
