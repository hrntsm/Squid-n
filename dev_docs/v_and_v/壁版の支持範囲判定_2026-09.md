# 壁版の支持範囲判定

作成日: 2026-09-10

## 対象と不具合

`squid-n-load/src/wall_plate_load.rs` の支持判定は、主架構との重なりが一つでもあれば辺全体を支持するとしていた。長さ 4000 mm の辺のうち 2000 mm だけに梁があるモデルでも、荷重伝達先なしの診断をすり抜ける。

支持区間を始点順に走査し、支持区間に重複がないことと、連続被覆の終端が辺の終端へ達するかを判定する。既存の材軸判定と同じ 10 mm の許容差を用いる。分配先がなければ既存の解析前エラーへ回す。部分支持を全長支持へ置き換える近似は行わない。

## 回帰検証

`wall_plate_load::tests` に次の独立した幾何条件を追加した。

- 4000 mm の辺を 2000 mm だけ支持する場合は診断する。
- 2000 mm の部材 2 本で連続支持する場合は全重量を保存する。片方の要素は逆向きとする。
- 先頭 2000 mm を二重に覆い、末尾 1000 mm を覆う場合も、途中の 1000 mm の隙間を診断する。

修正前は部分支持と重複区間の 2 テストが失敗した。

## 方針決定と変更

利用者の指示により、要素にならない囲まれた壁版の支持辺を `self_weight_shares` で、鉛直な間柱の上下端を `gravity_end_shares` で明示指定する方式へ変更した。未指定・不正指定では解析を止め、支持不能な辺の荷重を別の辺へ振り替えない。間柱自重も同じ端部負担率で配る。地震用重量も同じ指定を使う方針が利用者から指定されたため、密度直接集計を逐次伝達へ接続し、DL 経由との階重量一致を端部負担率 0・0.25・1 で確認する統合テストを追加した。

GUI と MCP の編集コマンド、Undo、SCZ 保存へ接続した。SCZ のデータ項目を追加するが、スキーマ番号は 1 のままとする。後方互換性は維持しない。ST-Bridge は指定値を持たず、取り込み後の指定が必要。自重負担率を明示した囲まれた壁版は取り付く壁版へ自動変換しない。

計算根拠2ページの自重分配の矛盾を解消し、間柱の端部配分の残課題を除いた。「すべて安全側」としていた記録を訂正した。負担率は利用者が支持条件から決める入力であり、妥当性をプログラムが判定するものではない。実験・実測との照合は未実施。

## 検証範囲

部分被覆・重複区間・分割部材、壁の任意負担率、スリット辺への指定、未指定・負値・非有限値、間柱の端部配分・逆向き・総量保存、編集の取り消し、SCZ 往復を検証する。

## 実行結果（2026-09-11）

Rust stable 1.98.1 で確認した。

| 検証 | 結果 |
|---|---|
| `cargo test --workspace --locked` | 2,383 成功、0 失敗、既存の 8 件は ignore |
| `cargo test -p squid-n-app --features gui` | 602 成功、0 失敗、既存の 7 件は ignore |
| `cargo test -p squid-n-mcp --features mcp` | 38 成功、0 失敗 |
| `cargo clippy --workspace --all-targets --locked -- -D warnings` | 成功 |
| `cargo clippy -p squid-n-app --all-targets --features gui --locked -- -D warnings` | 成功 |
| `cargo clippy -p squid-n-mcp --all-targets --features mcp --locked -- -D warnings` | 成功 |
| `cargo fmt --all -- --check` | 成功 |
| `cargo run -p xtask -- check-deps` | 成功 |
| `git diff --check` | 成功 |
| 変更 Markdown 14 ファイルの相対リンク | リンク先の欠落なし |
| mdBook ビルド | mdBook 未導入のため未実施 |

GUI の機能付きテストは実施したが、実画面の操作・目視検証は未実施。ここでの成功はコードの回帰検証であり、全構造計算理論の監査完了や実験による妥当性確認を意味しない。

## 支持梁の重複の追加確認（2026-09-11）

4000 mm の辺全体を支持する梁と、同じ辺の 2000 mm を支持する梁が重なると、支持判定を通過し、密度直接集計で重複区間の重量が加算される経路を確認した。重複梁への負担配分は入力から決まらないため、壁版の支持不備として診断する。別の間柱への自動振替は行わない。回帰テストは修正前に失敗し、エラー時に既存の節点重量を変更しないことも検証する。

追加修正後は `cargo test -p squid-n-load --locked`（163 件）、`cargo test -p squid-n-app --test wall_post_model --locked`（11 件）が成功。ワークスペース・GUI・MCP の `clippy --all-targets` も成功した。上記の全件テスト表は追加修正前の結果であり、追加修正後の全件再実行ではない。
