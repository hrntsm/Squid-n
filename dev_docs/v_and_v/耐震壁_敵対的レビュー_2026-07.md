# 耐震壁（壁エレメントモデル）敵対的レビュー（2026-07）

**監査日:** 2026-07-25（PR #121 マージ後）
**対象:** `squid_n_element::wall`（壁エレメント・雑壁・側柱）、開口低減、
および保有水平耐力（Ds・βu・耐力壁の種別）との連携。
**参考資料:** 平19国交告第594号第1、2007年版建築物の構造関係技術基準解説書、
JSCA関西 2008年度定例研究会「技術基準はどう変わったか − RC造編 −」（西崎 隆氏）。

耐震壁は `WallPanelElement`＝**4 節点 24 自由度の壁エレメントモデル**
（壁柱＝仮想中央柱を上下の剛梁で挟み、剛梁両端はピン）であり、シェル要素ではない。

---

## 1. 修正済み

### 1.1 【最重大・実証済み】非線形解析で耐震壁が永久に弾性のまま

- **ファイル:** `crates/squid-n-element/src/factory/mod.rs`（`build_nonlinear_behavior`）、
  `crates/squid-n-element/src/wall/wall_panel.rs`、
  `crates/squid-n-core/src/rc_wall_capacity.rs`（新規）。
- **不具合:** `build_nonlinear_behavior` は `ElementKind::Wall` を
  `_ => build_behavior`（弾性）へ落としており、`internal_force` は `K24·u` の
  線形弾性のままだった。すなわち**プッシュオーバーで耐震壁が降伏せず、
  押し込むほど際限なく水平力を負担する**。
- **実証:** 壁 4000×3000×t200（Fc24）を上辺水平に押した内力の実測値:

  | 押込み量 | 壁が負担する水平力 |
  |---|---|
  | 100 mm | 178,731 kN |
  | 200 mm | 357,463 kN（ちょうど 2 倍） |
  | 300 mm | 536,194 kN（ちょうど 3 倍） |

  完全な線形＝**降伏なし**。この壁の終局せん断強度は 1,000 kN 台であり、
  実強度の数百倍の水平力を負担していた。
- **影響:** 崩壊機構が形成されず、保有水平耐力 Qu は壁の弾性剛性に支配されて
  極端に過大（**危険側**）。βu（耐力壁の水平耐力比）も無意味な値になり、
  耐力壁の種別（WA〜WD）判定に用いる τu も弾性解の値だった。
- **修正:**
  - 耐震壁の終局せん断強度 Qu（荒川mean式系）と**耐力用**開口低減率
    r2 = 1−max(r0, l0/lw, h0/h) を Layer 0 の
    `squid_n_core::rc_wall_capacity` へ移設（`squid-n-element`/`squid-n-solver` は
    `squid-n-design-jp` に依存できないため。`rc_capacity` と同じ構成）。
    `squid_n_design_jp::rc::wall_nonlinear` は本体へ委譲し、単一の定義とした。
  - `WallPanelElement` の面内せん断を**弾完全塑性**化。せん断モード
    p（上辺 2 節点の壁面内水平並進）に沿う塑性すべり γp を導入し、
    `u_eff = u − γp·p`、`Q = pᵀ·K·u_eff` が Qu を超えないようリターンマッピング
    （`Δγp = (|Q|−Qu)·sign(Q)/(pᵀKp)`、Q は γp に線形なので 1 回で厳密に復帰）。
    降伏中の接線は整合接線 `K − (Kp)(Kp)ᵀ/(pᵀKp)`。
  - `build_nonlinear_behavior` で Wall に Qu を与える。Qu を算定できない壁
    （Fc 未設定等）は従来どおり弾性。**弾性解析（許容応力度計算）の経路は不変**。
    → この「Qu を算定できない壁は弾性」という扱いは危険側であり、その後
    [未入力材料強度の危険側フォールバック_2026-07.md](未入力材料強度の危険側フォールバック_2026-07.md)
    で撤回した（現在は解析をエラー停止する）。
  - 回帰テスト `test_wall_shear_yields_at_ultimate_strength`（300mm 押しても
    Qu を超えず、かつ Qu に到達）、`test_wall_stays_elastic_in_linear_path`
    （弾性経路は従来どおり線形）。

### 1.2 【重大】I 形断面のせん断形状係数 κ が内部整合性を欠き、面内せん断剛性が過大

- **ファイル:** `crates/squid-n-core/src/section_shape/material.rs`、
  `crates/squid-n-element/src/wall/wall_panel.rs`。
- **不具合:** 側柱付き壁の κ に用いていた閉形式
  `κ = 3(1+ξ)/(5(1−ξ³(1−η))²)·[η + ξ(1−η)((15/8)(1−ξ²)² − ξ⁴η)]` は、
  記号 ξ・η の定義が原典で確認できないまま仮定されていた（コード内に明記あり）。
  **内部整合性の検証で破れが確定**した: η=1（側柱幅＝壁厚＝**一様矩形**）なら
  ξ によらず κ=1.2 でなければならないが、実装は 0.6(1+ξ) を返す
  （ξ=0→0.60、ξ=0.5→0.90、ξ=1.0 のみ偶然 1.2）。
  結果として側柱が大きいほど κ が 1.2 から**減少**し（I 形断面では増大が正）、
  せん断断面積 `as_y = as_gross/κ` が**総断面積を超える非物理な値**になっていた
  （t=150・lw=4000・600角柱で as_y=1.45e6 mm² > 総断面 1.32e6 mm²、
  面内せん断剛性が最大 5.8 倍過大）。剛性率・偏心率・水平力分担にも波及する。
- **修正:** 材料力学の**厳密式** κ = A/I²·∫(Q(y)²/b(y))dy を
  `wall_shear_shape_factor_isection(d_total, dc_each, bc, t)` として実装し
  （フランジ＝側柱、ウェブ＝壁板の平面 I 形断面を数値積分）、壁要素から使用する。
  一様矩形で厳密に 1.2、側柱が大きいほど κ 増大、`A/κ` がウェブ断面積と
  総断面積の間に収まることをテストで固定した。従来の閉形式は後方互換のため残置。

### 1.3 【重大・危険側】側柱の面内ピン化が非線形パスに適用されず、せん断が二重計上

- **ファイル:** `crates/squid-n-element/src/factory/mod.rs`。
- **不具合:** 線形パスは `Beam` に対し `wall_side_column_release` を適用して
  面内両端ピン（`InPlaneReleasedColumn`）に差し替えるが、
  `build_nonlinear_behavior` の `Beam` アームにはこの判定がなく素通りしていた。
  その結果、**一次設計は「側柱＝面内両端ピン」、保有水平耐力は「側柱＝両端剛接」**
  という別モデルになる。さらに壁パネルは `as_gross = 壁板 + 側柱断面` として
  側柱分を既に負担しているため、非線形解析では側柱の面内せん断が**二重計上**され、
  保有水平耐力を過大評価する（危険側）。
- **修正:** 非線形パスの `Beam` アーム先頭に側柱判定を追加し、線形パスと同一の
  扱いに揃えた。

---

### 1.4 【重大・修正済み】雑壁（袖壁）の偏心 e の符号が壁の節点入力順に依存

`frame/beam/construct.rs` の袖壁偏心は `bottom_pair` のインデックスだけで符号を決め、
壁ローカル +x の向き（＝節点入力順で反転しうる）を `.abs()` で捨てていた。柱の両側に
壁があり 2 枚の向きが逆の場合、**左右の袖壁が柱の同じ側に載る**評価となり図心・合成
断面二次モーメントを誤っていた。
- **修正:** 符号付き射影 `sign_s · arm · dot(e_wall, 柱局所曲げ軸)` で偏心を与える。
- **検証:** 中央柱の両側に袖壁を付け、片方の壁の節点順を反転させたモデルで
  `iz` が一致することを確認する回帰テスト
  `test_misc_wall_wing_eccentricity_is_independent_of_wall_node_order` を追加。
  修正前は **iz = 53.9e9 vs 23.8e9（2.3 倍の差）** と入力順で剛性が変わっていた。

### 1.5 【修正済み】開口低減の幾何・式の不統一

- 開口周比の分母を壁エレメントと同じ `wall_panel_geometry`（壁長＝上下辺平均、
  高さ＝上下辺中点間距離）に統一した。従来は「全節点対の水平距離の最大（＝下辺長）」
  ×「z の全幅」で、台形壁では開口周比の分母とせん断断面の壁長が食い違っていた。
- 剛性用 r1 = 1−1.25·r0 と開口周比 r0 を `squid_n_core::rc_wall_capacity` へ集約し、
  `squid_n_design_jp::wall_opening` は委譲（耐力用 r2 と併せて単一定義化。
  片方だけ直して静かに乖離することを防ぐ）。
- 面外せん断 `as_z` にも開口低減を適用した（従来は面内のみで取りこぼし）。

### 1.6 【修正済み】側柱の収集条件とピン化条件の不一致

壁パネルは側柱断面を `as_gross` に算入するが、その前提は「側柱が面内両端ピン化され
面内せん断を負担しない」こと。従来は収集条件（節点対一致のみ）とピン化条件
（RC 判定・耐震壁成立・鉛直材判定）が食い違い、条件がズレる入力で面内せん断が
二重計上されていた。収集側にも同じ判定を課して一致させた。

### 1.7 【修正済み】幾何剛性へ渡す軸力が部材軸力でなかった

`pushover/assembly.rs` は `geometric_stiffness(f.data[0])`（＝節点 i の**グローバル
Fx**）を渡しており、鉛直柱・斜材・任意方向材で軸力とは無関係な成分を用いていた。
材端力を要素局所 ex へ射影した部材軸力 N（引張正）を渡すよう修正
（P-Δ を有効化した際の誤りを防ぐ潜在バグの解消）。

### 1.8 【修正済み】耐力壁の種別で壁式構造の列が使えなかった

`rc_wall_type(.., wall_structure = false, ..)` と固定され、常に緩い「壁式構造以外」の
列（WA 0.20・WB 0.25）で判定していた。`StoryStructure` には壁式がないため、設計タブに
「壁式構造」チェックボックスを追加して明示指定できるようにした。

---

### 1.9 【修正済み】壁質量が gross で自重側の控除と不整合

`mass_total = ρ·t·lw·h` は開口控除なしだったが、節点質量からの控除側
（`squid_n_load::story_gen`）は開口控除・サッシ重量加算済みで、差分だけ地震用質量が
恒常的に過大だった。開口面積を控除しサッシ重量を加算して規約を揃えた
（内法寸法補正の残差は §2.4）。

### 1.11 【修正済み】Qu の等価引張鉄筋比を設定不備時に代替値で埋めていた

側柱の主筋量が取得できない場合に、壁筋比から `at = ps·te·d` を代替値として用いて
いた。しかし**側柱があるのに主筋を読み取れないのは断面設定の不備**であり、
無音で代替値を用いると誤った Qu で解析が通ってしまう。
- **修正:** 代替は「付帯柱がない壁（壁のみの耐震壁）」に限定し、その場合の
  `at = ps·te·d`（pte = 100·ps）は正規の扱いとして明記した。設定不備
  （側柱があるのに主筋が取得できない／側柱も壁筋比もない／Fc 未設定）は
  `wall_shear_capacity_issue` が検出し、プッシュオーバーを**是正内容を示す
  日本語メッセージでエラー停止**させる。
- **テスト:** 側柱＋主筋あり→不備なし・Qu>0、側柱ありで主筋 0 本→不備検出かつ
  壁筋比で代替しない（Qu=0）、側柱なし＋壁筋比あり→正規に Qu>0、側柱も壁筋比も
  無し→不備検出、の 4 件。

### 1.10 【修正済み】開口低減が許容・終局・剛性で三重実装

`rc/wall.rs` の許容せん断 `min(1−l0/l, 1−r0, 1−h0/h)` は終局側の
`1−max(l0/l, r0, h0/h)` と恒等的に同一式でありながら別実装だった（片方だけ直すと
静かに乖離する）。剛性用 r1・開口周比 r0 と併せて
`squid_n_core::rc_wall_capacity` へ集約した。既存の壁テスト 43 件が不変で通ることで
両形式の同一性も確認できている。

---

## 2. 残課題（未修正。要・原典照合または再定式化）

### 2.1 【2026-09-11 修正済み】面内水平軸まわりの剛体回転で K24·u ≠ 0（剛体運動不変性の破れ）

変換行列 A の運動学 `ω = ex×(u_b−u_a)/lw` は、剛梁の剛体運動として符号・
レビチビタ・上下辺での lw_top/lw_bot の使い分けとも**正しい**（剛体並進、
鉛直軸まわり・面外法線軸まわりの剛体回転では K24·u = 0 を数値確認済み）。
しかし ω は定義上 ex 成分（剛梁軸まわり）を持たず四隅の回転自由度列も 0 のため、
θ = α·ex（壁を面外に倒す回転）を与えると壁柱端回転が 0 のまま上下中点の面外
相対並進だけが残り、**両端固定の逆対称曲げとしてスプリアスな内力**が生じる
（4000×3000×t150 で α=1e-3 rad のとき max|f| = 1.71e4 N）。
裏返すと壁の面外曲げが節点回転に依らず常に両端固定として評価される。
`side_column` の「ピンは面内のみ」という方針とも矛盾する。
剛体回転のテストが存在しないことが見落としの原因。

2026-09-11 に現行の `WallElement` で再現した。追加した
`test_wall_element_rigid_rotation_horizontal_axis_zero_force` は、各節点へ `u = θ × x` と
同一の回転 `θ` を与え、`K·u = 0` を要求する。
`cargo test -p squid-n-element --locked test_wall_element_rigid_rotation_horizontal_axis_zero_force`
で水平軸回転（0.001 rad）の最大節点力 17126.68784749801 N を検出して失敗した。
面外接合条件の確認待ちのため当該テストは理由付き `ignore` とし、
`-- --ignored` で再現できる状態を残す。剛性本体は未修正である。
四隅の節点回転と壁柱の面外回転を連成させるか、面外端部を解放するかは、
荷重負担と変形を変えるモデル化の選択なので利用者へ確認する。

### 2.2 雑壁（袖壁）の偏心 e の符号が壁の節点入力順に依存 → §1.4 で修正済み

### 2.3 【重大】雑壁の袖壁と腰壁・垂壁で同じ壁板を二重計上

開口位置が未入力、または開口が中央を跨がない壁では、`wing_length` が lw/2
（両側柱で壁長全部）、`strip_height` が h/2（上下梁で壁高さ全部）となり、
柱側と梁側で**同じ壁板が 2 回**剛性に算入される。領域の排他ルールが必要。
逆に、柱が中間節点で分割されている等で節点対が一致しない場合はどこにも
算入されず無剛性で消える取りこぼしもある。

### 2.4 壁質量の内法補正が未反映（開口控除・サッシ重量は §1.9 で対応済み）

`mass_total` に開口控除とサッシ重量を反映して自重側と揃えたが（§1.9）、自重側が
行う**周辺柱梁の内法寸法補正**（`wall_clear_area_factor`）は未反映。同関数は
`squid-n-load` にあり `squid-n-element` からは参照できない（両クレートは兄弟で
依存関係がない）ため、共通化の設計判断が必要。残差の分だけ地震用質量が過大
（安全側）。

### 2.5 開口低減の h・l が芯々（→ 台形・二重定義は §1.5 で修正済み。内法/芯々は未解決）

台形壁での分母不整合と式の二重実装は §1.5 で解消した。残るのは
**ドキュメントは「内法」と記すが実装は芯々**という定義の不一致。芯々は r0 を
過小＝剛性過大かつ耐震壁判定 r0≤0.4 が甘くなる方向のため、原典（開口周比を
梁心‐柱心で採るか内法で採るか）を確定させて実装と docs を揃える必要がある。

### 2.6 開口低減が面内曲げ剛性にほぼ効かない（面外せん断は §1.5 で対応済み）

壁パネルの面内水平剛性は `12EI/((1+φ)h³)` で開口低減 r は φ 項経由でしか効かない
ため、**曲げ支配の細長い壁では r を下げても剛性がほとんど下がらない**
（r→0 でも 12EIz/h³ が残る）。RC規準の「無開口として算定した剛性に r を乗じる」に
対して非保守側になり得る。曲げ剛性側への低減の扱いは要・原典照合。

### 2.7 側柱検出の穴

- 直交 2 枚の壁を共有する隅柱は、`ReleaseAxis` が単一値のため**1 方向しか
  ピン化されない**（実務で普通に発生）。
- 節点 ID の完全一致に依存するため、柱が中間節点で分割・壁が柱心からオフセット
  等では側柱と認識されず、壁パネル（`as_gross` に側柱込み）と剛接柱の双方が
  面内せん断を負担して剛性過大になる。
- ~~`wall_panel` 側の側柱収集条件と `side_column::detect` のピン化条件の不一致~~
  → §1.6 で修正済み。
- 側柱の沿壁方向せいを寸法の大小で推定しており、柱の実際の向きを見ていない。

### 2.8 開口低減の係数（→ 二重実装は §1.10 で解消済み）

許容せん断（RC規準18条）・終局せん断・剛性のいずれも
`squid_n_core::rc_wall_capacity` へ集約済み。残るのは**終局側の係数を 1.1 と
する文献があり要・原典照合**（実装は 1.0。もし 1.1 が正なら Qu を最大 1 割
過大評価する）。

### 2.9 保有水平耐力側の規定で未対応のもの（参考資料より）

- **脆性部材の扱い:** 「脆性的な破壊をする部材を持つ建築物の保有水平耐力は、
  原則としてそれらが破壊する時点を保有水平耐力とする」。耐震壁がせん断破壊
  する時点で保有水平耐力・Ds を決める運用は未実装（現状は Qu で頭打ちの
  弾完全塑性）。特に**耐震壁が連層で存在しない場合**は崩壊形形成前に壁の
  せん断破壊で保有水平耐力が決まる。
- **正負両方向:** 保有水平耐力は X 正・X 負・Y 正・Y 負の 4 通りで算定すべきだが、
  現状のプッシュオーバーは正方向のみ。
- **浮き上がり:** 架構全体の転倒による崩壊形は認められず節点の浮き上がりを
  拘束して検討する必要があるが、未対応。
- **崩壊形の呼称:** 告示の「部分崩壊形」は「ある特定の階が部分的に不安定になる
  のに十分な塑性ヒンジが形成されたとき」を指すが、本実装の
  `MechanismType::Partial` は「機構未形成／未確認」の意味で使っており、
  用語が一致していない。「局部崩壊形」も未定義。
- **剛梁モデルの注意点:** 剛梁を用いたモデル化では転倒モーメントによる軸力の
  再分配が評価できず、突き出た部分の剛梁剛性は 1 層分の壁剛性の 1/2 として
  評価すべきとされる。本実装は剛梁を変換行列で表現しており該当の扱いは未検討。

### 2.10 性能

`BeamElement::new` が梁 1 本ごとに `collect_misc_walls(model)` でモデル全走査を
行い、剛性組立のたびに全要素分呼ばれるため O(梁数×要素数×壁属性数) になる。
`internal_force` が毎回 `stiffness_24()` を再構築する点も無駄。

---

*本書は耐震壁まわりの V&V を対象とする。§2 の各項は着手時に本書を起点とすること。*

## 2026-09-11 面外回転ピンの指定に基づく修正

利用者から「面外回転はピン」と指定を受けた。壁柱の局所 y 軸まわり両端回転を静縮約で解放し、弾性剛性・せん断ばねの初期剛性・断面力復元に共通適用した。ファイバー壁柱も同じ自由度を内部回転として解放する。利用者の方針に従い、壁柱の面外曲げ抵抗は考慮しない。

確認待ちの ignore を解除した。3軸の剛体回転について弾性・ファイバー経路の剛性と状態内力、断面力復元を検証した。壁要素の30件のテストが成功した。実験照合は未実施。

壁モデルのスナップショットは、一次周期が0.07898 sから0.09606 s、DLの壁断面力絶対値合計が1.070e5から7.962e4へ変化した。面外両端ピンによる抵抗の解放と断面力復元の整合に対応する変更として期待値を更新した。面内EXの壁断面力・頂部変位、Ds・保有水平耐力のスナップショットは変化しなかった。

修正後の全体確認: ワークスペース2388件成功（既存8件ignore）、GUI付き602件成功（既存7件ignore）、MCP付き38件成功。ワークスペース・GUI・MCPのclippy（全て `--all-targets --locked -- -D warnings`）、fmtと差分チェックも成功した。

## 2026-09-11 非線形時刻歴の壁柱断面力

壁要素の `state_member_forces` が未実装で、非線形時刻歴の部材断面力記録で壁が常に欠落していた。状態断面力の存在を要求するテストで修正前の失敗を確認した。ファイバー壁柱は内部要素へ委譲し、せん断ばね経路は塑性すべりを除いた有効変位から復元する。降伏・除荷・再載荷で、記録せん断力と解析の内力合計の一致を検証する。

状態断面力の修正後の確認: 壁要素30件、solverの時刻歴40件が成功。全構成のclippy（ワークスペース・GUI・MCP、`--all-targets`）、fmt・差分チェックも成功。直前の全件テスト結果は面外ピン修正時点のものであり、状態断面力の追加後は上記の範囲で確認した。

## 壁質量と内法面積の整合（2026-09-11）

荷重計算は内法面積、壁質量行列は芯々面積で算定されており、節点質量の控除と再加算で質量が過大になっていた。既存の内法係数をcoreへ共通化し、質量行列にも適用した。4000×3000×150 mmの壁で周辺柱梁が600 mmの場合、内法3400×2400 mmから求めた質量と3方向の質量行列の合計を照合する回帰テストを追加した。控除寸法の規則は変更しない。

質量修正で壁モデルの一次周期0.09606→0.09227 s、二次周期0.06196→0.06008 sとなった。地震用重量・静的応力・Dsと保有水平耐力の期待値は一致した。周期のみ質量修正に伴う差分として更新した。

内法質量の修正後はワークスペース2389件成功（既存8件ignore）、GUI602件成功（既存7件ignore）、MCP38件成功。全構成のclippy（`--all-targets --locked -- -D warnings`）、fmt、差分チェックも成功した。

## 壁の節点順と面積（2026-09-11）

壁剛性は節点を上下辺に並べ直す一方、自重・質量の面積は入力順をそのまま多角形として扱っていた。矩形の対角線を横切る列挙順では面積が相殺されうる。壁幾何をcoreへ移し、荷重・質量も同じ周回順で評価するよう統一した。内法面積の回帰テストを正順・逆順・周回しない2通りに拡張し、質量行列の各方向合計と階重量が一致することを確認する。

節点順の統一後は、ワークスペース2389件、GUI602件、MCP38件のテストが成功（既存ignoreはそれぞれ8件、7件、0件）。全構成のclippy（`--all-targets`）、fmt、差分チェックも成功。既存の壁モデルのスナップショットに追加変更はなかった。

## 上下辺の水平ずれと対応付け（2026-09-11）

下辺x=0〜4000 mm、上辺x=-5000〜-1000 mm、高さ3000 mmの壁で、上辺の左右が逆転することを回帰テストで再現した。下辺始点からの射影の絶対値を比べると、上辺が下辺始点より負側にあるとき順序が反転する。符号付き射影の昇順へ修正した。左右対応と周回面積12,000,000 mm²を検証する。幾何的な対応付けの修正であり、壁の適用形状を新しく拡張するものではない。

上下辺対応の修正後は要素328件・荷重163件、壁モデル9件・壁間柱モデル11件のテストが成功した。全構成のclippy（`--all-targets`）、fmt・差分チェックも成功。直前の全体テストとは区別し、この修正は上記範囲で検証した。

## 未使用の旧せん断形状係数の削除（2026-09-11）

§1.2で誤りを確認した `wall_shear_shape_factor(xi, eta)` が公開APIに残っていた。実計算での参照はなく、旧式専用テストのみが使用していたため、後方互換性を維持しない方針に従い関数・公開再エクスポート・専用テストを削除した。現行の `wall_shear_shape_factor_isection` と矩形極限・断面形状・有効せん断面積の検証を維持する。原典照合一覧の旧式参照も履歴として整理した。

旧κ式削除の確認（2026-09-12）: 断面性能36件のテストが成功。削除した関数のコード内参照はなく、ワークスペース・GUI・MCPのclippy（`--all-targets`）と差分チェックが成功した。

## せん断形状係数の解析積分（2026-09-12）

現行のせん断エネルギー積分を区間ごとの多項式として積分した。ウェブ・フランジ各200分割のSimpson則と境界判定用の微小量を削除した。同じ理論式の厳密積分であり、断面モデルは変更しない。断面二次モーメントも正の寄与の和へ整理した。

D=4、dc=1、bc=2、t=1ではA=6、I=10、半断面の積分が167/15+53/30なのでκ=387/250=1.548となる。0.001倍・1倍・1000倍の寸法で照合した。修正前はκ=1.548000000007498となり、許容差1e-12のテストが失敗した。修正後は断面性能37件が成功した。この差は微小であり、大きな計算誤差の修正ではなく、数値積分誤差と実装上の調整値の除去である。

計算根拠の「側柱が大きいほどκが増大する」という無条件の記述も訂正した。中央ウェブのせいが0なら矩形に戻るため、側柱せいに対して全範囲で単調増加するわけではない。矩形へ戻る極限を追加検証した。

解析積分への変更後、ワークスペース2390件・GUI602件・MCP38件のテストが成功（既存ignoreは8件・7件・0件）。矩形へ戻る極限の追加検証を含むκの5テストも成功。全構成のclippy（`--all-targets`）、fmt・差分チェックが成功した。

## 側柱付き壁の断面定義の不一致（2026-09-12、統一方針承認済み・実装未完了）

`WallElement::try_new_scaled` はκにD=lw+dc、フランジせいdcを与えるため、κの積分断面のウェブ長はlw-dcになる。一方、せん断剛性の分子は `t*lw+左右柱面積` であり、断面の重複部分を含む。lw=4000、t=150、左右600角ではκ算定断面が1,230,000 mm²、剛性の分子が1,320,000 mm²となり、同じκに対して約7.32%大きい。これはコード式の手計算による比較であり、実建物の応答誤差を測定した値ではない。

さらに、側柱せいは幅とせいの大きい方を左右平均し、側柱幅は小さい方の左右最大値としており、長方形柱の向きや左右差を保存していない。利用者は、壁と柱の重複を控除し、左右の柱断面・向きを個別に反映する断面定義への統一を承認した。

実装準備で、側柱に形状未定義の断面も指定できることを確認した。断面積・幅・せいのみでは重複領域を一意に復元できない。利用者の指示により、この入力はエラーとして形状の明示を必須とする。断面定義の統一自体は再確認不要であり、実装・検証は未完了。

## 壁自重を受ける柱の軸力復元（2026-09-12）

材軸方向の部材荷重について、`fixed_internal_local` が固定端節点力を符号変換せず線形内挿していた。切断片の釣合いによる `N(x) = -f_FEF,i,x - 切断位置までの外力合計` へ修正した。梁・ブレースの軸力に共通の修正である。

等分布・三角形分布・集中荷重の単体検証、および高さ1000 mmの下端固定・上端自由の鉛直柱に下向き2 N/mmを載せる解析テストを追加した。修正前は柱脚軸力が0 Nで理論値−2000 Nに対して失敗し、修正後は全評価断面で `N(x) = −2(1000−x)` に一致した。新しい近似の導入ではなく、断面の釣合いの修正である。

実建物スナップショットは柱脚軸力合計が−3.143e6 Nから−3.231e6 Nへ変化した。長期軸力から重心を算定する偏心率も変化する（`secondary/eccentricity_analysis.rs::center_of_gravity_from_axial`）。変位・固有周期・保有耐力は不変。柱脚軸力合計は基礎梁のせん断力を含まないため、総反力の指標とはしていない。ワークスペース全体2,392件、GUI付き602件、MCP付き38件が成功（既存ignoreはそれぞれ8件・7件・0件）。ワークスペース・GUI・MCPのclippyは全て `--all-targets --locked -- -D warnings` で成功し、fmtとdiffチェックも成功した。実験照合は未実施。

### 側柱の任意角度回転に関する実装準備

`side_column/detect.rs::release_axis_for_normal` は、壁法線と柱の局所y・z軸の内積を比較し、近い局所軸の曲げを解放する。局所軸が壁に平行・直交する0度／90度配置には対応するが、例えば45度では選んだ局所軸が壁法線と一致しない。左右断面・向きの統一では、断面積分に加え、壁面内の回転方向に対する端部解放を整合させる必要がある。任意角度を外接矩形や近い軸へ置換する方針は採らない。任意角度の端部解放の実装・回帰テストは未完了。

現行κ関数に残っていた削除済みの閉形式と原典照合待ちのコメントを削除し、現在実行する区分多項式の積分と引数契約のみを残した。計算式そのものは変更していない。

形状未定義の側柱を解析前の入力エラーにするチェックを追加した。壁展開を経由するアプリ診断で検出する回帰テストが成功。ワークスペース全体2,393件、GUI付き603件、MCP付き38件が成功し、3構成のclippy（all-targets）・fmt・diffチェックも成功した。断面積分の統一・任意角度の端部解放は引き続き未完了。

側柱の解放軸は、近い局所軸の選択から壁法線の局所y-z面への射影に変更した。剛性を解放方向へ座標変換し、既存の `condense_end_releases` で両端回転を縮約して戻す。独自の2自由度逆行列・特異時の行列切出し処理は削除した。0.2・0.7・1.3 radの解放方向で両端モーメントと面内並進抵抗がゼロとなり、剛性対称性も成立することを確認した。側柱の既存テストを含む10件が成功。断面積分の統一は未完了。

端部解放修正後の全体回帰はワークスペース2,394件・GUI603件・MCP38件が成功（既存ignoreは8件・7件・0件）。3構成のclippy（all-targets）・fmt・diffチェックも成功した。スナップショットの変更は生じていない。

### 左右非対称断面の積分処理（実装準備）

区分一定幅断面の面積・図心・I・Asを同一断面から求める `strip_section_properties` を追加し、既存の対称I形κ算定をこの処理へ接続した。Q²/bは区間内四次式で、3点Gauss積分で厳密に積分できる。幅方向対称・同一材料の前提を契約と計算根拠に明記した。

長さ1の2区間の幅を1・2とした非対称断面を有理数手計算と比較し、A=3、図心7/6、I=11/12、∫Q²/b=43/120、As=605/258に一致した。尺度0.001・1・1000、左右反転、矩形の区間分割の不変性を検証した。coreの断面テスト39件が成功。壁・左右柱の重複を除いた幾何をこの区間列へ接続する処理と、任意角度・円形等の断面への拡張は未完了。

区分一定幅断面の共通化後は、ワークスペース2,396件・GUI603件・MCP38件のテストが成功（既存ignoreは8件・7件・0件）。3構成のclippy（all-targets）・fmt・diffチェックも成功。既存スナップショットに変化はない。

### 複合断面と材料定数差の確認事項

`SectionShape::calc_area` はSRCでは外形総面積、CFTでは鋼管面積を返す。通常の柱剛性は複合換算断面性能で材料の剛性比を反映するが、壁のせん断断面は柱の `cs.area` を加算して壁材料のGを一律に乗じている。周辺架構チェックは材料カテゴリの一致のみであり、RC壁とSRC側柱や、壁と異なるEc・GのRC側柱を除外しない。

重複控除・左右個別の形状と向きの反映は承認済み。複合断面と壁・柱の材料定数差をせん断性能へ反映する方針も利用者が承認した（2026-09-13再開）。外形を一律に壁材料へ置換する近似や、未承認の対応形状制限は導入していない。

### 矩形の重複控除の幾何処理

材料評価の回答待ちに依存しない補助処理 `wall_rectangular_section_properties` を追加した。矩形境界で区間分割し、同一区間を覆う幅の最大値を取る。壁と柱だけでなく柱同士の重複も一度だけ数える。壁要素本体への接続は未実施で、既存の断面定義の不一致は未解消。

壁長4000・厚さ150・左右600角でA=1,230,000 mm²、左600×800・右400×500でA=1,205,000 mm²、左柱だけ90度回転した800×600ではA=1,190,000 mm²と手計算に一致。左右反転時の図心・As、壁より細い柱、柱同士の重複も検証した。共通積分を含む4件のテストが成功。今回の処理は平行矩形用で、任意角度・円形・複合材料の対応を済ませたものではない。

### 材料定数付き断面積分（2026-09-13）

区分一定幅断面の積分を材料定数付きの共通処理へ拡張し、既存の幾何断面積分はE=G=1でこの処理を呼ぶよう共通化した。区間ごとのEで弾性図心とDを、Gでせん断ひずみエネルギーを評価する。材料値のフォールバックはない。

長さ1・幅1の2区間にE=10・20、G=3・5を与える手計算で、面積2、弾性図心7/6、D=55/6、Ks=27225/4186に一致した。左右反転、Gの一律2倍、同一材料への帰着を検証し、core断面テスト43件が成功。これは区間内単一材料の積分であり、幅方向に異材が並ぶSRC/CFT断面・任意角度形状の接続は未完了。材料方針の回答待ちは解消している。

材料定数付き区間積分の追加後、ワークスペース2,400件・GUI603件・MCP38件が成功（既存ignoreは8件・7件・0件）。3構成のclippyはall-targets・locked・警告エラー化で成功し、fmtとdiffチェックも成功。stable更新を確認し、Rust 1.98.1で変更なし。壁要素への接続は完了していない。

### 矩形RC断面の壁要素への接続（2026-09-13、全形状対応は未完了）

`wall/shear_section.rs` を追加し、左右個別の形状・局所軸・材料から重複を控除したせん断剛性を算定するよう壁要素を接続した。旧来の柱幅の最大値・柱せいの平均値によるκ形状と、壁芯々面積＋柱面積の加算をせん断剛性から除去した。

現時点の接続範囲は平行・直交配置の矩形RC側柱。SRC/CFT・円形・任意角度・幅方向異材の接続は未完了で、暫定的に未対応の入力エラーとする。これはユーザー要件を狭める確定仕様ではなく、未実装形状に誤った旧式を残さないための途中状態。最終的には承認済みの全形状・複合材料への対応が必要。

左右600角の実要素でAs=1,230,000/κに一致すること、側柱のGだけを2倍にすると壁のKsも増加するが2倍未満であることを検証した。壁関連71件のテストが成功。材料未割当・壁形状を流用した柱断面のテストフィクスチャも、明示したRC柱断面と材料へ修正した。

壁モデルの水平変位は0.009435→0.009842 mm、せん断力・曲げ力の絶対値合計は3.322e7→3.321e7、保有耐力合計は8.747e5→8.729e5 Nとなる。断面重複を除いたせん断剛性低下に伴う変位・力の配分の変化であり、材料強度・個別Qu式は変更していない。地震用重量・DL結果・Dsとランクは不変。差分を確認してスナップショットを更新した。

### 長期軸力無効化と側柱形状の保持（2026-09-13）

レビューで、長期軸力無効化が複製断面の `shape` を削除し、壁の側柱断面入力を無効にする不具合を確認した。SRC/CFTでは合成断面の曲げ・せん断・ねじり剛性の再評価も失われていた。

`build_behavior_with_axial_factor` に要素生成後の軸剛性用断面積の低減を集約し、線形解析と引張専用ブレースの反復から使用する。入力モデルの断面・材料・形状は変更しない。

- 線形解析の27テストが成功。SRC柱の12×12剛性について鉛直軸成分だけが1e-6倍となり、他の成分と集中・整合質量が保持されることを確認した。
- 壁を含むAppの解析で `no_long_axial_column=true` を設定し、形状未定義エラーなしで結果が得られる回帰テストが成功。
- `cargo test --workspace --locked`: 2402成功・0失敗・8無視。
- `cargo test -p squid-n-app --features gui --locked`: 605成功・0失敗・7無視。
- `cargo test -p squid-n-mcp --features mcp --locked`: 38成功・0失敗。
- workspace・GUI・MCPの `clippy --all-targets --locked -- -D warnings`、fmt、依存方向検査、`git diff --check` が成功。

この検証は軸剛性低減と現在接続済みの断面を対象とする。SRC/CFT等を壁のせん断断面へ接続する作業の完了を意味しない。鋼材・コンクリートの完全付着は利用者承認済み。界面にすべりが生じる場合の変形を過小評価しうる危険側の近似であり、計算根拠にも記載した。
