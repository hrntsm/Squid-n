# Beam と Fiber の整合質量 — 2026-09

## 対象

等断面線材の整合質量、断面の材料領域質量、断面回転慣性、剛域質量、および Beam/Fiber 間の定式化一致を検証する。検証対象は端部解放を含む整合質量、剛域を含む質量変換、Beam/Fiber 共通化までである。

## 参照解と採用式

断面の単位長さ当たり質量と質量二次モーメントを材料領域から積分する。

\\[ \\mu = \\int_A \\rho\\,dA, \\qquad \\mu I_y = \\int_A \\rho z^2\\,dA, \\qquad \\mu I_z = \\int_A \\rho y^2\\,dA \\]

材軸まわりの回転慣性は `μIp = μIy + μIz` とする。Timoshenko の曲げ面形状関数と断面回転慣性を運動エネルギーへ積分し、剛域がある場合は可撓部の剛体アーム変換と剛域分布質量を加えて全長質量を保存する。端部解放では、初期弾性剛性から固定した `S=-Kbb^-1 Kba`, `R=[I;S]` で `M=Rᵀ Mbar R` とする。

## 検証項目と実装追跡

| 項目 | 実装関数・入口 | テスト | 検証済み条件 | 判定 |
|---|---|---|---|---|
| 材料領域質量（共通入口） | `Model::element_mass_properties` → `SectionMassProperties::from_section` | `形状なしは主材料の断面諸元へフォールバックする` | 形状なし断面、主材料の密度と断面諸元 | ✅ |
| 材料領域質量（RC/SRC/CFT） | `squid_n_core::model::mass::shaped_mass` | RC/SRC は標準 γRC/γSRC を総断面へ適用し、鉄筋・内蔵鉄骨の密度変更で質量が変わらないこと、Fc・ConcreteClass により RC 密度が変わることを確認。RC 壁・床版はせん断補強筋材料の設定を質量計算で無視する。CFT は鋼管と充填コンクリートを別領域で評価 | RC、SRC、CFT の材料領域 | ✅ |
| Timoshenko 質量の共通実装 | `squid_n_element::frame::prismatic::consistent_mass_timoshenko` | `ティモシェンコ質量はphi零で既存の曲げ並進質量に一致する`、`ティモシェンコ質量は回転慣性とphiを曲げ面へ反映する`、`ティモシェンコ質量のphi零極限は全成分で標準係数に一致する` | 固定端相当（端部解放なし）、phi=0、phi≠0、回転慣性、xy/xz 符号、対称性・半正定値性。phi>0 は製品の形状関数を呼ばない数値参照で積分 | ✅ |
| 断面軸から材端回転への配分 | `SectionMassProperties::polar_inertia_per_length`、`consistent_mass_timoshenko` | `回転慣性はsectionの軸を対応する材端回転へ配る` | 固定端相当、非対称断面軸の対応 | ✅ |
| 剛域質量の追加 | `rigid_zone_mass` | `質量変換と剛域質量は対称で質量を保存する` | 剛域あり、両端の分布質量、固定端相当。剛域の並進質量を端部ごとに一度だけ加算し、回転慣性・並進回転結合を保持 | ✅ |
| 可撓部質量の剛体アーム変換 | `transform_mass` | 同上 | 剛域あり、剛体アーム変換、固定端相当 | ✅ |
| Beam/Fiber の質量入口 | `BeamElement::mass_matrix`、`FiberBeam::mass_matrix` | `beamとfiberは同じ断面なら整合質量が一致する`、`非対称断面のbeamとfiberの各曲げブロックが一致する`、`rc_src_cftの材料領域質量はbeamとfiberの全成分で一致する` | 剛域なしの12×12、RC/SRC/CFT の同一材料領域質量・断面性能（全成分の相対差 1e-3 以内）、剛域あり・非対称断面の各曲げブロック、端部解放なし | ✅ |
| 全体座標変換 | 各 `mass_matrix` の `axis.to_global` | `任意方向材の整合質量は独立な座標変換と一致する` | 斜材の非恒等局所座標、12×12全成分、独立構成した `Rᵀ M_local R` | ✅ |
| 軸振動の固有値 | `BeamElement::mass_matrix` | `test_1dof_beam_consistent_axial_mass_matches_theory` | 軸方向、端部解放なし | ✅ |
| 純ねじり固有値 | `BeamElement::mass_matrix`、固有値経路 | `test_eigen_pure_torsion_uses_mass_polar_inertia_not_j` | Beam 経路、固定端–自由端、`GJ` と `ρ(Iy+Iz)` の参照解 | ✅ |
| Fiber 純ねじり行列 | `FiberBeam::mass_matrix` | `fiberの純ねじり行列は質量極二次モーメントを使う` | Fiber の局所剛性・整合質量行列、`GJ` と `ρ(Iy+Iz)` の比 | ✅ |
| Fiber 純ねじり固有値 | `FiberBeam::mass_matrix` は確認済み。`solve_eigen` の Fiber 非線形経路は未接続 | — | Fiber の局所行列は確認済みだが、固定端–自由端の Fiber 全体固有値経路は未検証 | 🔶 |
| 端部解放の整合質量 | `condense_end_releases_with_mass` | `端部解放質量はbeamとfiberで一致し剛体並進質量を保存する` | ピン・半剛、対称性、非負対角、PSD（二次形式）、剛域、剛体並進質量保存、回転慣性・並進回転結合 | ✅ |
| 材端集中ばね梁の整合質量 | `ConcentratedSpringBeam::mass_matrix` | ばね剛性を変えた整合質量回帰、Lumped 不変 | 剛性側と同じ端部ばね自由度を質量縮約へ適用 | ✅ |
| 壁側柱の解放質量 | `InPlaneReleasedColumn::mass_matrix` | 解放回転の回帰 | 剛性側と同じ DOF 写像・座標変換で整合質量を縮約 | ✅ |
| 4節点壁の整合質量 | `WallElement::mass_matrix` | 壁質量の Lumped 回帰、整合質量の写像・総質量 | 壁柱の面内ピン写像と内法質量補正 | ✅ |

## 実行結果

- `cargo test -p squid-n-core model::mass::tests`：11 passed（円形主筋の多段配置を含む）
- `cargo test -p squid-n-element prismatic -- --nocapture`：11 passed
- `cargo test -p squid-n-element rigid_arm -- --nocapture`：6 passed
- `beamとfiberは同じ断面なら整合質量が一致する`：剛域なしの12×12比較、passed
- `非対称断面のbeamとfiberは各曲げブロックが一致する`：剛域ありの各曲げブロック比較、passed
- `端部解放質量は独立縮約とbeamとfiberで一致し剛体並進質量を保存する`：ピン・半剛、複数解放、剛域あり、独立参照照合、対称性・非負対角、passed
- 端部解放を含む Beam/Fiber 質量：対称性、対角非負、PSD（二次形式）、総質量、および回転慣性・並進回転結合の非ゼロ値を確認、passed
- 軸振動固有値テスト：passed
- `test_eigen_pure_torsion_uses_mass_polar_inertia_not_j`：Beam 経路の純ねじり固有値と `GJ`・`ρ(Iy+Iz)` の参照解照合、passed
- `fiberの純ねじり行列は質量極二次モーメントを使う`：Fiber の局所剛性・整合質量行列と `GJ`・`ρ(Iy+Iz)` の参照比照合、passed
- `test_build_nonlinear_behavior_fiber_uses_fiber_beam`：`ElementKind::Fiber` から材料ファイバーを生成する非線形 factory 経路を確認、passed
- `任意方向材の整合質量は独立な座標変換と一致する`：斜材の12×12比較、passed

## 残る検証範囲

本レポートは理論式・行列性質・要素間一致の Verification であり、実測・商用ソフトとの Validation は扱わない。✅は上表の個別行に記載した条件に限る。`Kbb` 特異時は端部解放質量を返さず、Beam/Fiber の既存 12 自由度 API では明示的な panic とする。解放条件を満たさない質量をフォールバックで返さない。workspace 全体および feature 付きの PR 前検証は別途実行する。
本レポートは理論式・行列性質・要素間一致の Verification であり、実測・商用ソフトとの Validation は扱わない。✅は上表の個別行に記載した条件に限る。`Kbb` 特異時は端部解放質量を返さず、Beam/Fiber の既存 12 自由度 API では明示的な panic とする。解放条件を満たさない質量をフォールバックで返さない。固有値組立 API は線形 `build_behavior` を使うため、非線形 `ElementKind::Fiber` の FiberBeam を通る全体固有値検証は未実施である。workspace 全体および feature 付きの PR 前検証は別途実行する。
