# P5.5 壁・MS 要素 実装仕様書

**対象フェーズ:** v1.0 製品 / P5.5（P5 から分離した特殊非線形要素）
**対象読者:** Rust ジュニアエンジニア＋技術リード
**親文書:** `../構造計算一貫プログラム_実装設計書.md`（以降「設計書」。`§x.y` はその章番号）
**先行フェーズ:** [P0](P0_基盤.md)〜[P5](P5_非線形.md)（特に P4 材料/スケルトン・P5 commit/rollback・ファイバ要素）
**前提環境:** Rust stable 1.8x 以降

> **状態:** MS 要素は実装済み（`squid_n_element::frame::multi_spring::MultiSpringElement`）。
> 壁谷澤 TVLEM は採用しない（[ADR 0013](../adr/0013-adopt-wall-element-model.md)）。
> 耐震壁は壁エレメント置換モデル
> （[壁エレメントモデル](../../docs/calc_basis/04_要素剛性/05_壁エレメントモデル.md)）で扱う。

---

## 0. このフェーズについて

### 0.1 目的

P5 で見送った**特殊な非線形要素**のうち、**MS（マルチスプリング）要素**を追加する。設計書 §6.4。

- **MS 要素**：RC 柱・梁端部の塑性ヒンジを**軸ばね群**で表現。N–M 相関を自然に表現。
  ファイバ要素（P5）との**相互検証**で妥当性を担保。

> **壁谷澤 TVLEM は採用しない**（ADR 0013）。採用文献の確定が前提で文献間の定義揺れがあり、
> 実験照合（妥当性確認）が必須である一方、実装・GUI・入出力・数量積算・V&V の経路が揃った
> 壁エレメント置換モデルを採用する。

### 0.2 完了像（ゴール）

設計書 §6.4 の DoD:

1. **MS**：一定軸力下の繰り返し曲げで、N–M 相関・履歴ループが**ファイバ要素と整合**（相互検証。§3.1）。

### 0.3 スコープ境界（含む／含まない）

| 項目 | P5.5 で | 備考 |
|---|---|---|
| MS 要素（軸ばね群・N–M） | **含む（実装済み）** | §6.4 |
| 壁エレメント置換モデル | **採用** | [ADR 0013](../adr/0013-adopt-wall-element-model.md)。仕様は `docs/calc_basis/` |
| 壁谷澤 TVLEM（マクロ壁要素） | **採用しない** | ADR 0013 |
| commit/rollback・自動選択への統合 | **含む** | P5 §6/§5 |
| 非線形パネル・力ベースファイバ | **含まない → 将来** | 設計書 §6.3/§6.7 |

---

## 1. タスク一覧と依存順序

```
T1 MS 要素（軸ばね群・N–M）(§2)   ← P4, P5 ファイバ
T1 ─> T2 テスト・DoD（MS↔ファイバ相互検証）(§3)
```

| ID | タスク | クレート | 依存 |
|---|---|---|---|
| T1 | MS 要素 | squid-n-element | P4, P5 |
| T2 | テスト・DoD | 全体 | T1 |

---

## 2. T1: MS（マルチスプリング）要素

設計書 §6.4。主に **RC 柱・梁端部の塑性ヒンジ**表現。

- 部材端の塑性化域を **軸方向ばねの集合**に置換し、中央はファイバー梁の弾性部で連結する。
- 各ばねは**軸力負担位置（断面内座標）**を持ち、軸方向の非線形履歴則（コンクリばね・鉄筋ばね、P4）に従う。
- **曲げと軸力の相互作用（N–M 相関）を自然に表現**できるのが利点（軸ばね群の合力＝N、偶力＝M）。
- せん断はファイバ系要素のせん断直列合成（[P5.6](P5.6_せん断直列合成.md)）に従う。
- 履歴状態は `ElemState`／材料（P4）側。commit/rollback（P5 §6）に乗る。

```rust
// squid-n-element/src/frame/multi_spring.rs
pub struct AxialSpring {
    pub y: f64,
    pub z: f64,
    pub material: squid_n_core::ids::MaterialId,
}
pub struct MultiSpringElement {
    pub springs: Vec<AxialSpring>, // 軸ばね配置（両端共通）
    pub inner: FiberBeam,          // 端部塑性化域を持つファイバー梁（P5）
}
```

> **自動選択（P5 §5）との関係:** `AxialBendingInteract` 部材の既定はファイバ。MS は**選択肢**
> （`ForceRegime` 上書き or 明示指定）として割当可能にする。

**DoD（T1）:** 一定軸力の繰り返し曲げで、N–M 相関・履歴ループが**ファイバ要素（P5）と整合**（相互検証）。

---

## 3. テスト・DoD（フェーズ全体）

### 3.1 MS ↔ ファイバ 相互検証
- 一定軸力下の繰り返し曲げで、MS とファイバ（P5）の N–M 相関・履歴ループが整合（許容差）。
- 純曲げ（N=0）で両者が P4 の M–φ と整合。

### 3.2 フェーズ DoD チェックリスト
| # | ゴール（§0.2） | 判定 | 章 |
|---|---|---|---|
| 1 | MS↔ファイバ 相互検証 | §3.1 | §2 |

> **全チェック緑＝ P5.5 完了。**

---

## 4. 数式・理論補足（ジュニア向け）

### 4.1 MS の N–M 相関
断面を軸ばね群に置換すると、各ばねの軸力の**合力が N、図心まわりの偶力が M**。軸力が増えると各ばねの
状態が変わり M 耐力も変わる＝N–M 相関が自然に出る。ファイバ要素と原理が近く、相互検証できる
（ファイバは連続断面、MS は離散ばね）。

---

## 5. 用語
| 用語 | 意味 |
|---|---|
| MS 要素 | マルチスプリング。断面を軸ばね群に置換した塑性ヒンジモデル |
| 軸ばね | 断面内の一点で軸方向の非線形挙動を担うばね |
| N–M 相関 | 軸力と曲げ耐力の相互作用 |

---

*本仕様書は P5.5（MS 要素）を対象とする。壁谷澤 TVLEM は採用しない
（[ADR 0013](../adr/0013-adopt-wall-element-model.md)）。*
