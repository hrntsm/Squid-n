# 床領域と壁領域

> 領域は主架構が囲むパネル（または主架構への取り付き）ごとに 1 つとする。
> 版は任意。二次部材の実体を領域が持つ（D6）のは Step 3 の後。
> 決定と段取りの正本は [`handoff/床領域・壁領域の再設計_申し送り.md`](../handoff/床領域・壁領域の再設計_申し送り.md)。

本書は **いまの実装** を説明する。壁側（`WallRegion` が解析要素 ID を持つ形）はまだ旧設計である。

## 現在の実装状況

| 項目 | 状態 |
|---|---|
| `FloorRegion`（囲まれ / 取り付き、版は任意、名前） | 実装済み |
| 面走査 `region_gen`（水平大梁の閉路） | 実装済み（床のみ） |
| 取り込み・準備計算での入れ直し `rebuild_floor_regions` | 実装済み（Step 3） |
| 取り付き領域の編集コマンド・床タブ | 実装済み（Step 2b） |
| 小梁の所属（中点の厳密内包＋レベル一致） | 入れ直し時に付与。実体はまだ `Model.secondary_members` |
| 荷重分配を小梁で再分割 | 未（Step 4）。いまは領域境界の三角形・台形 |
| 壁領域の作り替え・要素生成 | 未（Step 7〜8） |

ST-Bridge の `StbSlab` は小片のまま入ったあと、大梁パネルへ畳む。
重心が複数パネルに入る（中庭の入れ子）ときは面積が最小のパネルへ付ける。
どのパネルにも入らず、水平大梁にちょうど 1 辺が全長載り自由端が 1〜2 点の版は取り付き領域へ変換する。
それ以外の未所属の版と所属の付かない小梁は警告し、モデルから落とさない。

## 型の構成（床）

| 型 | フィールド | 内容 |
|---|---|---|
| `FloorRegion.shape` | `Enclosed { boundary }` / `Attached { anchor, extent }` | 囲まれは大梁閉路。取り付きは線または点 |
| `FloorRegion.plate` | `Option<SlabPlate>` | `None` は版なし（吹抜け・中庭など） |
| `FloorRegion.secondary_joist_ids` | `Vec<SecondaryMemberId>` | 所属小梁。実体は横断リスト側 |

壁領域はまだ `WallRegion.wall: Option<ElemId>` と `post_ids` である（Step 7 で吸収する）。

## `SecondaryMemberId` の不変条件


`SecondaryMember.id` は、**`Model::secondary_members` の配列添字と一致していなければなりません**。
領域側は ID で子を指すため、添字とずれると別の部材を指してしまいます。

この不変条件は `Model::validate` が確認し、破れていれば `IndexMismatch` を返します。

二次部材をまとめて間引くときは、必ず `Model::retain_secondary_members` を通してください。
素の `Vec::retain` で消すと後続の `id` が添字とずれ、領域側の参照が黙って別の部材を指します。
`retain_secondary_members` は間引きと同時に ID を振り直し、`secondary_joist_ids` と `post_ids` の参照を新しい ID へ張り替えます。

## 検証の内容

`Model::validate` は、領域の参照について次を確認します。

- `SecondaryMember.id` が配列添字と一致する
- `secondary_joist_ids` の参照先が実在し、種別が `Joist` である
- `post_ids` の参照先が実在し、種別が `Post` である
- `WallRegion.wall` の参照先が実在し、種別が `ElementKind::Wall` である
- ひとつの領域のリスト内で ID が重複しない

種別まで見ているのは、小梁を壁領域に登録するような取り違えを早い段階で弾くためです。

**なお、床をまたいだ重複は検証していません。**
同じ小梁が 2 枚の床の `secondary_joist_ids` に入っていても、現状では通ります。
荷重分配が所属関係を使うようになると、この状態は床荷重の二重計上になりますので、そのときに検証を追加してください。

## 参照整合の追随先

`SecondaryMemberId` を持つフィールドを `Model` へ追加したら、`Model::visit_secondary_member_ids` へ必ず追随してください。
二次部材の削除・挿入にともなう ID の繰り上げと繰り下げは、この走査を単一の情報源としています。

同じく、要素を削除する `DeleteMember` は `WallRegion.wall` を「版なし」へ戻します。
残したまま ID を繰り上げると、削除した壁の次にあった要素を指すことになるためです。
繰り上がった先がたまたま別の壁だと検証も通ってしまい、壁領域が黙って別の壁に付け替わります。

## 既定値

- `FloorRegion.secondary_joist_ids`: 空（`#[serde(default)]`）
- `Model.wall_regions`: 空（`#[serde(default)]`）
- レベル一致: `LEVEL_TOL_MM` = 1 mm
- 辺上を内部に含めない: `BOUNDARY_TOL_MM` = 1 mm
- 辺の全長被覆・材軸近さ: `MEMBER_AXIS_TOL_MM` = 10 mm
- 重心照合の面積相対差: `CENTROID_MATCH_AREA_REL` = 1e-3
- 片持ち変換後の取付き線: `span = [0, 1]`、`LoadTransfer::Anchor`（左が正）
- 版仕様が混在したパネル: `plate = None`

`SecondaryMember.id` には `#[serde(default)]` を付けていません。
既定値で補うと不変条件が破れたモデルを黙って受け入れることになり、`validate` で検出したい状態がそのまま読み込まれてしまうためです。
未リリースのため旧スキーマの互換は取らず、読めない古いファイルは読込時にエラーとします。

## 残課題

- 領域を編集するナビゲータ UI と MCP（Step 6）
- 荷重分配を小梁再分割へ（Step 4）、小梁設計を分配結果から出す（Step 5）
- 床をまたいだ小梁の重複を `validate` で止める（入れ直しは一意にするが、手編集の重複はまだ通る）
- `Model::secondary_members` の横断リストの廃止（D6。領域への移行が完了したあと）
- 壁領域の作り替え（Step 7〜9）
