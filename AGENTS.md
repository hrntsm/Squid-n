# Squid-n

Rustで作成する日本の建築構造計算一貫プログラム。
日本での利用を想定しており、コミットメッセージ・コード中のコメント・プロンプトへの応答などは日本語で行うこと。

ビルド・テスト・静的解析・機能フラグ・ドキュメントの手順は [CONTRIBUTING.md](CONTRIBUTING.md) に従う。
本書は重複を避け、エージェントとして特に徹底すべき事項のみを記載する。

## Gitコミットルール

コミットメッセージの 1 行目、および Pull Request のタイトルは、必ず以下のプレフィックスから始めること。
書式: `<プレフィックス>: <簡潔な変更内容>`

- **feat**: 新しい機能
- **fix**: バグの修正
- **docs**: ドキュメントのみの変更
- **style**: 空白、フォーマット、セミコロン追加など（コードの動作に影響しない変更）
- **refactor**: 仕様に影響がないコード改善（リファクタリング）
- **perf**: パフォーマンス向上関連
- **test**: テスト関連
- **chore**: ビルド、補助ツール、ライブラリ関連

## 実装方針

- 後方互換性は維持しない。
- 現在の要件を完全に満たす最もシンプルな実装を選ぶ。
- 自作実装より、確立された保守が行き届いたライブラリを優先する。
- 型・関数・UI 文言・コメントで新しい概念に名前を付ける前に、
  [dev_docs/specs/用語集.md](dev_docs/specs/用語集.md) を確認し、認識違い・重複した呼び名を
  防ぐ。維持の仕方は「ドキュメントの書き分け」の開発者向け節を参照。
- 構造計算上の近似・簡略化を選ぶときは、精度と実装量のトレードオフである前に、
  **安全側か危険側かをまず判定する**。実際の応力・変形・重量を過小評価しうる危険側の
  簡略化を、実装量の小ささだけを理由に選んではならない。安全側か危険側か自明でない場合、
  および安全側であっても利用者に開示すべき近似である場合は、実装を進める前にユーザーへ確認する。
  危険側の簡略化は、ユーザーが明示的にそれを指示したときに限り許容し、その場合も
  「危険側の近似である」ことを申し送り・`docs/` の計算根拠に明記する。
  コード中のコメントでは管理しない（陳腐化したコメントによる判断ミスを防ぐため）。

## コード中のコメント記載基準

- `//` は原則書かない。未対応事項のマーカー（`TODO`・`FIXME` 等）のみ例外とする。
- `///`・`//!` に残すのは、型・関数名で表せない契約のみとする。
  残してよいのは、単位・前提条件・失敗時の振る舞い・順序等の取り決め（例: タプルの要素順序）。
  算定の導出・設計理由・既定値・代表値は書かず、`docs/`（利用者向け計算根拠）または
  `dev_docs/`（開発経緯・申し送り）に記載する。
- 単位は可能な範囲で名前に埋め込む（例: 内部実装の引数・定数名に単位を含める）。
  公開 API の改名は波及が大きいため、別課題として扱い `///` の単位記載で担保する。
- テスト内の検証意図・手計算導出の注記は残してよい。陳腐化すればテスト自体が失敗するため検出可能である。
- 計算根拠・既定値・安全側／危険側の判定は `docs/` に集約し、コード側から文書へのリンクは残さない。
  `docs/` 側に実装箇所を記載し、`docs/` からコードへさかのぼれるようにする。

## コミット前に必須

- **静的解析（clippy / fmt）を CI と同条件で実行する**。特に `--all-targets` を省略しないこと
  （省略するとテストコードが clippy の対象外になる）。コマンドは CONTRIBUTING.md「静的解析」を参照。
- 変更に対応するドキュメントを更新する（下記「ドキュメントの書き分け」）。

## ドキュメントの書き分け

ドキュメントは目的別に 2 系統に分かれている。作業終了時には、変更に対応する系統を必ず更新すること。

### 利用者向け: `docs/`（mdBook）

- **利用者向けの計算根拠**として記述する。
- **開発経緯・過去との差分（変更前後の比較、実装のビフォーアフター、履歴的な言及）には触れてはいけない**。
  あくまで現在の仕様・計算根拠のみを、それ単体で完結する形で記述する。
- **既定値（デフォルト設定）はすべて記載する**。コードに埋め込まれた既定
  （材料モデルの既定パラメータ・強度の解決順・分割解像度・未設定時の扱い等）は、
  利用者が結果の根拠を追えるよう該当ページに明記すること。
- ビルド・プレビュー手順は CONTRIBUTING.md「ドキュメントサイト（mdBook）」を参照。

### 開発者向け: `dev_docs/`

- **開発経緯・過去との差分、V&V（検証記録）、申し送り事項などは、`docs/` には書かず `dev_docs/` に反映する**こと。
  配置するカテゴリ（specs / v_and_v / handoff）は [dev_docs/README.md](dev_docs/README.md) を参照。
- 申し送り・V&V を追加・更新したら、[CONTRIBUTING.md の dev_docs 節](CONTRIBUTING.md#dev_docs申し送りv) に従い、次も更新する:
  - `dev_docs/handoff/README.md`（目録）
  - `dev_docs/handoff/残課題一覧.md`（残課題がある場合）
  - `dev_docs/v_and_v/README.md`（レポート目録・索引 #N）
  - `dev_docs/v_and_v/未検証一覧.md`（未検証・一部が残る場合）
- **[dev_docs/specs/用語集.md](dev_docs/specs/用語集.md)** は実装に登場する概念とその呼び名の
  集約先。実装で新しく登場した用語（型・概念・呼称）は、その都度この用語集へ追記する。
  「誤用に気づいたら直す」だけでは、気づくまでの間は誤用したままになる。常に現状の
  全用語を映した状態に保つことで、次に同じ概念へ名前を付けようとしたときに既存の用語が
  見つかり、重複や混同を作る前に防げる（「パネル」が柱梁接合部と床の区画の両方に
  使われていた混同はこの運用がなかったために起きた）。

## プルリクエスト

- PR タイトルはコミットメッセージと同じプレフィックス規約に従い、`<プレフィックス>: <簡潔な変更内容>` とする。
- PR 本文は `.github/pull_request_template.md` の構成に従う。
- 本文では、**概要・変更内容・検証**を必ず具体的に記載する。UI や利用者から見える挙動を変更した場合は、その内容も明記する。
- 既存仕様からの変更、後方互換性のない変更、既知の残課題がある場合は、PR 本文の「影響・注意事項」「残課題」に記載する。
- `docs/` または `dev_docs/` の更新が必要な変更では、更新した文書と変更理由を PR 本文に記載する。
- レビューで特に確認してほしい点がある場合は、PR 本文の「レビューで確認してほしい点」に記載する。
